ScriptName = "pfx_ScriptName" -- Provide Moho with the name of this script object
CHANNEL_LAYER_T = 10000 --Value: 10000 | Ver: 13.5.3
CHANNEL_LAYER_S = 10001 --Value: 10001 | Ver: 13.5.3
CHANNEL_LAYER_ROT_Z = 10002 --Value: 10002 | Ver: 13.5.3
CHANNEL_LAYER_ROT_Y = 10003 --Value: 10003 | Ver: 13.5.3
CHANNEL_LAYER_ROT_X = 10004 --Value: 10004 | Ver: 13.5.3
CHANNEL_LAYER_FLIP_H = 10005 --Value: 10005 | Ver: 13.5.3
CHANNEL_LAYER_FLIP_V = 10006 --Value: 10006 | Ver: 13.5.3
CHANNEL_LAYER_SH = 10007 --Value: 10007 | Ver: 13.5.3
CHANNEL_LAYER_FOLLOW = 10008 --Value: 10008 | Ver: 13.5.3
CHANNEL_PHYSICS_NUDGE = 10009 --Value: 10009 | Ver: 13.5.3
CHANNEL_PHYSICS_MOTOR = 10010 --Value: 10010 | Ver: 13.5.3
CHANNEL_PHYSICS_FORCE = 10011 --Value: 10011 | Ver: 13.5.3
CHANNEL_LAYER_VIS = 10012 --Value: 10012 | Ver: 13.5.3
CHANNEL_LAYER_BLUR = 10013 --Value: 10013 | Ver: 13.5.3
CHANNEL_LAYER_ALPHA = 10014 --Value: 10014 | Ver: 13.5.3
CHANNEL_LAYER_AO = 10015 --Value: 10015 | Ver: 13.5.3
CHANNEL_LAYER_THRESHOLD = 10016 --Value: 10016 | Ver: 13.5.3
CHANNEL_LAYER_NOISE = 10017 --Value: 10017 | Ver: 13.5.3
CHANNEL_LAYER_PIXELATION = 10018 --Value: 10018 | Ver: 13.5.3
CHANNEL_LAYER_SHADOW = 10019 --Value: 10019 | Ver: 13.5.3
CHANNEL_LAYER_SHADING = 10020 --Value: 10020 | Ver: 13.5.3
CHANNEL_LAYER_MB = 10021 --Value: 10021 | Ver: 13.5.3
CHANNEL_LAYER_PERSPSHADOW = 10022 --Value: 10022 | Ver: 13.5.3
CHANNEL_LAYER_OUTLINE = 10023 --Value: 10023 | Ver: 13.5.3
CHANNEL_LAYER_COLOR = 10024 --Value: 10024 | Ver: 13.5.3
CHANNEL_LAYER_MARKERS = 10025 --Value: 10025 | Ver: 13.5.3
CHANNEL_LAYER_ALL = 10026 --Value: 10026 | Ver: 13.5.3
CHANNEL_POINT = 10030 --Value: 10030 | Ver: 13.5.3
CHANNEL_POINT_SEL = 10031 --Value: 10031 | Ver: 13.5.3
CHANNEL_CURVE = 10032 --Value: 10032 | Ver: 13.5.3
CHANNEL_CURVE_SEL = 10033 --Value: 10033 | Ver: 13.5.3
CHANNEL_FILL = 10034 --Value: 10034 | Ver: 13.5.3
CHANNEL_FILL_SEL = 10035 --Value: 10035 | Ver: 13.5.3
CHANNEL_LINE = 10036 --Value: 10036 | Ver: 13.5.3
CHANNEL_LINE_SEL = 10037 --Value: 10037 | Ver: 13.5.3
CHANNEL_WIDTH = 10038 --Value: 10038 | Ver: 13.5.3
CHANNEL_WIDTH_SEL = 10039 --Value: 10039 | Ver: 13.5.3
CHANNEL_CURVEEXP = 10040 --Value: 10040 | Ver: 13.5.3
CHANNEL_CURVEEXP_SEL = 10041 --Value: 10041 | Ver: 13.5.3
CHANNEL_SHAPEFX = 10042 --Value: 10042 | Ver: 13.5.3
CHANNEL_SHAPEFX_SEL = 10043 --Value: 10043 | Ver: 13.5.3
CHANNEL_FXXFORM = 10044 --Value: 10044 | Ver: 13.5.3
CHANNEL_FXXFORM_SEL = 10045 --Value: 10045 | Ver: 13.5.3
CHANNEL_THICKNESS = 10046 --Value: 10046 | Ver: 13.5.3
CHANNEL_THICKNESS_SEL = 10047 --Value: 10047 | Ver: 13.5.3
CHANNEL_POINTCOL = 10048 --Value: 10048 | Ver: 13.5.3
CHANNEL_POINTCOL_SEL = 10049 --Value: 10049 | Ver: 13.5.3
CHANNEL_POINTSTRENGTH = 10050 --Value: 10050 | Ver: 13.5.3
CHANNEL_POINTSTRENGTH_SEL = 10051 --Value: 10051 | Ver: 13.5.3
CHANNEL_SHAPE_ORDER = 10052 --Value: 10052 | Ver: 13.5.3
CHANNEL_BONE = 10053 --Value: 10053 | Ver: 13.5.3
CHANNEL_BONE_SEL = 10054 --Value: 10054 | Ver: 13.5.3
CHANNEL_BONE_1 = 10055 --Value: 10055 | Ver: 13.5.3
CHANNEL_BONE_2 = 10056 --Value: 10056 | Ver: 13.5.3
CHANNEL_BONE_3 = 10057 --Value: 10057 | Ver: 13.5.3
CHANNEL_BONE_4 = 10058 --Value: 10058 | Ver: 13.5.3
CHANNEL_BONE_5 = 10059 --Value: 10059 | Ver: 13.5.3
CHANNEL_BONE_6 = 10060 --Value: 10060 | Ver: 13.5.3
CHANNEL_BONE_7 = 10061 --Value: 10061 | Ver: 13.5.3
CHANNEL_BONE_8 = 10062 --Value: 10062 | Ver: 13.5.3
CHANNEL_BONE_9 = 10063 --Value: 10063 | Ver: 13.5.3
CHANNEL_BONE_10 = 10064 --Value: 10064 | Ver: 13.5.3
CHANNEL_BONE_11 = 10065 --Value: 10065 | Ver: 13.5.3
CHANNEL_BONE_T = 10066 --Value: 10066 | Ver: 13.5.3
CHANNEL_BONE_SEL_T = 10067 --Value: 10067 | Ver: 13.5.3
CHANNEL_BONE_1_T = 10068 --Value: 10068 | Ver: 13.5.3
CHANNEL_BONE_2_T = 10069 --Value: 10069 | Ver: 13.5.3
CHANNEL_BONE_3_T = 10070 --Value: 10070 | Ver: 13.5.3
CHANNEL_BONE_4_T = 10071 --Value: 10071 | Ver: 13.5.3
CHANNEL_BONE_5_T = 10072 --Value: 10072 | Ver: 13.5.3
CHANNEL_BONE_6_T = 10073 --Value: 10073 | Ver: 13.5.3
CHANNEL_BONE_7_T = 10074 --Value: 10074 | Ver: 13.5.3
CHANNEL_BONE_8_T = 10075 --Value: 10075 | Ver: 13.5.3
CHANNEL_BONE_9_T = 10076 --Value: 10076 | Ver: 13.5.3
CHANNEL_BONE_10_T = 10077 --Value: 10077 | Ver: 13.5.3
CHANNEL_BONE_11_T = 10078 --Value: 10078 | Ver: 13.5.3
CHANNEL_BONE_S = 10079 --Value: 10079 | Ver: 13.5.3
CHANNEL_BONE_SEL_S = 10080 --Value: 10080 | Ver: 13.5.3
CHANNEL_BONE_1_S = 10081 --Value: 10081 | Ver: 13.5.3
CHANNEL_BONE_2_S = 10082 --Value: 10082 | Ver: 13.5.3
CHANNEL_BONE_3_S = 10083 --Value: 10083 | Ver: 13.5.3
CHANNEL_BONE_4_S = 10084 --Value: 10084 | Ver: 13.5.3
CHANNEL_BONE_5_S = 10085 --Value: 10085 | Ver: 13.5.3
CHANNEL_BONE_6_S = 10086 --Value: 10086 | Ver: 13.5.3
CHANNEL_BONE_7_S = 10087 --Value: 10087 | Ver: 13.5.3
CHANNEL_BONE_8_S = 10088 --Value: 10088 | Ver: 13.5.3
CHANNEL_BONE_9_S = 10089 --Value: 10089 | Ver: 13.5.3
CHANNEL_BONE_10_S = 10090 --Value: 10090 | Ver: 13.5.3
CHANNEL_BONE_11_S = 10091 --Value: 10091 | Ver: 13.5.3
CHANNEL_BONE_FLIPH = 10092 --Value: 10092 | Ver: 13.5.3
CHANNEL_BONE_SEL_FLIPH = 10093 --Value: 10093 | Ver: 13.5.3
CHANNEL_BONE_FLIPV = 10094 --Value: 10094 | Ver: 13.5.3
CHANNEL_BONE_SEL_FLIPV = 10095 --Value: 10095 | Ver: 13.5.3
CHANNEL_BONE_LOCK = 10096 --Value: 10096 | Ver: 13.5.3
CHANNEL_BONE_SEL_LOCK = 10097 --Value: 10097 | Ver: 13.5.3
CHANNEL_BONE_TARGET = 10098 --Value: 10098 | Ver: 13.5.3
CHANNEL_BONE_SEL_TARGET = 10099 --Value: 10099 | Ver: 13.5.3
CHANNEL_BONE_PARENT = 10100 --Value: 10100 | Ver: 13.5.3
CHANNEL_BONE_SEL_PARENT = 10101 --Value: 10101 | Ver: 13.5.3
CHANNEL_BONE_DYNAMICS = 10102 --Value: 10102 | Ver: 13.5.3
CHANNEL_BONE_SEL_DYNAMICS = 10103 --Value: 10103 | Ver: 13.5.3
CHANNEL_BONE_MOTOR = 10104 --Value: 10104 | Ver: 13.5.3
CHANNEL_BONE_SEL_MOTOR = 10105 --Value: 10105 | Ver: 13.5.3
CHANNEL_BONE_GROUP = 10106 --Value: 10106 | Ver: 13.5.3
CHANNEL_BONE_GROUP_PARTS = 10107 --Value: 10107 | Ver: 13.5.3
CHANNEL_BONE_SEL_GROUP = 10108 --Value: 10108 | Ver: 13.5.3
CHANNEL_BONE_SEL_GROUP_PARTS = 10109 --Value: 10109 | Ver: 13.5.3
CHANNEL_BONE_WIND_DIR = 10110 --Value: 10110 | Ver: 13.5.3
CHANNEL_BONE_WIND_STRENGTH = 10111 --Value: 10111 | Ver: 13.5.3
CHANNEL_BONE_WIND_TURBULENCE = 10112 --Value: 10112 | Ver: 13.5.3
CHANNEL_SWITCH = 10113 --Value: 10113 | Ver: 13.5.3
CHANNEL_LAYER_ORDER = 10114 --Value: 10114 | Ver: 13.5.3
CHANNEL_ENABLE_PHYSICS = 10115 --Value: 10115 | Ver: 13.5.3
CHANNEL_PARTICLE = 10116 --Value: 10116 | Ver: 13.5.3
CHANNEL_POSER = 10117 --Value: 10117 | Ver: 13.5.3
CHANNEL_POSER_SEL = 10118 --Value: 10118 | Ver: 13.5.3
CHANNEL_AUDIO_LEVEL = 10119 --Value: 10119 | Ver: 13.5.3
CHANNEL_AUDIO_JUMP = 10120 --Value: 10120 | Ver: 13.5.3
CHANNEL_TRACKING_POINT = 10121 --Value: 10121 | Ver: 13.5.3
CHANNEL_TRACKING_POINT_SEL = 10122 --Value: 10122 | Ver: 13.5.3
CHANNEL_DOC_MARKERS = 10130 --Value: 10130 | Ver: 13.5.3
CHANNEL_CAMERA_TRACK = 10131 --Value: 10131 | Ver: 13.5.3
CHANNEL_CAMERA_ZOOM = 10132 --Value: 10132 | Ver: 13.5.3
CHANNEL_CAMERA_ROLL = 10133 --Value: 10133 | Ver: 13.5.3
CHANNEL_CAMERA_PANTILT = 10134 --Value: 10134 | Ver: 13.5.3

CHANNEL_LAYER_T = 10000 --Value: 10000 | Ver: 14.3
CHANNEL_LAYER_S = 10001 --Value: 10001 | Ver: 14.3
CHANNEL_LAYER_ROT_Z = 10002 --Value: 10002 | Ver: 14.3
CHANNEL_LAYER_ROT_Y = 10003 --Value: 10003 | Ver: 14.3
CHANNEL_LAYER_ROT_X = 10004 --Value: 10004 | Ver: 14.3
CHANNEL_LAYER_FLIP_H = 10005 --Value: 10005 | Ver: 14.3
CHANNEL_LAYER_FLIP_V = 10006 --Value: 10006 | Ver: 14.3
CHANNEL_LAYER_SH = 10007 --Value: 10007 | Ver: 14.3
CHANNEL_LAYER_FOLLOW = 10008 --Value: 10008 | Ver: 14.3
CHANNEL_PHYSICS_NUDGE = 10009 --Value: 10009 | Ver: 14.3
CHANNEL_PHYSICS_MOTOR = 10010 --Value: 10010 | Ver: 14.3
CHANNEL_PHYSICS_FORCE = 10011 --Value: 10011 | Ver: 14.3
CHANNEL_LAYER_VIS = 10012 --Value: 10012 | Ver: 14.3
CHANNEL_LAYER_BLUR = 10013 --Value: 10013 | Ver: 14.3
CHANNEL_LAYER_ALPHA = 10014 --Value: 10014 | Ver: 14.3
CHANNEL_LAYER_AO = 10015 --Value: 10015 | Ver: 14.3
CHANNEL_LAYER_THRESHOLD = 10016 --Value: 10016 | Ver: 14.3
CHANNEL_LAYER_NOISE = 10017 --Value: 10017 | Ver: 14.3
CHANNEL_LAYER_PIXELATION = 10018 --Value: 10018 | Ver: 14.3
CHANNEL_LAYER_SHADOW = 10019 --Value: 10019 | Ver: 14.3
CHANNEL_LAYER_SHADING = 10020 --Value: 10020 | Ver: 14.3
CHANNEL_LAYER_MB = 10021 --Value: 10021 | Ver: 14.3
CHANNEL_LAYER_PERSPSHADOW = 10022 --Value: 10022 | Ver: 14.3
CHANNEL_LAYER_OUTLINE = 10023 --Value: 10023 | Ver: 14.3
CHANNEL_LAYER_COLOR = 10024 --Value: 10024 | Ver: 14.3
CHANNEL_LAYER_MARKERS = 10025 --Value: 10025 | Ver: 14.3
CHANNEL_LAYER_ALL = 10026 --Value: 10026 | Ver: 14.3
CHANNEL_POINT = 10030 --Value: 10030 | Ver: 14.3
CHANNEL_POINT_SEL = 10031 --Value: 10031 | Ver: 14.3
CHANNEL_CURVE = 10032 --Value: 10032 | Ver: 14.3
CHANNEL_CURVE_SEL = 10033 --Value: 10033 | Ver: 14.3
CHANNEL_FILL = 10034 --Value: 10034 | Ver: 14.3
CHANNEL_FILL_SEL = 10035 --Value: 10035 | Ver: 14.3
CHANNEL_LINE = 10036 --Value: 10036 | Ver: 14.3
CHANNEL_LINE_SEL = 10037 --Value: 10037 | Ver: 14.3
CHANNEL_WIDTH = 10038 --Value: 10038 | Ver: 14.3
CHANNEL_WIDTH_SEL = 10039 --Value: 10039 | Ver: 14.3
CHANNEL_OPACITY = 10040 --Value: 10040 | Ver: 14.3
CHANNEL_OPACITY_SEL = 10041 --Value: 10041 | Ver: 14.3
CHANNEL_COLOR_DRIFT = 10042 --Value: 10042 | Ver: 14.3
CHANNEL_COLOR_DRIFT_SEL = 10043 --Value: 10043 | Ver: 14.3
CHANNEL_BLEND = 10044 --Value: 10044 | Ver: 14.3
CHANNEL_BLEND_SEL = 10045 --Value: 10045 | Ver: 14.3
CHANNEL_CURVEEXP = 10046 --Value: 10046 | Ver: 14.3
CHANNEL_CURVEEXP_SEL = 10047 --Value: 10047 | Ver: 14.3
CHANNEL_PROFILEOFFSET = 10048 --Value: 10048 | Ver: 14.3
CHANNEL_PROFILEOFFSET_SEL = 10049 --Value: 10049 | Ver: 14.3
CHANNEL_SHAPEFX = 10050 --Value: 10050 | Ver: 14.3
CHANNEL_SHAPEFX_SEL = 10051 --Value: 10051 | Ver: 14.3
CHANNEL_FXXFORM = 10052 --Value: 10052 | Ver: 14.3
CHANNEL_FXXFORM_SEL = 10053 --Value: 10053 | Ver: 14.3
CHANNEL_THICKNESS = 10054 --Value: 10054 | Ver: 14.3
CHANNEL_THICKNESS_SEL = 10055 --Value: 10055 | Ver: 14.3
CHANNEL_POINTCOL = 10056 --Value: 10056 | Ver: 14.3
CHANNEL_POINTCOL_SEL = 10057 --Value: 10057 | Ver: 14.3
CHANNEL_POINTSTRENGTH = 10058 --Value: 10058 | Ver: 14.3
CHANNEL_POINTSTRENGTH_SEL = 10059 --Value: 10059 | Ver: 14.3
CHANNEL_SHAPE_ORDER = 10060 --Value: 10060 | Ver: 14.3
CHANNEL_BONE = 10061 --Value: 10061 | Ver: 14.3
CHANNEL_BONE_SEL = 10062 --Value: 10062 | Ver: 14.3
CHANNEL_BONE_1 = 10063 --Value: 10063 | Ver: 14.3
CHANNEL_BONE_2 = 10064 --Value: 10064 | Ver: 14.3
CHANNEL_BONE_3 = 10065 --Value: 10065 | Ver: 14.3
CHANNEL_BONE_4 = 10066 --Value: 10066 | Ver: 14.3
CHANNEL_BONE_5 = 10067 --Value: 10067 | Ver: 14.3
CHANNEL_BONE_6 = 10068 --Value: 10068 | Ver: 14.3
CHANNEL_BONE_7 = 10069 --Value: 10069 | Ver: 14.3
CHANNEL_BONE_8 = 10070 --Value: 10070 | Ver: 14.3
CHANNEL_BONE_9 = 10071 --Value: 10071 | Ver: 14.3
CHANNEL_BONE_10 = 10072 --Value: 10072 | Ver: 14.3
CHANNEL_BONE_11 = 10073 --Value: 10073 | Ver: 14.3
CHANNEL_BONE_T = 10074 --Value: 10074 | Ver: 14.3
CHANNEL_BONE_SEL_T = 10075 --Value: 10075 | Ver: 14.3
CHANNEL_BONE_1_T = 10076 --Value: 10076 | Ver: 14.3
CHANNEL_BONE_2_T = 10077 --Value: 10077 | Ver: 14.3
CHANNEL_BONE_3_T = 10078 --Value: 10078 | Ver: 14.3
CHANNEL_BONE_4_T = 10079 --Value: 10079 | Ver: 14.3
CHANNEL_BONE_5_T = 10080 --Value: 10080 | Ver: 14.3
CHANNEL_BONE_6_T = 10081 --Value: 10081 | Ver: 14.3
CHANNEL_BONE_7_T = 10082 --Value: 10082 | Ver: 14.3
CHANNEL_BONE_8_T = 10083 --Value: 10083 | Ver: 14.3
CHANNEL_BONE_9_T = 10084 --Value: 10084 | Ver: 14.3
CHANNEL_BONE_10_T = 10085 --Value: 10085 | Ver: 14.3
CHANNEL_BONE_11_T = 10086 --Value: 10086 | Ver: 14.3
CHANNEL_BONE_S = 10087 --Value: 10087 | Ver: 14.3
CHANNEL_BONE_SEL_S = 10088 --Value: 10088 | Ver: 14.3
CHANNEL_BONE_1_S = 10089 --Value: 10089 | Ver: 14.3
CHANNEL_BONE_2_S = 10090 --Value: 10090 | Ver: 14.3
CHANNEL_BONE_3_S = 10091 --Value: 10091 | Ver: 14.3
CHANNEL_BONE_4_S = 10092 --Value: 10092 | Ver: 14.3
CHANNEL_BONE_5_S = 10093 --Value: 10093 | Ver: 14.3
CHANNEL_BONE_6_S = 10094 --Value: 10094 | Ver: 14.3
CHANNEL_BONE_7_S = 10095 --Value: 10095 | Ver: 14.3
CHANNEL_BONE_8_S = 10096 --Value: 10096 | Ver: 14.3
CHANNEL_BONE_9_S = 10097 --Value: 10097 | Ver: 14.3
CHANNEL_BONE_10_S = 10098 --Value: 10098 | Ver: 14.3
CHANNEL_BONE_11_S = 10099 --Value: 10099 | Ver: 14.3
CHANNEL_BONE_FLIPH = 10100 --Value: 10100 | Ver: 14.3
CHANNEL_BONE_SEL_FLIPH = 10101 --Value: 10101 | Ver: 14.3
CHANNEL_BONE_FLIPV = 10102 --Value: 10102 | Ver: 14.3
CHANNEL_BONE_SEL_FLIPV = 10103 --Value: 10103 | Ver: 14.3
CHANNEL_BONE_LOCK = 10104 --Value: 10104 | Ver: 14.3
CHANNEL_BONE_SEL_LOCK = 10105 --Value: 10105 | Ver: 14.3
CHANNEL_BONE_TARGET = 10106 --Value: 10106 | Ver: 14.3
CHANNEL_BONE_SEL_TARGET = 10107 --Value: 10107 | Ver: 14.3
CHANNEL_BONE_PARENT = 10108 --Value: 10108 | Ver: 14.3
CHANNEL_BONE_SEL_PARENT = 10109 --Value: 10109 | Ver: 14.3
CHANNEL_BONE_DYNAMICS = 10110 --Value: 10110 | Ver: 14.3
CHANNEL_BONE_SEL_DYNAMICS = 10111 --Value: 10111 | Ver: 14.3
CHANNEL_BONE_MOTOR = 10112 --Value: 10112 | Ver: 14.3
CHANNEL_BONE_SEL_MOTOR = 10113 --Value: 10113 | Ver: 14.3
CHANNEL_BONE_GROUP = 10114 --Value: 10114 | Ver: 14.3
CHANNEL_BONE_GROUP_PARTS = 10115 --Value: 10115 | Ver: 14.3
CHANNEL_BONE_SEL_GROUP = 10116 --Value: 10116 | Ver: 14.3
CHANNEL_BONE_SEL_GROUP_PARTS = 10117 --Value: 10117 | Ver: 14.3
CHANNEL_BONE_WIND_DIR = 10118 --Value: 10118 | Ver: 14.3
CHANNEL_BONE_WIND_STRENGTH = 10119 --Value: 10119 | Ver: 14.3
CHANNEL_BONE_WIND_TURBULENCE = 10120 --Value: 10120 | Ver: 14.3
CHANNEL_BONE_GRAVITY_DIR = 10121 --Value: 10121 | Ver: 14.3
CHANNEL_BONE_GRAVITY_STRENGTH = 10122 --Value: 10122 | Ver: 14.3
CHANNEL_SWITCH = 10123 --Value: 10123 | Ver: 14.3
CHANNEL_LAYER_ORDER = 10124 --Value: 10124 | Ver: 14.3
CHANNEL_ENABLE_PHYSICS = 10125 --Value: 10125 | Ver: 14.3
CHANNEL_PARTICLE = 10126 --Value: 10126 | Ver: 14.3
CHANNEL_POSER = 10127 --Value: 10127 | Ver: 14.3
CHANNEL_POSER_SEL = 10128 --Value: 10128 | Ver: 14.3
CHANNEL_AUDIO_LEVEL = 10129 --Value: 10129 | Ver: 14.3
CHANNEL_AUDIO_JUMP = 10130 --Value: 10130 | Ver: 14.3
CHANNEL_TRACKING_POINT = 10131 --Value: 10131 | Ver: 14.3
CHANNEL_TRACKING_POINT_SEL = 10132 --Value: 10132 | Ver: 14.3
CHANNEL_DOC_MARKERS = 10133 --Value: 10133 | Ver: 14.3
CHANNEL_CAMERA_TRACK = 10134 --Value: 10134 | Ver: 14.3
CHANNEL_CAMERA_ZOOM = 10135 --Value: 10135 | Ver: 14.3
CHANNEL_CAMERA_ROLL = 10136 --Value: 10136 | Ver: 14.3
CHANNEL_CAMERA_PANTILT = 10137 --Value: 10137 | Ver: 14.3

---@class InsertTextParams
---@field Font number --int32
---@field FontName String --LM_String
---@field Text String --LM_String
---@field Justification number --int32
---@field Fill boolean --bool
---@field Stroke boolean --bool
---@field OneFill boolean --bool
---@field CenterH boolean --bool
---@field LineCol rgb_color --rgb_color
---@field FillCol rgb_color --rgb_color
---@field LineWidth number --real
---@field Leading number --int32
---@field Kerning number --int32
---@field MakeLayer boolean --bool
---@field BalloonStyle String --LM_String
---@field BalloonFill boolean --bool
---@field BalloonStroke boolean --bool
---@field BalloonSize number --int32
---@field BalloonLineWidth number --real
---@field BalloonLineCol rgb_color --rgb_color
---@field BalloonFillCol rgb_color --rgb_color
---@field BalloonSizeProportional boolean --bool
---@field BalloonPoses String --LM_String
---@field TextSize number --int32
---@field BalloonFlipV boolean --bool
---@field BalloonFlipH boolean --bool
InsertTextParams={}
---@class LM_Cursor
LM_Cursor={}
---@class LM
---@field FREE_AXIS number --Value: 6 | Ver: < 9.5 | Desc: Arbitrary free axis
---@field NEGX_AXIS number --Value: 3 | Ver: < 9.5 | Desc: Negative X axis
---@field NEGY_AXIS number --Value: 4 | Ver: < 9.5 | Desc: Negative Y axis
---@field NEGZ_AXIS number --Value: 5 | Ver: < 9.5 | Desc: Negative Z axis
---@field X_AXIS number    --Value: 0 | Ver: < 9.5 | Desc: Positive X axis
---@field Y_AXIS number    --Value: 1 | Ver: < 9.5 | Desc: Positive Y axis
---@field Z_AXIS number    --Value: 2 | Ver: < 9.5 | Desc: Positive Z axis
---LM is the lowest-level module, and includes very basic objects like vectors and colors.
LM = {} 
    ---Causes the computer to beep. | Ver: < 9.5
    ---@return nil 
    function LM:Beep()
    end 
    ---Clamps a value to ensure that it is within certain bounds. Returns min if value is less than min, max if value is greater than max, and value otherwise.
    --- | Ver: < 9.5
    ---@param f number A number to clamp | Ver: < 9.5
    ---@param min number Minimum acceptable value | Ver: < 9.5
    ---@param MaxDimension number Maximum acceptable value | Ver: < 9.5
    ---@return number --the clamped value 
    function LM:Clamp(f, min, MaxDimension) 
    end
    ---Performs linear interpolation between min and max. If f is 0, returns min; if 1, returns max; otherwise interpolates.
    --- | Ver: < 9.5
    ---@param f number A number between 0 and 1 | Ver: < 9.5
    ---@param min number The starting value to interpolate | Ver: < 9.5
    ---@param max number The end value to interpolate | Ver: < 9.5
    ---@return number --a linear interpolation between start and end 
    function LM:Lerp(f, min, max)
    end
    ---Returns the closest integer to the supplied value. Rounds off the value.
    --- | Ver: < 9.5
    ---@param f number A number to round | Ver: < 9.5
    ---@return number --closest integer to the value passed in 
    function LM:Round(f)
    end
    ---Performs smooth interpolation between min and max. If f is 0, returns min; if 1, returns max; otherwise interpolates smoothly.
    --- | Ver: < 9.5
    ---@param f number A number between 0 and 1 | Ver: < 9.5
    ---@param min number The starting value to interpolate | Ver: < 9.5
    ---@param max number The end value to interpolate | Ver: < 9.5
    ---@return number --a smooth interpolation between start and end 
    function LM:Slerp(f, min, max)
    end
    ---Causes Moho to pause for the specified number of milliseconds.
    --- | Ver: < 9.5
    ---@param msec number Duration to pause | Ver: < 9.5
    ---@return nil 
    function LM:Snooze(msec)
    end
    ---@class BBox 
    ---@field fMax Vector3 --Ver: < 9.5
    ---@field fMin Vector3 --Ver: < 9.5
    BBox = {}
        LM.BBox={}
        ---instances of BBox class can be created by this method locally
        ---@return BBox 
        function LM.BBox:new_local()
        end
        ---instances of BBox class can be created by this method globally
        ---@return BBox  
        function LM.BBox:new()
        end
        ---Ver: < 9.5
        ---@param box BBox Ver: < 9.5
        ---@return nil
        function BBox:AccumulateBBox(box)
        end
        ---Ver: < 9.5
        ---@param v Vector3|Vector2 Ver: < 9.5
        ---@return nil
        function BBox:AccumulatePoint(v)
        end
        ---Ver: 10.0
        ---@return Vector3
        function BBox:Center()
        end
        ---Ver: 10.0
        ---@return Vector2
        function BBox:Center2D()
        end
        ---Ver: < 9.5
        ---@return nil
        function BBox:Clear()
        end
        ---Check if the box contains 3D vector, 2D vector or BBox , depending on the input type
        --- | Ver: 10.0|14.0
        ---@param val Vector3|Vector2|BBox Vector2 and Vector3 Ver: 10.0 and BBox Ver: 14.0 
        ---@return boolean
        function BBox:Contains(val)
        end
        ---Check if the box intersects another BBox. Added in version 14.0
        --- | Ver: 14.0
        ---@param box BBox Ver: 14.0
        ---@return boolean
        function BBox:Intersects(box)
        end
        ---Ver: < 9.5
        ---@return boolean
        function BBox:IsEmpty()
        end
        ---Ver: < 9.5
        ---@return number
        function BBox:MaxDimension()
        end
        ---Ver: < 9.5
        ---@return number
        function BBox:MaxDimension2D()
        end
        ---Ver: < 9.5
        ---@return number
        function BBox:MinDimension()
        end
        ---Ver: < 9.5
        ---@return number
        function BBox:MinDimension2D()
        end
        ---Ver: < 9.5
        ---@return nil
        function BBox:Normalize()
        end
    ---@class ColorOps
    ---@field Black rgb_color --Ver: 11.0
    ---@field Blue rgb_color --Ver: 11.0
    ---@field Clear rgb_color --Ver: 11.0
    ---@field Cyan rgb_color --Ver: 11.0
    ---@field Green rgb_color --Ver: 11.0
    ---@field Magenta rgb_color --Ver: 11.0
    ---@field Red rgb_color --Ver: 11.0
    ---@field White rgb_color --Ver: 11.0
    ---@field Yellow rgb_color --Ver: 11.0
    ---NOTE: All methods and properties of this class are static (which means they can be used without creating an instance of the class).
    LM.ColorOps = {}
        ---This is a static method (which means that it can be used without creating an instance of the class).
        ---For example:
        ---local rgbCol = LM.ColorOps:BlendColors(0.75, color1, color2)
        --- | Ver: 11.0
        ---@param percent number a value in the range 0 to 1 to define the proportion of color1 that will appear in the output rgbcol | Ver: 11.0
        ---@param color1 rgb_color Ver: 11.0
        ---@param color2 rgb_color Ver: 11.0
        ---@return rgb_color
        function LM.ColorOps:BlendColors(percent, color1, color2)
        end
        ---This is a static method (which means that it can be used without creating an instance of the class).
        ---For example:
        ---local rgbCol = LM.ColorOps:Hsv2Rgb(hsvCol)
        --- | Ver: 11.0
        ---@param col hsv_color Ver: 11.0
        ---@return rgb_color
        function LM.ColorOps:Hsv2Rgb(col)
        end
        ---This is a static method (which means that it can be used without creating an instance of the class).
        ---For example:
        ---local hsvCol = LM.ColorOps:HsvColor(10, 20, 30, 40)
        --- | Ver: 11.0
        ---@param h number Ver: 11.0
        ---@param s number Ver: 11.0
        ---@param v number Ver: 11.0
        ---@param a number|nil Default: 255 | Ver: 11.0
        ---@return hsv_color
        function LM.ColorOps:HsvColor(h, s, v, a)
        end
        ---This is a static method (which means that it can be used without creating an instance of the class).
        ---For example:
        ---local rgbCol = LM.ColorOps:InvertColor(10, 20, 30, 40)
        --- | Ver: 11.0
        ---@param r number Ver: 11.0
        ---@param g number Ver: 11.0
        ---@param b number Ver: 11.0
        ---@param a number|nil Default: 255 | Ver: 11.0
        ---@return rgb_color
        function LM.ColorOps:InvertColor(r, g, b, a)
        end
        ---This is a static method (which means that it can be used without creating an instance of the class).
        ---For example:
        ---local hsvCol = LM.ColorOps:Rgb2Hsv(rgbCol)
        --- | Ver: 11.0
        ---@param col rgb_color Ver: 11.0
        ---@return hsv_color
        function LM.ColorOps:Rgb2Hsv(col)
        end
        ---This is a static method (which means that it can be used without creating an instance of the class).
        ---For example:
        ---local rgbCol = LM.ColorOps:RgbColor(10, 20, 30, 40)
        --- | Ver: 11.0
        ---@param r number Ver: 11.0
        ---@param g number Ver: 11.0
        ---@param b number Ver: 11.0
        ---@param a number|nil Default: 255 | Ver: 11.0
        ---@return rgb_color
        function LM.ColorOps:RgbColor(r, g, b, a)
        end
        ---This is a static method (which means that it can be used without creating an instance of the class).
        ---For example:
        ---local b = LM.ColorOps:SameColor(lColor, rColor)
        --- | Ver: 11.0
        ---@param lColor rgb_color Ver: 11.0
        ---@param rColor rgb_color Ver: 11.0
        ---@return boolean
        function LM.ColorOps:SameColor(lColor, rColor)
        end

    ---@class ColorVector
    ---@field a number --Ver: < 9.5 | The alpha (or opacity) component of the color. Values can range from 0 to 1. An alpha of 0 is a totally transparent color, while a value of 1 indicates a totally opaque color.
    ---@field b number --Ver: < 9.5 | The blue component of the color. Values can range from 0 to 1.
    ---@field g number --Ver: < 9.5 | The green component of the color. Values can range from 0 to 1.
    ---@field r number --Ver: < 9.5 | The red component of the color. Values can range from 0 to 1.
    ---The ColorVector class is used to represent a color. The rgb_color class is also used to represent colors, but ColorVector's are useful when you need to perform mathematical operations on colors.
    ColorVector = {}
            LM.ColorVector = {}
            ---Instances of ColorVector can be created by this method locally
            ---@return ColorVector
            function LM.ColorVector:new_local()
            end
            ---Instances of ColorVector can be created by this method globally
            ---@return ColorVector
            function LM.ColorVector:new()
            end
            ---Converts a ColorVector into an rgb_color object, returning the new object.
            --- | Ver: < 9.5
            ---@return rgb_color --a new rgb_color object with the equivalent color values as the original ColorVector
            function ColorVector:AsColorStruct()
            end
            ---Inverts a color.
            --- | Ver: < 9.5
            ---@return nil
            function ColorVector:Invert()
            end
            ---Returns true if a color is pure black, false otherwise.
            --- | Ver: < 9.5
            ---@return boolean --true if the color is pure black, false otherwise
            function ColorVector:IsBlack()
            end
            ---Returns true if a color is totally opaque, false otherwise.
            --- | Ver: < 9.5
            ---@return boolean --true if the color is totally opaque, false otherwise
            function ColorVector:IsOpaque()
            end
            ---Returns true if a color is totally transparent, false otherwise.
            --- | Ver: < 9.5
            ---@return boolean --true if the color is totally transparent, false otherwise
            function ColorVector:IsTransparent()
            end
            ---Returns true if a color is pure white, false otherwise.
            --- | Ver: < 9.5
            ---@return boolean --true if the color is pure white, false otherwise
            function ColorVector:IsWhite()
            end
            ---Computes the magnitude of a color vector.
            --- | Ver: < 9.5
            ---@return number --the color's magnitude (length)
            function ColorVector:Mag()
            end
            ---Normalizes a ColorVector. This process ensures that each of the r, g, b and a values is within the range 0 to 1.
            --- | Ver: < 9.5
            ---@return nil
            function ColorVector:Normalize()
            end
            ---This method accepts multiple input formats: 
            ---ColorVector:Set(0-1, 0-1, 0-1) | ColorVector:Set(0-1, 0-1, 0-1, 0-1) | ColorVector:Set(rgb_color) | ColorVector:Set(ColorVector) 
            ---Note that the values are real numbers 0-1, not integers 0-255.
            ---For example:
            --- local vc1 = LM.ColorVector:new_local()
            --- vc1:Set(0, .995, 0, 1) -- almost pure green
            --- | Ver: < 9.5
            ---@param arg1 number | rgb_color | ColorVector The r value you wish to set | rgb_color object whose value you wish to copy | another LM_ColorVector object whose value you wish to copy | Ver: < 9.5
            ---@param arg2 number | nil  The g value you wish to set. Leave empty if first argument is rgb_color or ColorVector | Ver: < 9.5
            ---@param arg3 number | nil The b value you wish to set. Leave empty if first argument is rgb_color or ColorVector | Ver: < 9.5
            ---@param arg4 number | nil The a value you wish to set | Default: 1.0 |Ver: < 9.5
            ---@return nil
            function ColorVector:Set(arg1, arg2, arg3, arg4)
            end
            ---Computes the squared magnitude of a color vector.
            --- | Ver: < 9.5
            ---@return number --the color's squared magnitude
            function ColorVector:SquaredMag()
            end
    ---@class hsv_color
    ---@field a number --Ver: 11.0 | The Alpha (Transparency) component
    ---@field h number --Ver: 11.0 | The Hue component
    ---@field s number --Ver: 11.0 | The Saturation component
    ---@field v number --Ver: 11.0 | The Value (or Brightness) component
    LM.hsv_color = {}
        ---Instances of hsv_color can be created by this method locally
        ---@return hsv_color
        function LM.hsv_color:new_local()
        end
        ---Instances of hsv_color can be created by this method globally
        ---@return hsv_color
        function LM.hsv_color:new()
        end
    ---@class Matrix
    ---The Matrix class is used to represent a 4x4 transformation matrix. Matrix objects are used to transform points in 3D space.
    Matrix = {}
        LM.Matrix = {}
        ---Instances of Matrix can be created by this method locally
        ---@return Matrix
        function LM.Matrix:new_local()
        end
        ---Instances of Matrix can be created by this method globally
        ---@return Matrix
        function LM.Matrix:new()
        end
        ---Set the matrix to be an identity matrix. An identity matrix has no transformation - any vectors transformed by it will retain their original positions. Use this function to reset a matrix before creating a different transformation.
        --- | Ver: < 9.5
        ---@return nil
        function Matrix:Identity()
        end
        ---Invert the matrix.
        --- | Ver: < 9.5
        ---@return nil
        function Matrix:Invert()
        end
        ---Multiplies two matrices. The calling object is multiplied by the matrix "m2".
        ---X:Multiply(Y) is multiply Y into X (X = X * Y)
        ---Notice that X:Multiply(Y) leaves Y unchanged and changes X
        --- | Ver: < 9.5
        ---@param m2 Matrix A Matrix object to multiply into the calling object | Ver: < 9.5
        ---@return nil
        function Matrix:Multiply(m2)
        end
        ---Prepend a perspective transform onto the current matrix.
        --- | Ver: < 9.5
        ---@param alpha number The field of view angle (in radians) | Ver: < 9.5
        ---@param nearr number Distance from the virtual eye to the near clipping plane | Ver: < 9.5
        ---@param farr number Distance from the virtual eye to the far clipping plane | Ver: < 9.5
        ---@param aspect number Aspect ratio of the final viewing plane | Ver: < 9.5
        ---@return nil
        function Matrix:Perspective(alpha, nearr, farr, aspect)
        end
        ---Prepend a rotation onto the current matrix.
        --- | Ver: < 9.5
        ---@param axis number See the axis constants | Ver: < 9.5
        ---@param angle number The angle (in radians) to rotate the matrix | Ver: < 9.5
        ---@return nil
        function Matrix:Rotate(axis, angle)
        end
        ---Prepend a scale onto the current matrix.
        --- | Ver: < 9.5
        ---@param sx number The x value of the scale | Ver: < 9.5
        ---@param sy number The y value of the scale | Ver: < 9.5
        ---@param sz number The z value of the scale | Ver: < 9.5
        ---@return nil
        function Matrix:Scale(sx, sy, sz)
        end
        ---Ver: < 9.5
        ---@return number
        function Matrix:ScaleValue()
        end
        ---Set the value of a matrix by copying an existing matrix.
        --- | Ver: < 9.5
        ---@param m2 Matrix Another Matrix object whose value you wish to copy | Ver: < 9.5
        ---@return nil
        function Matrix:Set(m2)
        end
        ---This method accepts multiple input formats:
        ---Matrix:Transform(Vector2) | Matrix:Transform(BBox) | Matrix:Transform(Vector3)
        ---Apply a matrix transformation to a Vector2, BBox or Vector 3
        ---The primitive T:Transform(V) is effectively: V = V * T
        ---Notice that T:Transform(V) leaves T unchanged and changes V
        --- | Ver: < 9.5
        ---@param arg1 Vector2 | BBox | Vector3 A Vector2, BBox, or Vector3 object to transform | Ver: < 9.5
        ---@return nil
        function Matrix:Transform(arg1)
        end
        ---Prepend a translation onto the current matrix.
        --- | Ver: < 9.5
        ---@param dx number The x value of the translation | Ver: < 9.5
        ---@param dy number The y value of the translation | Ver: < 9.5
        ---@param dz number The z value of the translation | Ver: < 9.5
        ---@return nil
        function Matrix:Translate(dx, dy, dz)
        end
    ---@class Point
    ---@field x number --The horizontal location of the pixel. | Ver: < 9.5
    ---@field y number --The vertical location of the pixel. | Ver: < 9.5
    ---The Point class is used to indicate a pixel location on the screen.
    Point = {}
        LM.Point = {}
        ---Instances of Point can be created by this method locally
        ---@return Point
        function LM.Point:new_local()
        end
        ---Instances of Point can be created by this method globally
        ---@return Point
        function LM.Point:new()
        end
        ---This method accepts multiple input formats:
        ---Point:Set(xVal, yVal) | Point:Set(p2)
        ---Set the value of a point. This function is an alternative to setting the x and y values individually.
        --- | Ver: < 9.5
        ---@param arg1 number | Point The x value you wish to set | The Point object whose value you wish to copy | Ver: < 9.5
        ---@param arg2 number | nil The y value you wish to set. Leave empty if first argument is a Point | Ver: < 9.5
        ---@return nil
        function Point:Set(arg1, arg2)
        end
    ---@class Rect
    ---@field bottom number --The bottom-most pixel in the rectangle. | Ver: < 9.5
    ---@field left number   --The left-most pixel in the rectangle. | Ver: < 9.5
    ---@field right number  --The right-most pixel in the rectangle. | Ver: < 9.5
    ---@field top number    --The top-most pixel in the rectangle. | Ver: < 9.5
    ---The Rect class is used to represent a rectangular shape in screen coordinates.
    Rect = {}
        LM.Rect = {}
        ---Instances of Rect can be created by this method locally
        ---@return Rect
        function LM.Rect:new_local()
        end
        ---Instances of Rect can be created by this method globally
        ---@return Rect
        function LM.Rect:new()
        end
        ---Tests whether the rectangle contains the given point.
        --- | Ver: < 9.5
        ---@param pt Point A point that is being tested for inclusion in the rectangle | Ver: < 9.5
        ---@return boolean --true if the point is contained in the rectangle, otherwise false
        function Rect:Contains(pt)
        end
        ---This function "normalizes" a rectangle to ensure that the left value is smaller than the right, and the top value is smaller than the bottom.
        ---This should always be true of a well-behaved rectangle, and this function makes sure that is the case.
        --- | Ver: < 9.5
        ---@return nil
        function Rect:Normalize()
        end
        ---Ver: 9.5
        ---@param l number Ver: 9.5
        ---@param t number Ver: 9.5
        ---@param r number Ver: 9.5
        ---@param b number Ver: 9.5
        ---@return nil
        function Rect:Set(l, t, r, b)
        end
    ---@class rgb_color
    ---@field a number --The alpha (or opacity) component of the color. Values range from 0 (transparent) to 255 (opaque). | Ver: < 9.5
    ---@field b number --The blue component of the color. Values can range from 0 to 255. | Ver: < 9.5
    ---@field g number --The green component of the color. Values can range from 0 to 255. | Ver: < 9.5
    ---@field r number --The red component of the color. Values can range from 0 to 255. | Ver: < 9.5
    ---The rgb_color class is a basic structure for storing a color value, including opacity/transparency.
    rgb_color = {}    
        LM.rgb_color = {}
        ---Instances of rgb_color can be created by this method locally
        ---@return rgb_color
        function LM.rgb_color:new_local()
        end
        ---Instances of rgb_color can be created by this method globally
        ---@return rgb_color
        function LM.rgb_color:new()
        end
    ---@class String
    String = {}  
        LM.String = {}
        ---Instances of String can be created by this method locally
        ---@return String
        function LM.String:new_local()
        end
        ---Instances of String can be created by this method globally
        ---@return String
        function LM.String:new()
        end
        ---Ver: < 9.5
        ---@return string
        function String:Buffer()
        end
        ---Ver: < 9.5
        ---@param str string Ver: < 9.5
        ---@return nil
        function String:Set(str)
        end
    ---@class Vector2 
    ---@field x number --The x (or horizontal) coordinate of the vector. | Ver: < 9.5
    ---@field y number --The y (or vertical) coordinate of the vector. | Ver: < 9.5
    ---The Vector2 class is used to represent a 2D vector or point.
    Vector2 = {}
        LM.Vector2 = {}
        ---instances of Vector2 class can be created by this method locally
        ---@return Vector2
        function LM.Vector2:new_local()
        end
        ---instances of Vector2 class can be created by this method globally
        ---@return Vector2
        function LM.Vector2:new()
        end
        ---Ver: 11.1
        ---@param vec2 Vector2 Ver: 11.1
        ---@return Vector3
        function Vector2:Cross(vec2)
        end
        ---Computes the dot product of this vector and a second one.
        --- | Ver: < 9.5
        ---@param vec2 Vector2 A Vector2 object | Ver: < 9.5
        ---@return number --the dot product of the calling vector and the passed in argument
        function Vector2:Dot(vec2)
        end
        ---Returns a vector that points 90 degrees away from the orignal vector.
        --- | Ver: < 9.5
        ---@return Vector2 --a new vector, pointing 90 degrees away from the original
        function Vector2:GetOrthogonal()
        end
        ---Computes the magnitude of a vector.
        --- | Ver: < 9.5
        ---@return number --the vector's magnitude (length)
        function Vector2:Mag()
        end
        ---Returns a normalized copy of the vector.
        ---The vector object is unchanged, and normalized copy is returned.
        ---The normalized vector will have a length of 1.0.
        --- | Ver: < 9.5
        ---@return Vector2 --a normalized copy of the original vector
        function Vector2:Norm()
        end
        ---Normalizes a vector. The vector is normalized (adjusted so that it points in the same direction, but has a length of one) in place, adjusting the x and y values as necessary.
        --- | Ver: < 9.5
        ---@return nil
        function Vector2:NormMe()
        end
        ---Rotates the vector by the specified angle.
        --- | Ver: < 9.5
        ---@param angle number The angle (in radians) to rotate the vector | Ver: < 9.5
        ---@return nil
        function Vector2:Rotate(angle)
        end
        ---This method accepts multiple input formats:
        ---Vector2:Set(vx, vy) | Vector2:Set(vec2)
        ---Set the value of a vector. This function is an alternative to setting the x and y values individually.
        --- | Ver: < 9.5
        ---@param arg1 number | Vector2 The x value you wish to set | A vec2 value you wish to set | Ver: < 9.5
        ---@param arg2 number | nil The y value you wish to set. Leave empty if first argument is a Vector2 | Ver: < 9.5
        ---@return nil
        function Vector2:Set(arg1, arg2)
        end
        ---Computes the squared magnitude of a vector.
        --- | Ver: < 9.5
        ---@return number --the vector's squared magnitude
        function Vector2:SquaredMag()
        end
    ---@class Vector3
    ---@field x number --The x (or horizontal) coordinate of the vector. | Ver: < 9.5
    ---@field y number --The y (or vertical) coordinate of the vector. | Ver: < 9.5
    ---@field z number --The z (or depth) coordinate of the vector. | Ver: < 9.5
    ---The Vector3 class is used to represent a 3D vector or point.
    Vector3 = {}
        LM.Vector3 = {}
        ---instances of Vector3 class can be created by this method locally
        ---@return Vector3
        function LM.Vector3:new_local()
        end
        ---instances of Vector3 class can be created by this method globally
        ---@return Vector3
        function LM.Vector3:new()
        end
        ---Computes the cross product of this vector and a second one
        --- | Ver: < 9.5
        ---@param vec2 Vector3 Ver: < 9.5
        ---@return Vector3 --a new vector that is the cross product of the calling vector and the passed in argument
        function Vector3:Cross(vec2) 
        end
        ---Computes the dot product of this vector and a second one.
        --- | Ver: < 9.5
        ---@param vec2 Vector3 A Vector3 object | Ver: < 9.5
        ---@return number --the dot product of the calling vector and the passed in argument
        function Vector3:Dot(vec2) 
        end
        ---Returns two vectors that are orthogonal to the original vector.
        --- | Ver: < 9.5
        ---@param vecU Vector3 This vector will be filled in so that it is orthogonal to the original vector | Ver: < 9.5
        ---@param vecV Vector3 This vector will be filled in so that it is orthogonal to both the original vector and vecU | Ver: < 9.5
        ---@return nil
        function Vector3:GetOrthogonals(vecU, vecV)
        end
        ---Computes the magnitude of a vector.
        --- | Ver: < 9.5
        ---@return number --the vector's magnitude (length)
        function Vector3:Mag()
        end
        ---Returns a normalized copy of the vector.
        ---The vector object is unchanged, and normalized copy is returned.
        ---The normalized vector will have a length of 1.0.
        --- | Ver: < 9.5
        ---@return Vector3 --a normalized copy of the original vector
        function Vector3:Norm() 
        end
        ---Normalizes a vector. The vector is normalized (adjusted so that it points in the same direction, but has a length of one) in place, adjusting the x, y and z values as necessary.
        --- | Ver: < 9.5
        ---@return nil
        function Vector3:NormMe() 
        end
        ---Rotates the vector around the specified axis by the specified angle.
        --- | Ver: < 9.5
        ---@param axis number See the axis constants | Ver: < 9.5
        ---@param angle number The angle (in radians) to rotate the vector | Ver: < 9.5
        ---@return nil
        function Vector3:Rotate(axis, angle)
        end
        ---This method accepts multiple input formats:
        ---Vector3:Set(vx, vy, vz) | Vector3:Set(vec2)
        ---Set the value of a vector. This function is an alternative to setting the x, y and z values individually.
        --- | Ver: < 9.5
        ---@param arg1 number | Vector3 The x value you wish to set | A vec2 value you wish to set | Ver: < 9.5
        ---@param arg2 number | nil The y value you wish to set. Leave empty if first argument is a Vector3 | Ver: < 9.5
        ---@param arg3 number | nil The z value you wish to set. Leave empty if first argument is a Vector3 | Ver: < 9.5
        ---@return nil
        function Vector3:Set(arg1, arg2, arg3)
        end
        ---Computes the squared magnitude of a vector.
        --- | Ver: < 9.5
        ---@return number --the vector's squared magnitude
        function Vector3:SquaredMag()
        end
    ---@class GUI
    ---@field ALERT_INFO number     --This type of alert is used to give the user non-critical information. | Value: 0 | Ver: < 9.5
    ---@field ALERT_QUESTION number --This type of alert is used to ask the user a question. | Value: 2 | Ver: < 9.5
    ---@field ALERT_WARNING number  --This type of alert is used to warn the user (perhaps that they are about to perform some action that cannot be undone). | Value: 1 | Ver: < 9.5
    ---@field ALIGN_BOTTOM number --Align control on the bottom side of its container | Value: 2 | Ver: < 9.5
    ---@field ALIGN_CENTER number --Align control in the center of its container | Value: 1 | Ver: < 9.5
    ---@field ALIGN_FILL number   --Resize control to fill its container | Value: 3 | Ver: < 9.5
    ---@field ALIGN_LEFT number   --Align control on the left side of its container | Value: 0 | Ver: < 9.5
    ---@field ALIGN_RIGHT number  --Align control on the right side of its container | Value: 2 | Ver: < 9.5
    ---@field ALIGN_TOP number    --Align control on the top side of its container | Value: 0 | Ver: < 9.5
    ---@field MSG_BASE number   --The starting message code for use by Moho scripts | Value: -40000 | Ver: < 9.5
    ---@field MSG_CANCEL number --The user clicked Cancel or pressed escape | Value: -1008 | Ver: < 9.5
    ---@field MSG_OK number     --The user clicked OK or pressed enter | Value: -1007 | Ver: < 9.5
    ---@field FOLLOW_BOTTOM number --Value: 8 | Ver: < 9.5
    ---@field FOLLOW_LEFT number   --Value: 1 | Ver: < 9.5
    ---@field FOLLOW_RIGHT number  --Value: 4 | Ver: < 9.5
    ---@field LM.GUI.FOLLOW_TOP number    --Value: 2 | Ver: < 9.5
    ---@field KEY_BACKSPACE number --The Backspace key | Value: -4 | Ver: < 9.5
    ---@field KEY_BIND number      --Added in version 11 | Value: -50 | Ver: 11.0
    ---@field KEY_DELETE number    --The Delete key | Value: -5 | Ver: < 9.5
    ---@field KEY_DESELECT number  --Added in version 11 | Value: -51 | Ver: 11.0
    ---@field KEY_DOWN number      --The down arrow key | Value: -13 | Ver: < 9.5
    ---@field KEY_END number       --The End key | Value: -7 | Ver: < 9.5
    ---@field KEY_ESCAPE number    --The Escape key | Value: -2 | Ver: < 9.5
    ---@field KEY_F1 number        --The F1 key | Value: -20 | Ver: < 9.5
    ---@field KEY_F10 number       --The F10 key | Value: -29 | Ver: < 9.5
    ---@field KEY_F11 number       --The F11 key | Value: -30 | Ver: < 9.5
    ---@field KEY_F12 number       --The F12 key | Value: -31 | Ver: < 9.5
    ---@field KEY_F2 number        --The F2 key | Value: -21 | Ver: < 9.5
    ---@field KEY_F3 number        --The F3 key | Value: -22 | Ver: < 9.5
    ---@field KEY_F4 number        --The F4 key | Value: -23 | Ver: < 9.5
    ---@field KEY_F5 number        --The F5 key | Value: -24 | Ver: < 9.5
    ---@field KEY_F6 number        --The F6 key | Value: -25 | Ver: < 9.5
    ---@field KEY_F7 number        --The F7 key | Value: -26 | Ver: < 9.5
    ---@field KEY_F8 number        --The F8 key | Value: -27 | Ver: < 9.5
    ---@field KEY_F9 number        --The F9 key | Value: -28 | Ver: < 9.5
    ---@field KEY_HOME number      --The Home key | Value: -6 | Ver: < 9.5
    ---@field KEY_LEFT number      --The left arrow key | Value: -10 | Ver: < 9.5
    ---@field KEY_PAGEDOWN number  --The Page Down key | Value: -9 | Ver: < 9.5
    ---@field KEY_PAGEUP number    --The Page Up key | Value: -8 | Ver: < 9.5
    ---@field KEY_RETURN number    --The Return or Enter key | Value: -1 | Ver: < 9.5
    ---@field KEY_RIGHT number     --The right arrow key | Value: -12 | Ver: < 9.5
    ---@field KEY_TAB number       --The Tab key | Value: -3 | Ver: < 9.5
    ---@field KEY_UP number        --The up arrow key | Value: -11 | Ver: < 9.5
    ---@field DEFAULT_INDENT number  --The default amount of indentation | Value: 16 | Ver: < 9.5
    ---@field DEFAULT_PADDING number --The default amount of padding | Value: 8 | Ver: < 9.5
    ---@field FIELD_FLOAT number   --A text control that only accepts floating-point input | Value: 2 | Ver: < 9.5
    ---@field FIELD_INT number     --A text control that only accepts integer input | Value: 1 | Ver: < 9.5
    ---@field FIELD_NODIGITS number --A text control that does not accept any numerical input | Value: 5 | Ver: < 9.5
    ---@field FIELD_TEXT number    --A regular text control | Value: 0 | Ver: < 9.5
    ---@field FIELD_UFLOAT number  --A text control that only accepts non-negative floating-point input | Value: 4 | Ver: < 9.5
    ---@field FIELD_UINT number    --A text control that only accepts non-negative integer input | Value: 3 | Ver: < 9.5
    ---@field UNIT_DEGREES number --Added in version 14.0 | Value: 2 | Ver: 14.0
    ---@field UNIT_FPS number     --Added in version 14.0 | Value: 3 | Ver: 14.0
    ---@field UNIT_FRAMES number  --Added in version 14.0 | Value: 5 | Ver: 14.0
    ---@field UNIT_MULT number    --Added in version 14.0 | Value: 4 | Ver: 14.0
    ---@field UNIT_NONE number    --Added in version 14.0 | Value: -1 | Ver: 14.0
    ---@field UNIT_PERCENT number --Added in version 14.0 | Value: 0 | Ver: 14.0
    ---@field UNIT_PIXELS number  --Added in version 14.0 | Value: 1 | Ver: 14.0
    ---@field VIEW_MODE_PIXEL number  --Added in version 10 | Value: 1 | Ver: 10.0
    ---@field VIEW_MODE_VECTOR number --Added in version 10 | Value: 2 | Ver: 10.0
    ---LM.GUI is a user interface module that is built on top of Anime Studio's cross-platform user interface library. Scripts can use the user interface widgets in this module to build dialog boxes, and to set up custom interfaces for toolbar buttons.
    LM.GUI = {}
        LM.GUI.LM_Message = {}
        ---instances of LM_Message class can be created by this method locally
        ---@return BBox 
        function LM.GUI.LM_Message:new_local()
        end
        ---instances of LM_Message class can be created by this method globally
        ---@return BBox  
        function LM.GUI.LM_Message:new()
        end
        ---Presents the user with an alert box
        ---With this function, you can specify up to three lines of text and three buttons, or just a single line of text with a single button. If any of the text labels or buttons are nil, then that item will not appear in the alert box. If all three buttons are nil then the alert box is not output. After the user clicks a button in the alert box, the function returns 0, 1, or 2, depending if the user pressed button 1, 2, or 3.
        --- | Ver: < 9.5
        ---@param alertType number A constant indicating the type of alert box | Ver: < 9.5
        ---@param str1 string The first line of text to display | Ver: < 9.5
        ---@param str2 string|nil The second line of text to display | Default: nil | Ver: < 9.5
        ---@param str3 string|nil The third line of text to display | Default: nil | Ver: < 9.5
        ---@param but1 string|nil The first button's label | Default: nil | Ver: < 9.5
        ---@param but2 string|nil The second button's label | Default: nil | Ver: < 9.5
        ---@param but3 string|nil The third button's label | Default: nil | Ver: < 9.5
        ---@return number --Number of the button that the user pressed (0 for but1, 1 for but2, 2 for but3)
        function LM.GUI.Alert(alertType, str1, str2, str3, but1, but2, but3)
        end
        ---Creates a new LM_AngleWidget object
        --- | Ver: < 9.5 | Ver: 13.5.2
        ---@param msg number|nil Default: 0 | Ver: < 9.5
        ---@param horizontal boolean|nil Default: false | Ver: 13.5.2
        ---@return LM_AngleWidget --The newly-created angle widget
        function LM.GUI.AngleWidget(msg, horizontal)
        end
        ---Creates a new LM_Button object that can then be added to a dialog or toolbar.
        --- | Ver: < 9.5
        ---@param label string The text that should appear on the button | Ver: < 9.5
        ---@param msg number|nil Default: 0 | Ver: < 9.5
        ---@return LM_Button --the newly-created button
        function LM.GUI.Button(label, msg)
        end
        ---Creates a new LM_Button object
        --- | Ver: < 9.5
        ---@param label string A text label to appear at the side of the check box | Ver: < 9.5
        ---@param msg number|nil Default: 0 | Ver: < 9.5
        ---@return LM_CheckBox
        function LM.GUI.CheckBox(label, msg)
        end
        ---Creates a new LM_ColorSwatch object
        --- | Ver: < 9.5
        ---@param allowAlpha boolean True if the user should be allowed to specify alpha (opacity), false otherwise | Ver: < 9.5
        ---@param msg number|nil Default: 0 | Ver: < 9.5
        ---@return LM_ColorSwatch --The newly created color swatch object
        function LM.GUI.ColorSwatch(allowAlpha, msg)
        end
        ---The Cursor HotSpot is the pixel offset from Cursor Position that the cursor visually indicates on the screen. (hotX=0, hotY=0) is the extreme top left of the image. Moho's standard cursors are 16 pixels square. The "arrow" cursors (such as scale) have a hotspot at 1,1; the crosshair cursor's hotspot is at 7,7.
        ---See also LM Utilities
        --- | Ver: < 9.5
        ---@param path string the pathname for the image file for the cursor. typically a .png image | Ver: < 9.5
        ---@param hotX number hotspot pixel offset: 0 = extreme left of the .png | Ver: < 9.5
        ---@param hotY number hotspot offset: 0 = extreme top of the .png | Ver: < 9.5
        ---@return LM_Cursor
        function LM.GUI.Cursor(path, hotX, hotY)
        end
        ---Creates a new LM_Divider object that can then be added to a dialog or toolbar.
        --- | Ver: < 9.5
        ---@param vertical boolean True to create a vertical divider, false for a horizontal divider | Ver: < 9.5
        ---@return LM_Divider --the newly-created divider
        function LM.GUI.Divider(vertical)
        end
        ---Creates a new LM_DynamicText object that can then be added to a dialog or toolbar.
        --- | Ver: < 9.5
        ---@param label string The initial text label | Ver: < 9.5
        ---@param width number|nil The width of the text label in pixels (set this to 0 for automatic sizing) | Default: 0 | Ver: < 9.5
        ---@return LM_DynamicText --the newly-created dynamic text
        function LM.GUI.DynamicText(label, width)
        end
        ---Creates a button with a .png image background
        ---The button can act like a regular button or can have "on/off" state to act like a checkbox.
        ---The image must be placed into the ScriptResources folder.
        --- | Ver: < 9.5 | Ver: 12.0
        ---@param path string A path to a .png image inside the ScriptResources folder. Don't use the .png extension though. For example, "ScriptResources/my_script_name/image1" | Ver: < 9.5
        ---@param tooltip string Tooltip that appears when on mouseover. | Ver: < 9.5
        ---@param checkbox boolean Set this to true if you want your button to act as a checkbox. | Ver: < 9.5
        ---@param msg number|nil Default: 0 | Ver: < 9.5
        ---@param recolor boolean|nil Should the image be recolored when user changes the interface color scheme in the program settings. | Default: false | Ver: 12.0
        ---@return LM_Button
        function LM.GUI.ImageButton(path, tooltip, checkbox, msg, recolor)
        end
        ---Ver: 11.1 | Ver: 12.0
        ---@param path string Ver: 11.1
        ---@param radioMode boolean|nil Default: true | Ver: 11.1
        ---@param recolor boolean|nil Default: false | Ver: 12.0
        ---@return LM_PopupMenu
        function LM.GUI.ImagePopupMenu(path, radioMode, recolor)
        end
        ---Creates a new LM_ImageTextList object
        --- | Ver: 12.2
        ---@param width number The width in pixels | Ver: 12.2
        ---@param height number The height in pixels | Ver: 12.2
        ---@param msg number|nil Default: 0 | Ver: 12.2
        ---@return LM_ImageTextList
        function LM.GUI.ImageTextList(width, height, msg)
        end
        ---Creates a new LM_Menu object. You then need to create a pop-up menu object using LM.GUI.PopupMenu and set this menu object as the menu of the pop-up menu using the SetMenu method. The pop-up menu object can then be added to a dialog or toolbar using the AddChild method. After that, you can use the AddItem method to add items to your menu.
        --- | Ver: < 9.5
        ---@param title string The menu's title | Ver: < 9.5
        ---@return LM_Menu --the newly-created menu
        function LM.GUI.Menu(title)
        end
        ---Prompts the user to select a file to open.
        ---Returns the path name of the selected file as a string. If the user cancels the file dialog, the function returns an empty string, "".
        --- | Ver: < 9.5
        ---@param caption string A string to use as a caption in the file dialog | Ver: < 9.5
        ---@return string --The path name of the selected file, or an empty string if the user cancels.
        function LM.GUI.OpenFile(caption)
        end
        ---Creates a new LM_PopupDialog object that can then be added to a dialog or toolbar.
        --- | Ver: < 9.5
        ---@param label string The text to appear on the popup dialog | Ver: < 9.5
        ---@param lockOpen boolean|nil If true, the dialog will stay open continuously. If false, the dialog will close when the user clicks on somethign else | Default: false | Ver: < 9.5
        ---@param msg number|nil Default: 0 | Ver: < 9.5
        ---@return LM_PopupDialog --the newly-created popup dialog
        function LM.GUI.PopupDialog(label, lockOpen, msg)
        end
        ---Creates a new LM_PopupMenu. The LM_PopupMenu is just a "button" that an LM_Menu object needs to be attached to. after creating popup menu Create a new LM_Menu object. and set this menu object as the menu of the pop-up menu using the SetMenu method. The pop-up menu object can then be added to a dialog or toolbar using the AddChild method. After that, you can use the AddItem method to add items to your menu.
        --- | Ver: < 9.5
        ---@param width number The width of the popup menu in pixels | Ver: < 9.5
        ---@param radioMode boolean|nil If true, then only one item can be selected from the popup menu. If false, then the commands in the menu are not selected with a check, but work like normal menu commands. | Default: true | Ver: < 9.5
        ---@return LM_PopupMenu --the newly-created popup menu
        function LM.GUI.PopupMenu(width, radioMode)
        end
        ---Creates a new LM_RadioButton object
        --- | Ver: < 9.5
        ---@param label string The text that should appear next to the radio button | Ver: < 9.5
        ---@param msg number|nil Default: 0 | Ver: < 9.5
        ---@return LM_RadioButton --the newly-created radio button
        function LM.GUI.RadioButton(label, msg)
        end
        ---Prompts the user to select a file to save.
        ---Returns the path name of the selected file as a string. If the user cancels the file dialog, the function returns an empty string, "".
        --- | Ver: < 9.5
        ---@param caption string A string to use as a caption in the file dialog | Ver: < 9.5
        ---@return string --The path name of the selected file, or an empty string if the user cancels.
        function LM.GUI.SaveFile(caption)
        end
        ---Creates a new LM_ScrollBar object
        --- | Ver: < 9.5
        ---@param length number Ver: < 9.5
        ---@param vertical boolean Ver: < 9.5
        ---@param msg number Ver: < 9.5
        ---@param resizingMode number|nil Default: LM_FOLLOW_LEFT | Ver: < 9.5
        ---@return LM_ScrollBar --the newly-created scroll bar
        function LM.GUI.ScrollBar(length, vertical, msg, resizingMode)
        end
        ---Ver: 11.0
        ---@param caption string Ver: 11.0
        ---@return string
        function LM.GUI.SelectFolder(caption)
        end
        ---Creates a new LM_ShortButton object
        --- | Ver: 13.5.2
        ---@param label string Ver: 13.5.2
        ---@param msg number|nil Default: 0 | Ver: 13.5.2
        ---@return LM_ShortButton --The newly created ShortButton
        function LM.GUI.ShortButton(label, msg)
        end
        ---Ver: 10.0
        ---@param allowAlpha boolean Ver: 10.0
        ---@param msg number|nil Default: 0 | Ver: 10.0
        ---@return LM_ColorSwatch
        function LM.GUI.ShortColorSwatch(allowAlpha, msg)
        end
        ---The LM_SimpleDialog class is used to create dialog boxes - windows that can be filled with other user interface controls. An LM_SimpleDialog object automatically provides OK and Cancel buttons.
        --- | Ver: < 9.5
        ---@param title string Ver: < 9.5
        ---@param subClass table Ver: < 9.5
        ---@return LM_SimpleDialog
        function LM.GUI.SimpleDialog(title, subClass)
        end
        ---Ver: < 9.5
        ---@param length number Ver: < 9.5
        ---@param vertical boolean Ver: < 9.5
        ---@param showTicks boolean Ver: < 9.5
        ---@param msg number|nil Default: 0 | Ver: < 9.5
        ---@param resizingMode number|nil Default: LM_FOLLOW_LEFT | Ver: < 9.5
        ---@return LM_Slider
        function LM.GUI.Slider(length, vertical, showTicks, msg, resizingMode)
        end
        ---Creates a new LM_StaticText object that can then be added to a dialog or toolbar.
        --- | Ver: < 9.5
        ---@param label string The text to appear | Ver: < 9.5
        ---@return LM_StaticText
        function LM.GUI.StaticText(label)
        end
        ---Creates a new LM_TextControl object that can then be added to a dialog or toolbar.
        --- | Ver: < 9.5 | Ver: 11.1
        ---@param width number The width of the text control in pixels (enter 0 for automatic sizing) | Ver: < 9.5
        ---@param text string The initial text to display | Ver: < 9.5
        ---@param msg number|nil A message value to be triggered when the user changes the text in the box | Default: 0 | Ver: < 9.5
        ---@param fieldType number|nil The type of text allowed - see the constants for this option | Default: LM_FIELD_TEXT | Ver: < 9.5
        ---@param label string|nil The text label to precede the control | Default: NULL | Ver: 11.1
        ---@return LM_TextControl
        function LM.GUI.TextControl(width, text, msg, fieldType, label)
        end
        ---Creates a new LM_TextList object that can then be added to a dialog or toolbar.
        --- | Ver: < 9.5
        ---@param width number Width in pixels | Ver: < 9.5
        ---@param height number Height in pixels | Ver: < 9.5
        ---@param msg number|nil The message value to send when the user clicks on an item in the list | Default: 0 | Ver: < 9.5
        ---@return LM_TextList
        function LM.GUI.TextList(width, height, msg)
        end

        ---@class LM_View 
        ---The LM_View class is the basis for most of the user interface classes available to Moho scripts. It represents a rectangular area in the user interface where some kind of interaction takes place.
        LM_View = {}
            ---Enables or disables a view object. For example, this function is used to gray out a user interface button.
            --- | Ver: < 9.5
            ---@param enable boolean True to enable the view, false to disable it | Ver: < 9.5
            ---@return nil
            function LM_View:Enable(enable)
            end
            ---Ver: 11.0
            ---@return number
            function LM_View:Height()
            end
            ---Tells whether the view is currently enabled.
            --- | Ver: < 9.5
            ---@return boolean --true if the view is enabled, false otherwise
            function LM_View:IsEnabled()
            end
            ---It returns true if dragging button matches the given argument: pass 1 to test LMB, 2 to test RMB, or 0 to test if ANY button is dragging.
            --- | Ver: 11.0
            ---@param button number | nil Default: 0 | Ver: 11.0
            ---@return boolean --true if dragging button matches the given argument: pass 1 to test LMB, 2 to test RMB, or 0 to test if ANY button is dragging
            function LM_View:IsMouseDragging(button)
            end
            ---You can create your own cursor files and place them into the ScriptUtility folder.
            ---The LM Utilities file has the following pre-defined cursors you can use:
            ---MOHO.disabledCursor
            --- | MOHO.moveCursor
            --- | MOHO.scaleCursor
            --- | MOHO.rotateCursor
            --- | MOHO.crosshairCursor
            --- | An example from the lm_transform_layer (Moho 12.2):
            --- mouseEvent.view:SetCursor(MOHO.moveCursor)
            --- NOTE: if needed, "mouseEvent.view:SetCursor()" will unset any custom cursor
            --- | Ver: < 9.5
            ---@param cursor LM_Cursor An object created by the LM.GUI.Cursor method | Ver: < 9.5
            ---@return nil
            function LM_View:SetCursor(cursor)
            end
            ---Ver: 12.0
            ---@param tooltip string Ver: 12.0
            ---@return nil
            function LM_View:SetToolTip(tooltip)
            end
            ---Ver: 11.0
            ---@return number
            function LM_View:Width()
            end
            ---@class LM_BaseWidget 
            ---This class is a specialized type of view object. You would never create an LM_BaseWidget object by itself - instead, common controls like buttons and check boxes are types of LM_BaseWidget's.
            LM_View.LM_BaseWidget = {}
                ---Call this function to force the widget to redraw itself. Ordinarily, widgets take care of this on their own, but you can force it if you need to.
                --- | Ver: < 9.5
                ---@return nil
                function LM_View.LM_BaseWidget:Redraw()
                end
                ---@class LM_AngleWidget
                ---The LM_AngleWidget class provides a knob to let the user enter an angle value. To create a new object of this class, use the LM.GUI.AngleWidget method.
                LM_View.LM_BaseWidget.LM_AngleWidget = {}
                    ---Added in version 14.0
                    --- | Ver: 14.0
                    ---@param b boolean Ver: 14.0
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_AngleWidget:AllowWrapAround(b)
                    end
                    ---Sets the value of the angle widget (in radians).
                    ---For example:
                    --- d.MyAngleWidget:SetValue(math.pi)
                    --- | Ver: < 9.5
                    ---@param angle number An angle in radians | Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_AngleWidget:SetValue(angle)
                    end
                    ---Returns the current angle value (in radians). 
                    ---For the user's convenience, the Angle Widget's value is displayed in degrees.
                    ---However, for the internal functions, radians are much more useful, so this method returns the value in radians.
                    ---If you need to get the same value in degrees, you can use following:
                    --- math.floor(math.deg(val) * 100 + 0.5) / 100
                    --- | Ver: < 9.5
                    ---@return number
                    function LM_View.LM_BaseWidget.LM_AngleWidget:Value()
                    end
                ---@class LM_Button  
                ---The LM_Button class represents a basic push button, like the OK and Cancel buttons in a dialog box. To create a new button, use the LM.GUI.Button or LM.GUI.ImageButton methods.
                LM_View.LM_BaseWidget.LM_Button = {}
                    ---This message will get sent if the user holds down the alt key while clicking the button. (Added in version 10)
                    ---| Ver: 10.0
                    ---@param alternateMsg number Ver: 10.0
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_Button:SetAlternateMessage(alternateMsg)
                    end
                    ---Ver: 10.0
                    ---@param intervalSecs number Ver: 10.0
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_Button:SetContinuousMessages(intervalSecs)
                    end
                    ---Ver: 10.0
                    ---@param img string Ver: 10.0
                    ---@param desiredImageWidth number|nil Default: 0 | Ver: 10.0
                    ---@param desiredImageHeight number|nil Default: 0 | Ver: 10.0
                    ---@param redraw boolean|nil Default: true | Ver: 10.0
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_Button:SetImage(img, desiredImageWidth, desiredImageHeight, redraw)
                    end
                    ---Sets button's label
                    --- | Ver: 10.0
                    ---@param label string Ver: 10.0
                    ---@param adjustSize boolean|nil Default: true | Ver: 10.0
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_Button:SetLabel(label, adjustSize)
                    end
                    ---Ver: 10.0
                    ---@param tooltip string Ver: 10.0
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_Button:SetToolTip(tooltip)
                    end
                    ---Ver: < 9.5
                    ---@param value boolean Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_Button:SetValue(value)
                    end
                    ---Ver: < 9.5
                    ---@return boolean
                    function LM_View.LM_BaseWidget.LM_Button:Value()
                    end
                ---@class LM_CheckBox  
                ---The LM_CheckBox class is a check box that can be marked on or off. To create a new object of this class, use the LM.GUI.CheckBox method.
                LM_View.LM_BaseWidget.LM_CheckBox = {}
                    ---This method sets the value of the check box.
                    ---Example:
                    --- function MyDialog:UpdateWidgets()
                    ---  -- turn the first check box ON:
                    ---  self.MyCheckBox1:SetValue(true)
                    ---
                    ---  -- turn the second check box OFF:
                    ---  self.MyCheckBox2:SetValue(false)
                    --- end
                    --- | Ver: < 9.5
                    ---@param value boolean True to set the box checked, false for unchecked | Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_CheckBox:SetValue(value)
                    end
                    ---Returns true if the box is checked, false otherwise.
                    ---This method returns whether the box is checked or not.
                    ---Example:
                    --- local IsOn = MyCheckBox:Value()
                    --- print(tostring(IsOn))
                    --- | Ver: < 9.5
                    ---@return boolean
                    function LM_View.LM_BaseWidget.LM_CheckBox:Value()
                    end
                ---@class LM_ColorSwatch  
                ---The LM_ColorSwatch class lets the user choose a color. It is filled in with the current color selection, and when the user clicks it, a color picker appears to select a new color. To create a new color swatch, you would use the LM.GUI.ColorSwatch method.
                LM_View.LM_BaseWidget.LM_ColorSwatch = {}
                    ---Ver: 11.0
                    ---@param b boolean Ver: 11.0
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_ColorSwatch:SetConstantMessages(b)
                    end
                    ---Ver: 11.0
                    ---@param beginSelectionMsg number Ver: 11.0
                    ---@param endSelectionMsg number Ver: 11.0
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_ColorSwatch:SetModalMessages(beginSelectionMsg, endSelectionMsg)
                    end
                    ---Set the value of the color swatch
                    ---| Ver: < 9.5
                    ---@param color rgb_color Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_ColorSwatch:SetValue(color)
                    end
                    ---Get the rgb_color value of the color swatch
                    --- | Ver: < 9.5
                    ---@return rgb_color
                    function LM_View.LM_BaseWidget.LM_ColorSwatch:Value()
                    end
                ---@class LM_Divider  
                ---The LM_Divider class is a simple vertical or horizontal line used to visually separate user interface elements. To create a new divider, you would use the LM.GUI.Divider method.
                LM_View.LM_BaseWidget.LM_Divider = {}
                ---@class LM_DynamicText  
                ---The LM_DynamicText class is used to display a text label in a dialog or toolbar, whose value can change. To create a new dynamic text label, you would use the LM.GUI.DynamicText method.
                LM_View.LM_BaseWidget.LM_DynamicText = {}
                    ---Sets the value of the text object
                    --- | Ver: < 9.5
                    ---@param value string|number The value can be a string or a number | Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_DynamicText:SetValue(value)
                    end
                ---@class LM_ImageTextList  
                ---LM_ImageTextList widget in a dialog box. To create an object of this class, use the LM.GUI.ImageTextList method.
                LM_View.LM_BaseWidget.LM_ImageTextList = {}
                    ---Ver: 12.2
                    ---@param label string Ver: 12.2
                    ---@param redraw boolean|nil Default: true | Ver: 12.2
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_ImageTextList:AddItem(label, redraw)
                    end
                    ---Ver: 12.2
                    ---@param label string Ver: 12.2
                    ---@param redraw boolean|nil Default: true | Ver: 12.2
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_ImageTextList:AddItemAlphabetically(label, redraw)
                    end
                    ---Ver: 12.2
                    ---@return boolean
                    function LM_View.LM_BaseWidget.LM_ImageTextList:AllowsMultipleSelection()
                    end
                    ---Ver: 12.2
                    ---@param label string Ver: 12.2
                    ---@return boolean
                    function LM_View.LM_BaseWidget.LM_ImageTextList:ContainsItem(label)
                    end
                    ---Ver: 12.2
                    ---@return number
                    function LM_View.LM_BaseWidget.LM_ImageTextList:CountItems()
                    end
                    ---Ver: 12.2
                    ---@return boolean
                    function LM_View.LM_BaseWidget.LM_ImageTextList:DrawsPrimarySelection()
                    end
                    ---Ver: 12.2
                    ---@param enable boolean Ver: 12.2
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_ImageTextList:Enable(enable)
                    end
                    ---Ver: 12.2
                    ---@param itemIndex number Ver: 12.2
                    ---@return string
                    function LM_View.LM_BaseWidget.LM_ImageTextList:GetItem(itemIndex)
                    end
                    ---As of Moho 12.2, instead of getting the selected items, this method retrieves the item from the list regardless of the item's selection status. See the test file for details.
                    --- | Ver: 12.2
                    ---@param selItemIndex number Ver: 12.2
                    ---@return string
                    function LM_View.LM_BaseWidget.LM_ImageTextList:GetSelItem(selItemIndex)
                    end
                    ---Ver: 12.2
                    ---@param label string Ver: 12.2
                    ---@param where number Ver: 12.2
                    ---@param redraw boolean|nil Default: true | Ver: 12.2
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_ImageTextList:InsertItem(label, where, redraw)
                    end
                    ---Ver: 12.2
                    ---@param itemIndex number Ver: 12.2
                    ---@return boolean
                    function LM_View.LM_BaseWidget.LM_ImageTextList:IsItemSelected(itemIndex)
                    end
                    ---Ver: 12.2
                    ---@return number
                    function LM_View.LM_BaseWidget.LM_ImageTextList:NumSelectedItems()
                    end
                    ---Ver: 12.2
                    ---@param id number Ver: 12.2
                    ---@param redraw boolean|nil Default: true | Ver: 12.2
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_ImageTextList:RemoveItem(id, redraw)
                    end
                    ---Ver: 12.2
                    ---@param itemIndex number Ver: 12.2
                    ---@param scrollToTop boolean|nil Default: false | Ver: 12.2
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_ImageTextList:ScrollItemIntoView(itemIndex, scrollToTop)
                    end
                    ---Ver: 12.2
                    ---@return number
                    function LM_View.LM_BaseWidget.LM_ImageTextList:SelItem()
                    end
                    ---Ver: 12.2
                    ---@return string
                    function LM_View.LM_BaseWidget.LM_ImageTextList:SelItemLabel()
                    end
                    ---Ver: 12.2
                    ---@param allow boolean Ver: 12.2
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_ImageTextList:SetAllowsMultipleSelection(allow)
                    end
                    ---Ver: 12.2
                    ---@param drawPrimary boolean Ver: 12.2
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_ImageTextList:SetDrawsPrimarySelection(drawPrimary)
                    end
                    ---Ver: 12.2
                    ---@param id number Ver: 12.2
                    ---@param label string Ver: 12.2
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_ImageTextList:SetItemLabel(id, label)
                    end
                    ---This method accepts multiple input formats:
                    ---ImageTextList:SetSelItem(id, redraw, addToSelection) | ImageTextList:SetSelItem(label, redraw, addToSelection)
                    ---Up to Moho 14.1 (included), this method doesn't allow to select items by means of IDs. However, there is a workaround that hopefully should fulfill most situations consisting of using SetSelItem(label, redraw, addToSelection) method instead in, e.g., the following way: d.myTextList:SetSelItem(d.myTextList:GetItem(0), true, false)) which with, in this case, you'll get list's first item selected by ID, indirectly, but working as expected.
                    --- | Ver: 12.2
                    ---@param arg1 number | string The id of the item to select; or the label of the item to select | Ver: 12.2
                    ---@param arg2 boolean|nil  Whether to redraw after selection | Default: true | Ver: 12.2
                    ---@param arg3 boolean|nil  Whether to add to the current selection | Default: false | Ver: 12.2
                    ---@return boolean|nil
                    function LM_View.LM_BaseWidget.LM_ImageTextList:SetSelItem(arg1, arg2, arg3)
                    end
                ---@class LM_Menu  
                ---The LM_Menu class represents a list of commands. A menu by itself does not appear in the user interface - it must be attached to an LM_PopupMenu widget in order to be placed in a dialog box or toolbar. To create a new menu, you would use the LM.GUI.Menu method.
                LM_View.LM_BaseWidget.LM_Menu = {}
                    ---Adds a new command to the bottom of the menu
                    --- | Ver: < 9.5
                    ---@param label string the item's text label - in lua a string | Ver: < 9.5
                    ---@param shortcut number a shortcut key used to trigger the menu command. note that the c binding (correctly) describes this as type "char" but in lua this is a number in the range 0 to 255 | Ver: < 9.5
                    ---@param msg number the message value to send when the command is selected by the user | Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_Menu:AddItem(label, shortcut, msg)
                    end
                    ---Adds a new command alphabetically to the menu
                    --- | Ver: < 9.5
                    ---@param label string the item's text label | Ver: < 9.5
                    ---@param shortcut number a shortcut key used to trigger the menu command | Ver: < 9.5
                    ---@param msg number the message value to send when the command is selected by the user | Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_Menu:AddItemAlphabetically(label, shortcut, msg)
                    end
                    ---Returns the number of commands in the menu
                    --- | Ver: < 9.5
                    ---@return number --the number of commands in the menu
                    function LM_View.LM_BaseWidget.LM_Menu:CountItems()
                    end
                    ---Returns the id of the first checked command in the menu (starting at 0 for the first item)
                    --- | Ver: < 9.5
                    ---@return number --the id of the first checked command in the menu
                    function LM_View.LM_BaseWidget.LM_Menu:FirstChecked()
                    end
                    ---Returns the text label of the first checked command in the menu
                    --- | Ver: < 9.5
                    ---@return string --the text label of the first checked command in the menu
                    function LM_View.LM_BaseWidget.LM_Menu:FirstCheckedLabel()
                    end
                    ---Returns the message value of the first checked command in the menu
                    --- | Ver: < 9.5
                    ---@return number --the message value of the first checked command in the menu
                    function LM_View.LM_BaseWidget.LM_Menu:FirstCheckedMsg()
                    end
                    ---Ver: 12.0
                    ---@param atIndex number Ver: 12.0
                    ---@param label string Ver: 12.0
                    ---@param shortcut number Ver: 12.0
                    ---@param msg number Ver: 12.0
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_Menu:InsertItem(atIndex, label, shortcut, msg)
                    end
                    ---Tests whether a menu command is checked
                    --- | Ver: < 9.5
                    ---@param msg number the message value of the desired command | Ver: < 9.5
                    ---@return boolean --true if check, false otherwise
                    function LM_View.LM_BaseWidget.LM_Menu:IsChecked(msg)
                    end
                    ---Checks the enabled status of a menu command
                    --- | Ver: < 9.5
                    ---@param msg number the message value of the desired command | Ver: < 9.5
                    ---@return boolean --true if enabled, false if disabled
                    function LM_View.LM_BaseWidget.LM_Menu:IsEnabled(msg)
                    end
                    ---Returns the text label of a command in the menu
                    --- | Ver: < 9.5
                    ---@param msg number the message value of the desired command | Ver: < 9.5
                    ---@return string --the text label of a command in the menu
                    function LM_View.LM_BaseWidget.LM_Menu:ItemLabel(msg)
                    end
                    ---Removes all commands from a menu, leaving the menu empty
                    --- | Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_Menu:RemoveAllItems()
                    end
                    ---Attaches a checkmark to a menu command
                    --- | Ver: < 9.5
                    ---@param msg number the message value of the desired command | Ver: < 9.5
                    ---@param checked boolean true to add a checkmark, false to remove a checkmark | Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_Menu:SetChecked(msg, checked)
                    end
                    ---Attaches a checkmark to a menu command
                    --- | Ver: < 9.5
                    ---@param label string the text label of the matching menu command | Ver: < 9.5
                    ---@param checked boolean true to add a checkmark, false to remove a checkmark | Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_Menu:SetCheckedLabel(label, checked)
                    end
                    ---Enables or disables a menu command
                    --- | Ver: < 9.5
                    ---@param msg number the message value of the desired command | Ver: < 9.5
                    ---@param enabled boolean true if enabled, false if disabled | Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_Menu:SetEnabled(msg, enabled)
                    end
                    ---Un-checks all commands in the menu
                    --- | Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_Menu:UncheckAll()
                    end
                ---@class LM_PopupDialog
                ---The LM_PopupDialog class creates a popup dialog widget on the screen. Clicking on this object will bring up the actual dialog. To create a new popup dialog widget, you would use the LM.GUI.PopupDialog method.
                LM_View.LM_BaseWidget.LM_PopupDialog = {}
                    ---Returns the dialog object attached to the popup dialog
                    --- | Ver: < 9.5
                    ---@return LM_Dialog --the dialog object attached to the popup dialog
                    function LM_View.LM_BaseWidget.LM_PopupDialog:Dialog()
                    end
                    ---Use this function to attach a dialog to the widget so that when the user clicks on it, the dialog appears
                    --- | Ver: < 9.5
                    ---@param dialog LM_Dialog the dialog object to display when the user clicks on the popup dialog | Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_PopupDialog:SetDialog(dialog)
                    end
                ---@class LM_PopupMenu  
                ---The LM_PopupMenu class creates a popup menu widget on the screen. Clicking on this object will bring up the actual menu. To create a new popup menu widget, you would use the LM.GUI.PopupMenu method.
                LM_View.LM_BaseWidget.LM_PopupMenu = {}
                    ---Returns the menu object attached to the popup menu
                    --- | Ver: < 9.5
                    ---@return LM_Menu --the menu object attached to the popup menu
                    function LM_View.LM_BaseWidget.LM_PopupMenu:Menu()
                    end
                    ---Use this function to attach a menu to the widget so that when the user clicks on it, the menu appears
                    --- | Ver: < 9.5
                    ---@param menu LM_Menu the menu object to display when the user clicks on the popup menu | Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_PopupMenu:SetMenu(menu)
                    end
                ---@class LM_RadioButton  
                ---The LM_RadioButton class represents one choice among several. Two or more radio buttons are typically used to let the user choose between mutually exclusive options. When one is selected, the other choices get automatically de-selected. To create a new radio button, you would use the LM.GUI.RadioButton method.
                LM_View.LM_BaseWidget.LM_RadioButton = {}
                    ---Sets the value of the radio button (whether it is checked or not)
                    --- | Ver: < 9.5
                    ---@param value boolean true if the button should be checked, false if un-checked | Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_RadioButton:SetValue(value)
                    end
                    ---Returns the current value of the radio button
                    --- | Ver: < 9.5
                    ---@return boolean true if checked, false if un-checked
                    function LM_View.LM_BaseWidget.LM_RadioButton:Value()
                    end
                ---@class LM_ScrollBar  
                LM_View.LM_BaseWidget.LM_ScrollBar = {}
                    ---Ver: < 9.5
                    ---@param min number Ver: < 9.5
                    ---@param max number Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_ScrollBar:GetRange(min, max)
                    end
                    ---Ver: < 9.5
                    ---@param min number Ver: < 9.5
                    ---@param max number Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_ScrollBar:SetRange(min, max)
                    end
                    ---Ver: < 9.5
                    ---@param smallStep number Ver: < 9.5
                    ---@param largeStep number Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_ScrollBar:SetStepSize(smallStep, largeStep)
                    end
                    ---Ver: < 9.5
                    ---@param value number Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_ScrollBar:SetValue(value)
                    end
                    ---Ver: < 9.5
                    ---@param value number Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_ScrollBar:SetValueWithMessage(value)
                    end
                    ---Ver: < 9.5
                    ---@return number
                    function LM_View.LM_BaseWidget.LM_ScrollBar:Value()
                    end
                ---@class LM_ShortButton  
                ---To create a new object of this class use the LM.GUI.ShortButton method.
                LM_View.LM_BaseWidget.LM_ShortButton = {}
                    ---Ver: 13.5.2
                    ---@param label string Ver: 13.5.2
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_ShortButton:SetLabel(label)
                    end
                ---@class LM_Slider  
                LM_View.LM_BaseWidget.LM_Slider = {}
                    ---Ver: < 9.5
                    ---@return number
                    function LM_View.LM_BaseWidget.LM_Slider:GetNumTicks()
                    end
                    ---Ver: < 9.5
                    ---@return boolean
                    function LM_View.LM_BaseWidget.LM_Slider:GetShowTicks()
                    end
                    ---Ver: < 9.5
                    ---@return boolean
                    function LM_View.LM_BaseWidget.LM_Slider:GetSnapToTicks()
                    end
                    ---Ver: < 9.5
                    ---@param b boolean Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_Slider:SetConstantMessages(b)
                    end
                    ---Ver: < 9.5
                    ---@param b boolean Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_Slider:SetFatSlider(b)
                    end
                    ---Ver: < 9.5
                    ---@param numTicks number Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_Slider:SetNumTicks(numTicks)
                    end
                    ---Ver: < 9.5
                    ---@param min number Ver: < 9.5
                    ---@param max number Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_Slider:SetRange(min, max)
                    end
                    ---Ver: < 9.5
                    ---@param b boolean Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_Slider:SetShowTicks(b)
                    end
                    ---Ver: < 9.5
                    ---@param b boolean Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_Slider:SetSnapToTicks(b)
                    end
                    ---Ver: < 9.5
                    ---@param value number Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_Slider:SetValue(value)
                    end
                    ---Ver: < 9.5
                    ---@return number
                    function LM_View.LM_BaseWidget.LM_Slider:Value()
                    end
                ---@class LM_StaticText  
                ---The LM_StaticText class is used to display a non-changing text label in a dialog or toolbar. To create a new static text label, you would use the LM.GUI.StaticText method.
                LM_View.LM_BaseWidget.LM_StaticText = {}
                ---@class LM_TextControl  
                ---The LM_TextControl class displays a box where the user can enter text or numbers. To create a new LM_TextControl, you would use the LM.GUI.TextControl method.
                LM_View.LM_BaseWidget.LM_TextControl = {}
                    ---Returns the text string contained in the control as a floating-point number
                    --- | Ver: < 9.5
                    ---@return number --the number contained in the control
                    function LM_View.LM_BaseWidget.LM_TextControl:FloatValue()
                    end
                    ---Returns the text string contained in the control as an integer
                    --- | Ver: < 9.5
                    ---@return number --The number contained in the control
                    function LM_View.LM_BaseWidget.LM_TextControl:IntValue()
                    end
                    ---Tells the control to send messages whenever the text changes
                    ---Normally, a text control will send a message when the user presses tab to move to the next text field. This function tells the control to send messages whenever the text changes at all, even if the user is still typing away.
                    --- | Ver: < 9.5
                    ---@param b boolean true to send a message for every change, false to send messages only when the user leaves the text control's focus | Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_TextControl:SetConstantMessages(b)
                    end
                    --- Ver: 14.0
                    ---@param n number Ver: 14.0
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_TextControl:SetMaxDecimalPlaces(n)
                    end
                    --- Ver: 14.0
                    ---@param percentageMode boolean Ver: 14.0
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_TextControl:SetPercentageMode(percentageMode)
                    end
                    --- Ver: 14.0
                    ---@param unitType number Text control units to display - see the constants for this option UNIT_NONE | Ver: 14.0
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_TextControl:SetUnits(unitType)
                    end
                    --- Ver: < 9.5
                    ---@param value string|number The value can be a string or a number | Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_TextControl:SetValue(value)
                    end
                    ---Sets how much the numerical value should change with each movement of the scroll wheel.
                    ---When a text control contains a numerical value, the user can adjust this value by holding the mouse over the box and rolling the scroll wheel up and down. Use this function to set how much the numerical value should change with each movement of the scroll wheel.
                    --- | Ver: < 9.5
                    ---@param f number the increment to add or subtract for each scroll wheel "click" | Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_TextControl:SetWheelInc(f)
                    end
                    --- Ver: 10.0
                    ---@param b boolean Ver: 10.0
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_TextControl:SetWheelInteger(b)
                    end
                    ---Returns the text string contained in the control
                    --- | Ver: < 9.5
                    ---@return string --the control's text
                    function LM_View.LM_BaseWidget.LM_TextControl:Value()
                    end
                ---@class LM_TextList  
                ---The LM_TextList class provides a scrollable list of text items. To create a new text list, you would use the LM.GUI.TextList method.
                LM_View.LM_BaseWidget.LM_TextList = {}
                    ---Adds a new text item to the end of the list
                    --- | Ver: < 9.5
                    ---@param label string the text string to add to the list | Ver: < 9.5
                    ---@param redraw boolean|nil Default: true | Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_TextList:AddItem(label, redraw)
                    end
                    ---Inserts a new item into the list alphabetically.
                    --- | Ver: < 9.5
                    ---@param label string the text string to add to the list | Ver: < 9.5
                    ---@param redraw boolean|nil Default: true | Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_TextList:AddItemAlphabetically(label, redraw)
                    end
                    ---Returns the number of entries in the list
                    --- | Ver: < 9.5
                    ---@return number --the number of entries in the list
                    function LM_View.LM_BaseWidget.LM_TextList:CountItems()
                    end
                    ---Returns the string value of an item in the list
                    --- | Ver: < 9.5
                    ---@param id number the number of the entry (starting with 0 for the first item) | Ver: < 9.5
                    ---@return string --the string value of an item in the list
                    function LM_View.LM_BaseWidget.LM_TextList:GetItem(id)
                    end
                    ---Removes an item from the list
                    --- | Ver: < 9.5
                    ---@param id number the number of the entry to remove (starting with 0 for the first item) | Ver: < 9.5
                    ---@param redraw boolean|nil Default: true | Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_TextList:RemoveItem(id, redraw)
                    end
                    ---Returns the id number of the currently selected item (starting with 0 for the first item)
                    --- | Ver: < 9.5
                    ---@return number --the id number of the currently selected item
                    function LM_View.LM_BaseWidget.LM_TextList:SelItem()
                    end
                    ---Returns the text string associated with the currently selected item
                    --- | Ver: < 9.5
                    ---@return string --the text string associated with the currently selected item
                    function LM_View.LM_BaseWidget.LM_TextList:SelItemLabel()
                    end
                    ---Changes the text string associated with an entry in the list
                    ---Ver: < 9.5
                    ---@param id number the number of the entry (starting with 0 for the first item) | Ver: < 9.5
                    ---@param label string the new text string to replace that entry in the list | Ver: < 9.5
                    ---@return nil
                    function LM_View.LM_BaseWidget.LM_TextList:SetItemLabel(id, label)
                    end
                    ---This method accepts multiple input formats:
                    ---TextList:SetSelItem(id, redraw) | TextList:SetSelItem(label, redraw).
                    ---The first one Selects an item in the list and the second one Selects an item that matches the supplied string.
                    --- | Ver: < 9.5
                    ---@param arg1 number | string The id number of the entry (starting with 0 for the first item); a string matching the item you wish to select | Ver: < 9.5
                    ---@param arg2 boolean|nil Default: true | Ver: < 9.5
                    ---@return boolean|nil --true if the item exists and is selected, false if the item cannot be found
                    function LM_View.LM_BaseWidget.LM_TextList:SetSelItem(arg1, arg2)
                    end
            ---@class LM_LayoutView  
            ---The LM_LayoutView class is used to arrange other user interface objects in a dialog box or toolbar. You never create a layout view yourself - it is provided for you when you create a dialog box or fill in a toolbar.
            LM_View.LM_LayoutView = {}  
                ---Add a new widget to the layout. Use this to add user interface controls one at a time to a dialog box.
                --- | Ver: < 9.5
                ---@param view any The view to add to the layout | Ver: < 9.5
                ---@param alignment number|nil How to align the child view - see alignment constants LM.GUI.ALIGN_CENTER | Ver: < 9.5
                ---@param indent number|nil How far to indent the child view | Default: 0 | Ver: < 9.5
                ---@return nil
                function LM_View.LM_LayoutView:AddChild(view, alignment, indent)
                end
                ---Add some blank space to the layout. Pass a value of 0 for flexible padding that will expand to fit available space.
                --- | Ver: < 9.5
                ---@param pixels number|nil How much blank space in pixels | Default: LM_DEFAULT_PADDING | Ver: < 9.5
                ---@return nil
                function LM_View.LM_LayoutView:AddPadding(pixels)
                end
                ---Begin indenting all further views that get added to the layout.
                --- | Ver: < 9.5
                ---@param amount number|nil How far to indent in pixels | Default: LM_DEFAULT_INDENT | Ver: < 9.5
                ---@return nil
                function LM_View.LM_LayoutView:Indent(amount)
                end
                ---End the current row or column of layout and go back to the style of layout before that row or column was started.
                --- | Ver: < 9.5
                ---@return nil
                function LM_View.LM_LayoutView:Pop()
                end
                ---Switch to horizontal layout for the next set of child views to be added.
                --- | Ver: < 9.5
                ---@param alignment number|nil How to align the horizontal row in the current layout | Default: LM_ALIGN_LEFT | Ver: < 9.5
                ---@param padding number|nil How much padding to provide between elements | Default: -1 | Ver: < 9.5
                ---@return nil
                function LM_View.LM_LayoutView:PushH(alignment, padding)
                end
                ---Switch to vertical layout for the next set of child views to be added.
                --- | Ver: < 9.5
                ---@param alignment number|nil How to align the vertical column in the current layout | Default: LM_ALIGN_LEFT | Ver: < 9.5
                ---@param padding number|nil How much padding to provide between elements | Default: -1 | Ver: < 9.5
                ---@return nil
                function LM_View.LM_LayoutView:PushV(alignment, padding)
                end
                ---Stop indenting all further views that get added to the layout.
                --- | Ver: < 9.5
                ---@param amount number|nil How far to un-indent in pixels | Default: LM_DEFAULT_INDENT | Ver: < 9.5
                ---@return nil
                function LM_View.LM_LayoutView:Unindent(amount)
                end
        ---@class LM_Message  
        ---@field fWhat number --The fWhat value of a message object indicates what the message is supposed to represent. For example, each button in a dialog should have been assigned a unique message value. When the dialog receives a message, this value tells it what to do. | Ver: < 9.5
        ---The LM_Message class is used to notify a script when certain actions take place, like a button being pressed or when a menu selection is made. If necessary, instances of this class can be created by:
        LM_Message = {}
            ---Ver: < 9.5
            ---@return nil
            function LM_Message:ClearKeys()
            end
            ---Ver: < 9.5
            ---@return number
            function LM_Message:CountKeys()
            end
            ---Ver: < 9.5
            ---@param key string Ver: < 9.5
            ---@return boolean
            function LM_Message:GetBool(key)
            end
            ---Ver: < 9.5
            ---@param key string Ver: < 9.5
            ---@return rgb_color
            function LM_Message:GetColor(key)
            end
            ---Ver: < 9.5
            ---@param key string Ver: < 9.5
            ---@return number
            function LM_Message:GetFloat(key)
            end
            ---Ver: < 9.5
            ---@param key string Ver: < 9.5
            ---@return number
            function LM_Message:GetInt(key)
            end
            ---Ver: < 9.5
            ---@param keyID number Ver: < 9.5
            ---@return string
            function LM_Message:GetKeyName(keyID)
            end
            ---Ver: < 9.5
            ---@param key string Ver: < 9.5
            ---@return LM_Message
            function LM_Message:GetMessage(key)
            end
            ---Ver: < 9.5
            ---@param key string Ver: < 9.5
            ---@return Point
            function LM_Message:GetPoint(key)
            end
            ---Ver: < 9.5
            ---@param key string Ver: < 9.5
            ---@return nil
            function LM_Message:GetPtr(key)
            end
            ---Ver: < 9.5
            ---@param key string Ver: < 9.5
            ---@return Rect
            function LM_Message:GetRect(key)
            end
            ---Ver: < 9.5
            ---@param key string Ver: < 9.5
            ---@return string
            function LM_Message:GetString(key)
            end
            ---Ver: < 9.5
            ---@param key string Ver: < 9.5
            ---@return Vector2
            function LM_Message:GetVector2(key)
            end
            ---Ver: < 9.5
            ---@param key string Ver: < 9.5
            ---@return Vector3
            function LM_Message:GetVector3(key)
            end
            ---Ver: < 9.5
            ---@param key string Ver: < 9.5
            ---@return boolean
            function LM_Message:HasKey(key)
            end
            ---Ver: < 9.5
            ---@param key string Ver: < 9.5
            ---@return nil
            function LM_Message:Remove(key)
            end
            ---Set the value of the message
            --- | Ver: < 9.5
            ---@param key string Ver: < 9.5
            ---@param value string|boolean|number|rgb_color|Point|Vector2|Vector3|Rect|LM_Message Ver: < 9.5
            ---@return nil
            function LM_Message:Set(key, value)
            end
            ---Ver: < 9.5
            ---@param key string Ver: < 9.5
            ---@param value any Ver: < 9.5
            ---@return nil
            function LM_Message:SetPtr(key, value)
            end
        ---@class LM_Dialog  
        ---Parent class for LM_SimpleDialog.
        LM_Dialog = {}
            ---@class LM_SimpleDialog  
            ---The LM_SimpleDialog class is used to create dialog boxes - windows that can be filled with other user interface controls. An LM_SimpleDialog object automatically provides OK and Cancel buttons. To create a new object of this class, use the LM.GUI.SimpleDialog method.
            LM_Dialog.LM_SimpleDialog = {}
                ---Displays the dialog box on screen in a modal fashion (meaning that nothing else will happen in Moho until the user presses OK or Cancel in the dialog)
                --- | Ver: < 9.5
                ---@return number --LM.GUI.MSG_OK if the user presses OK, or LM.GUI.MSG_CANCEL if the user presses Cancel
                function LM_Dialog.LM_SimpleDialog:DoModal()
                end
                ---Displays the dialog box on screen in a modeless fashion (meaning that the user is free to work in other parts of the Moho application while the dialog is open)
                --- | Ver: < 9.5
                ---@return nil
                function LM_Dialog.LM_SimpleDialog:DoModeless()
                end
                ---Ver: < 9.5
                ---@return LM_LayoutView
                function LM_Dialog.LM_SimpleDialog:GetLayout()
                end
                ---This function is called when a user interface object in the dialog sends a message. Often, you can ignore these messages, but if your dialog needs to respond to one or more messages, it can do so by implementing this function.
                --- | Ver: < 9.5
                ---@param msg LM_Message A message object generated by the user interacting with the dialog | Ver: < 9.5
                ---@return nil
                function LM_Dialog.LM_SimpleDialog:HandleMessage(msg)
                end
                ---This function is called if the user presses the dialog's OK button.
                ---When this function is called, you should read the values from the user interface controls and act on them in some way appropriate to the dialog's intended purpose.
                --- | Ver: < 9.5
                ---@return nil
                function LM_Dialog.LM_SimpleDialog:OnOK()
                end
                ---This function is called if the user presses the dialog's OK button.
                ---This function should text the values of the user interface controls and determine if they are all within allowed ranges (the Validate function above can be helpful for this).
                ---If this function determines that the dialog settings are all allowed, it should return true.
                ---Otherwise, it should return false - the user will then have the opportunity to correct illegal values and try pressing the OK button again.
                --- | Ver: < 9.5
                ---@return boolean --True if the dialog's values are legal, otherwise false
                function LM_Dialog.LM_SimpleDialog:OnValidate()
                end
                ---This function is called when the user interface controls need to be updated.
                ---Typically, it is called once when the dialog is first displayed.
                ---When this function is called, you should adjust the values of the user interface controls to match the underlying data.
                --- | Ver: < 9.5
                ---@return nil
                function LM_Dialog.LM_SimpleDialog:UpdateWidgets()
                end
                ---This function tests that the value of a numerical text field falls within a certain range.
                ---This can be useful if you only want the dialog to allow certain ranges of numbers.
                --- | Ver: < 9.5
                ---@param pCtrl LM_TextControl an LM_TextControl object whose numerical value you wish to test | Ver: < 9.5
                ---@param low number the minimum desired value for the text field | Ver: < 9.5
                ---@param high number the maximum desired value for the text field | Ver: < 9.5
                ---@return boolean --true if the numerical value is within the range of low to high
                function LM_Dialog.LM_SimpleDialog:Validate(pCtrl, low, high)
                end
        ---@class LM_Graphics  
        ---The LM_Graphics class is used for drawing in an LM_View. It lets you draw basic lines and shapes, and to convert between coordinate systems (the screen pixel space and the 3D coordinate system of Moho layers). You typically would use an LM_Graphics object with a moho tool script to determine what object(s) the user clicks on and to draw extra tool elements like control handles.
        --->>> WIP >>>
        ---There are two groups of "Draw" methods.
        ---Those that use a vec2 as the location and those that separate the x and y dimensions.
        ---Empirical evidence indicates that these groups interpret "radius" values differently.
        ---The vec2 group uses moho units so: ... where the view is of a very small area of the workspace a very small radius gives the same number of on-screen pixels as … where the screen shows the whole work area and beyond a large radius is needed to give the same on-screen size.
        ---The x,y group interprets "radius" as an on-screen size irrespective of the level of workspace zoom.
        LM_Graphics = {}
            ---Add a line segment to the current enclosed shape. All the line segments you add should form the outer boundary of the shape.
            --- | Ver: < 9.5
            ---@param end1 Vector2 First endpoint of the line segment | Ver: < 9.5
            ---@param end2 Vector2 Second endpoint of the line segment | Ver: < 9.5
            ---@return nil
            function LM_Graphics:AddLine(end1, end2)
            end
            ---Prepend an arbitrary matrix to the current matrix transform.
            --- | Ver: < 9.5
            ---@param matrix Matrix Ver: < 9.5
            ---@return nil
            function LM_Graphics:ApplyMatrix(matrix)
            end
            ---This function may be called before any other commands on an LM_Graphics object in order to reset it.
            ---For example, if it is called after ApplyMatrix() it will reset the transforms as though the matrix had not been applied.
            ---Often it is not necessary to call this.
            ---For example to draw a line between two points p2 and p3: --
            --- local g = view:Graphics()
            --- local matrix = LM.Matrix:new_local()
            --- moho.drawingLayer:GetFullTransform(moho.layerFrame, matrix, moho.document)
            --- g:Push()
            --- g:ApplyMatrix(matrix)
            --- g:SetColor(MOHO.MohoGlobals.SelCol)
            --- g:DrawLine(p2.x, p2.y, p3.x, p3.y)
            --- g:Pop()
            --- | Ver: < 9.5
            ---@return nil
            function LM_Graphics:BeginDraw()
            end
            ---Call this function to begin testing whether the user has clicked on an object.
            --- | Ver: < 9.5
            ---@param where Point The pixel location you want to test (often the location of a mouse click) | Ver: < 9.5
            ---@param pickWidth number Ver: < 9.5
            ---@return nil
            function LM_Graphics:BeginPicking(where, pickWidth)
            end
            ---Begin drawing an enclosed shape.
            --- | Ver: < 9.5
            ---@return nil
            function LM_Graphics:BeginShape()
            end
            ---Clear the drawing area.
            --- | Ver: < 9.5
            ---@param r number Red | Ver: < 9.5
            ---@param g number Green | Ver: < 9.5
            ---@param b number Blue | Ver: < 9.5
            ---@param a number|nil Alpha (opacity) | Default: 255 | Ver: < 9.5
            ---@return nil
            function LM_Graphics:Clear(r, g, b, a)
            end
            ---Returns the current scale of the drawing. A value of 1 means the drawing is actual size, 0.5 would be half size, 2.0 would be double size, etc.
            --- | Ver: < 9.5
            ---@param ignoreZoom boolean True if you wish to ignore zoom effects, false otherwise | Ver: < 9.5
            ---@return number --the drawing's current scale
            function LM_Graphics:CurrentScale(ignoreZoom)
            end
            ---Returns the current matrix transformation.
            --- | Ver: < 9.5
            ---@return Matrix
            function LM_Graphics:CurrentTransform()
            end
            ---This method accepts multiple input formats:
            ---LM_Graphics:DrawDiamondMarker(xf, yf, radius) | LM_Graphics:DrawDiamondMarker(xf, yf, zf, radius)
            --- | Ver: 9.5
            ---@param arg1 number Ver: 9.5
            ---@param arg2 number Ver: 9.5
            ---@param arg3 number Ver: 9.5
            ---@param arg4 number|nil Ver: 9.5
            ---@return nil
            function LM_Graphics:DrawDiamondMarker(arg1, arg2, arg3, arg4)
            end
            ---Ver: 10.0
            ---@param width number Ver: 10.0
            ---@param x1 number Ver: 10.0
            ---@param y1 number Ver: 10.0
            ---@param x2 number Ver: 10.0
            ---@param y2 number Ver: 10.0
            ---@return nil
            function LM_Graphics:DrawFatLine(width, x1, y1, x2, y2)
            end
            ---Ver: 11.0
            ---@param width1 number Ver: 11.0
            ---@param width2 number Ver: 11.0
            ---@param x1 number Ver: 11.0
            ---@param y1 number Ver: 11.0
            ---@param x2 number Ver: 11.0
            ---@param y2 number Ver: 11.0
            ---@return nil
            function LM_Graphics:DrawFatLine2(width1, width2, x1, y1, x2, y2)
            end
            ---Draw a fat marker.
            --- | Ver: < 9.5
            ---@param xf number X location | Ver: < 9.5
            ---@param yf number Y location | Ver: < 9.5
            ---@param radius number Radius | Ver: < 9.5
            ---@return nil
            function LM_Graphics:DrawFatMarker(xf, yf, radius)
            end
            ---Draw a line between two pixels.
            --- | Ver: < 9.5
            ---@param x1 number Start x location | Ver: < 9.5
            ---@param y1 number Start y location | Ver: < 9.5
            ---@param x2 number End x location | Ver: < 9.5
            ---@param y2 number End y location | Ver: < 9.5
            ---@return nil
            function LM_Graphics:DrawLine(x1, y1, x2, y2)
            end
            ---Draw a marker (for example, the control points on Moho curves).
            --- | Ver: < 9.5
            ---@param xf number X location | Ver: < 9.5
            ---@param yf number Y location | Ver: < 9.5
            ---@return nil
            function LM_Graphics:DrawMarker(xf, yf)
            end
            ---Call this function when you're finished with all drawing operations.
            --- | Ver: < 9.5
            ---@return nil
            function LM_Graphics:EndDraw()
            end
            ---End the current enclosed shape and draw it on the screen.
            --- | Ver: < 9.5
            ---@return nil
            function LM_Graphics:EndShape()
            end
            ---Draw a filled circle
            --- | Ver: < 9.5
            ---@param center Vector2 Center point | Ver: < 9.5
            ---@param radius number Radius | Ver: < 9.5
            ---@return nil
            function LM_Graphics:FillCircle(center, radius)
            end
            ---Ver: < 9.5
            ---@param center Vector2 Ver: < 9.5
            ---@param radius number Ver: < 9.5
            ---@return nil
            function LM_Graphics:FillCirclePixelRadius(center, radius)
            end
            ---Ver: 9.5
            ---@param r Rect Ver: 9.5
            ---@return nil
            function LM_Graphics:FillOval(r)
            end
            ---Ver: < 9.5
            ---@param seedX number Ver: < 9.5
            ---@param seedY number Ver: < 9.5
            ---@return boolean
            function LM_Graphics:FloodFill(seedX, seedY)
            end
            ---Draw a circle outline.
            --- | Ver: < 9.5
            ---@param center Vector2 Center point | Ver: < 9.5
            ---@param radius number Radius | Ver: < 9.5
            ---@return nil
            function LM_Graphics:FrameCircle(center, radius)
            end
            ---Ver: < 9.5
            ---@param center Vector2 Ver: < 9.5
            ---@param radius number Ver: < 9.5
            ---@return nil
            function LM_Graphics:FrameCirclePixelRadius(center, radius)
            end
            ---Ver: 9.5
            ---@param r Rect Ver: 9.5
            ---@return nil
            function LM_Graphics:FrameOval(r)
            end
            ---Returns the height in pixels of the drawing area.
            --- | Ver: < 9.5
            ---@return number
            function LM_Graphics:Height()
            end
            ---Test whether a given pixel is 100% white.
            --- | Ver: < 9.5
            ---@param where Point The pixel you wish to test | Ver: < 9.5
            ---@return boolean --true if the pixel is white, otherwise false
            function LM_Graphics:IsFullWhite(where)
            end
            ---This method accepts multiple input formats:
            ---LM_Graphics:LineTo(real, real) | LM_Graphics:LineTo(int32, int32)
            ---Draw a line from the current pen position to the given pixel.
            --- | Ver: < 9.5
            ---@param arg1 number X location | Ver: < 9.5
            ---@param arg2 number Y location | Ver: < 9.5
            ---@return nil
            function LM_Graphics:LineTo(arg1, arg2)
            end
            ---This method accepts multiple input formats:
            ---LM_Graphics:MoveTo(real, real) | LM_Graphics:MoveTo(int32, int32)
            ---Move the pen location to a given pixel. Nothing is drawn.
            --- | Ver: < 9.5
            ---@param arg1 number X location | Ver: < 9.5
            ---@param arg2 number Y location | Ver: < 9.5
            ---@return nil
            function LM_Graphics:MoveTo(arg1, arg2)
            end
            
            ---After drawing an object, call this function to see if it was clicked on by the user.
            --- | Ver: < 9.5
            ---@return boolean --true if the object was picked, otherwise false
            function LM_Graphics:Pick()
            end
            ---Pop the current matrix stack. This is a way of restoring a saved transformation.
            --- | Ver: < 9.5
            ---@return nil
            function LM_Graphics:Pop()
            end
            ---Push the current matrix stack. This is a way of saving the current transformation.
            --- | Ver: < 9.5
            ---@return nil
            function LM_Graphics:Push()
            end
            ---Prepend a rotation to the current matrix transform.
            --- | Ver: < 9.5
            ---@param angle number Angle in radians | Ver: < 9.5
            ---@return nil
            function LM_Graphics:Rotate(angle)
            end
            ---Prepend a 2D scale to the current matrix transform.
            --- | Ver: < 9.5
            ---@param sx number X scale | Ver: < 9.5
            ---@param sy number Y scale | Ver: < 9.5
            ---@return nil
            function LM_Graphics:Scale(sx, sy)
            end
            ---This method accepts multiple input formats:
            ---LM_Graphics:ScreenToWorld(Point, Vector2) | LM_Graphics:ScreenToWorld(Point, Vector3)
            ---Converts a screen pixel location to world coordinates. As an example, this can be used to convert between a mouse click to a location in the current Moho layer.
            --- | Ver: < 9.5
            ---@param arg1 Point Pixel location | Ver: < 9.5
            ---@param arg2 Vector2|Vector3 The world position - filled in by this function | Ver: < 9.5
            ---@return nil
            function LM_Graphics:ScreenToWorld(arg1, arg2)
            end
            ---Draw a selection rectangle.
            --- | Ver: < 9.5
            ---@param r Rect The rectangle to draw | Ver: < 9.5
            ---@param fillRect boolean|nil Default: false | Ver: < 9.5
            ---@return nil
            function LM_Graphics:SelectionRect(r, fillRect)
            end
            ---Ver: < 9.5
            ---@param pixels number Ver: < 9.5
            ---@return nil
            function LM_Graphics:SetBezierTolerance(pixels)
            end
            ---This method accepts multiple input formats:
            ---LM_Graphics:SetColor(rgb_color) | LM_Graphics:SetColor(int32, int32, int32, int32)
            ---Sets the drawing color for subsequent drawing operations.
            --- | Ver: < 9.5
            ---@param arg1 rgb_color|number The color object or red component | Ver: < 9.5
            ---@param arg2 number|nil The green component. Leave empty if using rgb_color | Ver: < 9.5
            ---@param arg3 number|nil The blue component. Leave empty if using rgb_color | Ver: < 9.5
            ---@param arg4 number|nil The alpha (opacity) component | Default: 255 | Ver: < 9.5
            ---@return nil
            function LM_Graphics:SetColor(arg1, arg2, arg3, arg4)
            end
            ---Sets the width in pixels for line drawing operations.
            --- | Ver: < 9.5
            ---@param w number Line pixel width | Ver: < 9.5
            ---@return nil
            function LM_Graphics:SetPenWidth(w)
            end
            ---Turns on or off smoothing (antialiasing) for drawing operations.
            --- | Ver: < 9.5
            ---@param value boolean True to draw smooth lines and shapes, false to turn off smoothing | Ver: < 9.5
            ---@return nil
            function LM_Graphics:SetSmoothing(value)
            end
            ---Ver: 10.0
            ---@param mode number Ver: 10.0
            ---@return nil
            function LM_Graphics:SetViewMode(mode)
            end
            ---Ver: 10.0
            ---@param offsetX number Ver: 10.0
            ---@param offsetY number Ver: 10.0
            ---@return nil
            function LM_Graphics:SetViewOffset(offsetX, offsetY)
            end
            ---Ver: 10.0
            ---@param pixelScaling number Ver: 10.0
            ---@return nil
            function LM_Graphics:SetViewPixelScaling(pixelScaling)
            end
            ---Ver: 10.0
            ---@param angle number Ver: 10.0
            ---@return nil
            function LM_Graphics:SetViewRotation(angle)
            end
            ---Ver: 10.0
            ---@param z number Ver: 10.0
            ---@return nil
            function LM_Graphics:SetViewZoom(z)
            end
            ---Prepend a 2D translation to the current matrix transform.
            --- | Ver: < 9.5
            ---@param dx number X translation | Ver: < 9.5
            ---@param dy number Y translation | Ver: < 9.5
            ---@return nil
            function LM_Graphics:Translate(dx, dy)
            end
            ---Ver: 10.0
            ---@return number
            function LM_Graphics:ViewMode()
            end
            ---Ver: 10.0
            ---@return Vector2
            function LM_Graphics:ViewOffset()
            end
            ---Ver: 10.0
            ---@return number
            function LM_Graphics:ViewPixelScaling()
            end
            ---Ver: 10.0
            ---@return number
            function LM_Graphics:ViewRotation()
            end
            ---Ver: 10.0
            ---@return number
            function LM_Graphics:ViewZoom()
            end
            ---Returns the width in pixels of the drawing area.
            --- | Ver: < 9.5
            ---@return number
            function LM_Graphics:Width()
            end
            ---This method accepts multiple input formats:
            ---LM_Graphics:WorldToScreen(Vector2, Point) | LM_Graphics:WorldToScreen(Vector2, Vector2)
            ---Converts a 2D world position to a screen pixel location. As an example, this can be used to convert between a point in a Moho layer to a screen pixel.
            --- | Ver: < 9.5
            ---@param arg1 Vector2 2D world position | Ver: < 9.5
            ---@param arg2 Point|Vector2 Pixel location - filled in by this function | Ver: < 9.5
            ---@return boolean returns true if the point is visible on screen, false otherwise
            function LM_Graphics:WorldToScreen(arg1, arg2)
            end
            ---@class LM_Graphics_OpenGL_Fixed  
            ---Added in 11.1
            LM_Graphics.LM_Graphics_OpenGL_Fixed = {}
                ---Added in version 14.0 to replace LM_Graphics_OpenGL_Fixed:OpenGLMode
                --- | Ver: 14.0
                ---@return boolean
                function LM_Graphics.LM_Graphics_OpenGL_Fixed:GPUMode()
                end
                ---Added in version 11. In Moho 14.0 replaced by LM_Graphics_OpenGL_Fixed:GPUMode
                --- | Ver: 11.0
                ---@return boolean
                function LM_Graphics.LM_Graphics_OpenGL_Fixed:OpenGLMode()
                end
---@class MOHO
---@field Localize fun(path: string): string --in this funciton you can choose your language: local phrase = {} phrase['uiLabel'] = "Add Keyframe" local fileWord = MOHO.Localize("/Menus/File/File=File") if fileWord == "Файл" then  phrase['uiLabel'] = "Пробивка Ключей" end.  Here is how to choose your language: AR ملف, CA Dossier, DE Datei, EN File, ES Archivo, FA فایل, FR Fichier, HI फ़ाइल, JA ファイル, KO 파일, PT Arquivo, RU Файл, TR Dosya, ZH_CN 文件, ZH_HK 文件
---@field VECTOR3D_EXTRUDE number --Value: 1 | Ver: < 9.5
---@field VECTOR3D_INFLATE number --Value: 3 | Ver: < 9.5
---@field VECTOR3D_LATHE number   --Value: 2 | Ver: < 9.5
---@field VECTOR3D_NONE number   --Value: 0 | Ver: < 9.5
---@field BM_ADD number           --Value: 4 | Ver: < 9.5 | Desc: Additive blending
---@field BM_COLOR number        --Value: 8 | Ver: < 9.5 | Desc: Color blending
---@field BM_COLOR_BURN number   --Value: 13 | Ver: 14.2 | Desc: Masked layers, disables masking and masked layers
---@field BM_COLOR_DODGE number  --Value: 11 | Ver: 13.5.2 | Desc: Color Dodge
---@field BM_DIFFERENCE number   --Value: 5 | Ver: < 9.5 | Desc: Difference blending
---@field BM_HUE number          --Value: 6 | Ver: < 9.5 | Desc: Hue blending
---@field BM_LINEAR_DODGE number --Value: 12 | Ver: 14.2 | Desc: Masked layers, disables masking and masked layers
---@field BM_LUMINOSITY number   --Value: 9 | Ver: < 9.5 | Desc: Luminosity blending
---@field BM_MULTIPLY number     --Value: 1 | Ver: < 9.5 | Desc: Multiply blending
---@field BM_NORMAL number       --Value: 0 | Ver: < 9.5 | Desc: Normal blending
---@field BM_OVERLAY number      --Value: 3 | Ver: < 9.5 | Desc: Overlay blending
---@field BM_SATURATION number   --Value: 7 | Ver: < 9.5 | Desc: Saturation blending
---@field BM_SCREEN number       --Value: 2 | Ver: < 9.5 | Desc: Screen blending
---@field BM_SOFT_LIGHT number   --Value: 10 | Ver: 13.5.2 | Desc: Soft Light
---@field FLEXI_BINDING number  --Value: 1 | Ver: < 9.5 | Desc: Flexible binding
---@field REGION_BINDING number --Value: 2 | Ver: < 9.5 | Desc: Region binding
---@field FC_CAMERA_FREE number   --Value: 4 | Ver: < 9.5 | Desc: Rotate freely to face camera
---@field FC_CAMERA_X number      --Value: 1 | Ver: < 9.5 | Desc: Rotate on X axis to face camera
---@field FC_CAMERA_Y number      --Value: 2 | Ver: < 9.5 | Desc: Rotate on Y axis to face camera
---@field FC_CAMERA_Z number      --Value: 3 | Ver: < 9.5 | Desc: Rotate on Z axis to face camera
---@field FC_NONE number          --Value: 0 | Ver: < 9.5 | Desc: No camera-facing rotation
---@field FC_PLANE_FREE number    --Value: 8 | Ver: < 9.5 | Desc: Rotate freely to face camera plane
---@field FC_PLANE_X number       --Value: 5 | Ver: < 9.5 | Desc: Rotate on X axis to face camera plane
---@field FC_PLANE_Y number       --Value: 6 | Ver: < 9.5 | Desc: Rotate on Y axis to face camera plane
---@field FC_PLANE_Z number       --Value: 7 | Ver: < 9.5 | Desc: Rotate on Z axis to face camera plane
---@field CHANNEL_BOOL number    --Value: 4 | Ver: < 9.5 | Desc: Boolean channel
---@field CHANNEL_COLOR number   --Value: 3 | Ver: < 9.5 | Desc: Color channel
---@field CHANNEL_STRING number  --Value: 5 | Ver: < 9.5 | Desc: String channel
---@field CHANNEL_UNKNOWN number --Value: 0 | Ver: < 9.5 | Desc: Unknown channel type
---@field CHANNEL_VAL number     --Value: 1 | Ver: < 9.5 | Desc: Floating-point value channel
---@field CHANNEL_VEC2 number    --Value: 2 | Ver: < 9.5 | Desc: 2D vector channel
---@field CHANNEL_VEC3 number    --Value: 6 | Ver: < 9.5 | Desc: 3D vector channel
---@field PEAKED number      --Value: 0 | Ver: < 9.5 | Desc: Sharp corner at curve point
---@field PEAK_WEIGHT number --Value: 9.9999997473788e-05 | Ver: 14.2
---@field SMOOTH number      --Value: 0.30000001192093 | Ver: < 9.5 | Desc: Smooth curve at point
---@field CURVE_INTERP_LEGACY number --Value: 0 | Ver: < 9.5 | Desc: Legacy curve type
---@field CURVE_INTERP_V7 number     --Value: 1 | Ver: < 9.5 | Desc: Curve type from version 7+
---@field MSG_BASE number   --Value: -40000 | Ver: < 9.5 | Desc: Base message code for Moho scripts
---@field MSG_CANCEL number --Value: -1008 | Ver: < 9.5 | Desc: Cancel or Escape
---@field MSG_OK number     --Value: -1007 | Ver: < 9.5 | Desc: OK or Enter
---@field GRADIENT_ANGLE number     --Value: 3 | Ver: 14.0
---@field GRADIENT_LINEAR number    --Value: 0 | Ver: 14.0
---@field GRADIENT_RADIAL number    --Value: 1 | Ver: 14.0
---@field GRADIENT_REFLECTED number --Value: 2 | Ver: 14.0
---@field GROUP_MASK_HIDE_ALL number --Value: 2 | Ver: < 9.5 | Desc: Masking hides all sub-layers
---@field GROUP_MASK_NONE number     --Value: 0 | Ver: < 9.5 | Desc: No masking
---@field GROUP_MASK_SHOW_ALL number --Value: 1 | Ver: < 9.5 | Desc: Masking shows all sub-layers
---@field SM_BILINEAR number --Value: 1 | Ver: < 9.5 | Desc: Bilinear sampling
---@field SM_NEAREST number  --Value: 0 | Ver: < 9.5 | Desc: Nearest neighbor sampling
---@field INTERP_BEZIER number     --Value: 9 | Ver: < 9.5 | Desc: Bezier interpolation
---@field INTERP_BOUNCE number     --Value: 10 | Ver: 10.0 | Desc: Bounce interpolation
---@field INTERP_CYCLE number      --Value: 5 | Ver: < 9.5 | Desc: Cycle interpolation
---@field INTERP_EASE number       --Value: 2 | Ver: < 9.5 | Desc: Ease in/out interpolation
---@field INTERP_EASE_IN number    --Value: 7 | Ver: < 9.5 | Desc: Ease in interpolation
---@field INTERP_EASE_OUT number   --Value: 8 | Ver: < 9.5 | Desc: Ease out interpolation
---@field INTERP_ELASTIC number    --Value: 11 | Ver: 10.0 | Desc: Elastic interpolation
---@field INTERP_LINEAR number     --Value: 0 | Ver: < 9.5 | Desc: Linear interpolation
---@field INTERP_NOISY number      --Value: 4 | Ver: < 9.5 | Desc: Noisy interpolation
---@field INTERP_POSE number       --Value: 6 | Ver: < 9.5 | Desc: Action reference interpolation
---@field INTERP_SMOOTH number     --Value: 1 | Ver: < 9.5 | Desc: Smooth interpolation
---@field INTERP_STEP number       --Value: 3 | Ver: < 9.5 | Desc: Step interpolation
---@field KEY_SELECTED number --Value: 1 | Ver: < 9.5
---@field LDQ_ANTIALIASING number      --Value: 256 | Ver: 9.5 | Desc: General antialiasing
---@field LDQ_BRUSHES number           --Value: 512 | Ver: 9.5 | Desc: Line brushes
---@field LDQ_FILLS number             --Value: 4 | Ver: 9.5 | Desc: Vector fills
---@field LDQ_GRAY_UNSELECTED number   --Value: 4096 | Ver: 9.5 | Desc: Gray out unselected layers
---@field LDQ_IMAGES number            --Value: 16 | Ver: 9.5 | Desc: Images
---@field LDQ_IMAGES_HQ number         --Value: 32 | Ver: 9.5 | Desc: High quality images
---@field LDQ_LABELS number            --Value: 2048 | Ver: 9.5 | Desc: Bone and layer labels
---@field LDQ_MASKED_LAYERS number     --Value: 32768 | Ver: 14.2 | Desc: Masked layers override
---@field LDQ_MASKING number           --Value: 64 | Ver: 9.5 | Desc: Masking effects
---@field LDQ_OUTLINES number          --Value: 8 | Ver: 9.5 | Desc: Vector outlines
---@field LDQ_SHAPE_EFFECTS number     --Value: 1024 | Ver: 9.5 | Desc: Fill and line effects
---@field LDQ_TRANSPARENCY number      --Value: 128 | Ver: 9.5 | Desc: Transparency
---@field LDQ_WIREFRAME number         --Value: 2 | Ver: 9.5 | Desc: Construction curves and bones
---@field LT_3D number       --Value: 8 | Ver: < 9.5 | Desc: 3D layer
---@field LT_AUDIO number    --Value: 9 | Ver: < 9.5 | Desc: Audio layer
---@field LT_BONE number     --Value: 4 | Ver: < 9.5 | Desc: Bone layer
---@field LT_GROUP number    --Value: 3 | Ver: < 9.5 | Desc: Group layer
---@field LT_IMAGE number    --Value: 2 | Ver: < 9.5 | Desc: Image layer
---@field LT_NOTE number     --Value: 7 | Ver: < 9.5 | Desc: Note layer
---@field LT_PARTICLE number --Value: 6 | Ver: < 9.5 | Desc: Particle layer
---@field LT_PATCH number    --Value: 10 | Ver: < 9.5 | Desc: Patch layer
---@field LT_SWITCH number   --Value: 5 | Ver: < 9.5 | Desc: Switch layer
---@field LT_TEXT number     --Value: 11 | Ver: < 9.5 | Desc: Text layer
---@field LT_UNKNOWN number  --Value: 0 | Ver: < 9.5 | Desc: Unknown layer
---@field LT_VECTOR number   --Value: 1 | Ver: < 9.5 | Desc: Vector layer
---@field LAYERWND_SEARCHCONTEXT_ALL number                  --Value: 0 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_COMMENTSCONTAIN number      --Value: 9 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_COMPCONTAINS number         --Value: 3 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_GROUPNAMECONTAINS number    --Value: 2 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_KINDCONTAINS number         --Value: 4 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_LABELIS number              --Value: 5 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_NAMECONTAINS number         --Value: 1 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_TAGSCONTAIN number          --Value: 8 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_TIMELINEVISIBILITYIS number --Value: 7 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_VISIBILITYIS number         --Value: 6 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_LABEL_BLUE number       --Value: 5 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_LABEL_CADETBLUE number  --Value: 10 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_LABEL_CORAL number      --Value: 11 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_LABEL_GRAY number       --Value: 0 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_LABEL_GREEN number      --Value: 4 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_LABEL_ORANGE number     --Value: 2 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_LABEL_PINK number       --Value: 8 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_LABEL_PURPLE number     --Value: 6 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_LABEL_RED number        --Value: 1 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_LABEL_TAN number        --Value: 7 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_LABEL_TURQUOISE number  --Value: 9 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_LABEL_YELLOW number     --Value: 3 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_VIS_ISNOTVISIBLE number         --Value: 1 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_VIS_ISORISNOTVISIBILE number    --Value: 2 | Ver: 12.2
---@field LAYERWND_SEARCHCONTEXT_VIS_ISVISIBLE number            --Value: 0 | Ver: 12.2
---@field COMBO_ADD number      --Value: 1 | Ver: 14.0
---@field COMBO_INTERSECT number --Value: 3 | Ver: 14.0
---@field COMBO_NORMAL number   --Value: 0 | Ver: 14.0
---@field COMBO_SUBTRACT number --Value: 2 | Ver: 14.0
---@field MM_ADD_MASK number             --Value: 2 | Ver: < 9.5 | Desc: Add to current mask
---@field MM_ADD_MASK_INVIS number       --Value: 4 | Ver: < 9.5 | Desc: Add to mask but keep invisible
---@field MM_CLEAR_ADD_MASK number       --Value: 6 | Ver: < 9.5 | Desc: Clear mask then add layer
---@field MM_CLEAR_ADD_MASK_INVIS number --Value: 7 | Ver: < 9.5
---@field MM_MASKED number               --Value: 0 | Ver: < 9.5 | Desc: Normal masking
---@field MM_NOTMASKED number            --Value: 1 | Ver: < 9.5 | Desc: No masking
---@field MM_SUB_MASK number             --Value: 3 | Ver: < 9.5 | Desc: Subtract from mask
---@field MM_SUB_MASK_INVIS number       --Value: 5 | Ver: < 9.5 | Desc: Subtract but keep invisible
---@field MING_VIEW_CAMERA number    --Value: 2 | Ver: < 9.5
---@field MING_VIEW_DIRECTOR number --Value: 1 | Ver: < 9.5
---@field MING_VIEW_PANEL number    --Value: 3 | Ver: < 9.5
---@field RS_FILL_BACKGROUND number             --Value: 2 | Ver: < 9.5
---@field RS_FILL_BACKGROUND_SHOWTHROUGH number --Value: 3 | Ver: < 9.5
---@field RS_FILL_CHALK number                  --Value: 7 | Ver: < 9.5
---@field RS_FILL_CRAYON number                 --Value: 4 | Ver: < 9.5
---@field RS_FILL_HATCHED number                --Value: 5 | Ver: < 9.5
---@field RS_FILL_NONE number                   --Value: 1 | Ver: < 9.5
---@field RS_FILL_NORMAL number                 --Value: 0 | Ver: < 9.5
---@field RS_FILL_PEN number                    --Value: 6 | Ver: < 9.5
---@field RS_LAYER_CUTOUT number                --Value: 2 | Ver: < 9.5
---@field RS_LAYER_HEAVY_OUTLINE number         --Value: 1 | Ver: < 9.5
---@field RS_LAYER_NORMAL number                --Value: 0 | Ver: < 9.5
---@field RS_LINE_BLACK number                  --Value: 2 | Ver: < 9.5
---@field RS_LINE_CHALK number                  --Value: 6 | Ver: < 9.5
---@field RS_LINE_CRAYON number                 --Value: 4 | Ver: < 9.5
---@field RS_LINE_NONE number                   --Value: 1 | Ver: < 9.5
---@field RS_LINE_NORMAL number                 --Value: 0 | Ver: < 9.5
---@field RS_LINE_PEN number                    --Value: 5 | Ver: < 9.5
---@field RS_LINE_SKETCHY number                --Value: 3 | Ver: < 9.5
---@field RS_POSEBM_RELATIVETOCURRENT number    --Value: 2 | Ver: < 9.5
---@field RS_POSEBM_RELATIVETODEFAULT number    --Value: 1 | Ver: < 9.5
---@field RS_POSEBM_STANDARD number             --Value: 0 | Ver: < 9.5
---@field SHADING_EXTRUDED_EDGE number --Value: 4 | Ver: 11.0
---@field SHADING_HATCHED number       --Value: 2 | Ver: < 9.5
---@field SHADING_NONE number          --Value: 0 | Ver: < 9.5
---@field SHADING_TOON number          --Value: 1 | Ver: < 9.5
---@field disabledCursor LM_Cursor --Value: LM.GUI.Cursor("ScriptResources/disabled_cursor", 8, 8)
---@field moveCursor LM_Cursor --Value: LM.GUI.Cursor("ScriptResources/move_cursor", 1, 1)
---@field scaleCursor LM_Cursor --Value: LM.GUI.Cursor("ScriptResources/scale_cursor", 1, 1)
---@field rotateCursor LM_Cursor --Value: LM.GUI.Cursor("ScriptResources/rotate_cursor", 1, 1)
---@field crosshairCursor LM_Cursor --Value: LM.GUI.Cursor("ScriptResources/crosshair_cursor", 7, 7)
---MOHO is the module that is a direct interface into Moho itself. The objects and functions in the MOHO module allow scripts to create and manipulate layers, vector artwork, bone systems, and more. Notice that the LM Utilities file adds a number of methods to the MOHO module. You can find them listed here: LM Utilities.
MOHO = {}
    --Returns an integer with only the p-th bit set.
    ---@param p number 
    ---@return number 
    function MOHO.bit(p) 
    end
    --Checks whether the p-th bit is 1 in integer x.
    -- Typical call: if hasbit(x, bit(3)) then ...
    ---@param x number 
    ---@param p number 
    ---@return number 
    function MOHO.hasbit(x, p)
    end 
    --Returns a new integer with the p-th bit=1 in x.
    ---@param x number 
    ---@param p number 
    ---@return number 
    function MOHO.setbit(x, p) 
    end
    --Returns a new integer with the p-th bit=0 in x.
    ---@param x number 
    ---@param p number 
    ---@return number 
    function MOHO.clearbit(x, p) 
    end
    ---@param moho ScriptInterface
    function MOHO.RunUpdateTable(moho) 
    end
    function MOHO.BM_COLORRandomRange(min, max)
    end
    function MOHO.MultiplyColor(color, amount)
    end
    ---@param moho ScriptInterface
    ---@param layer MohoLayer
    ---@param frame number
    function MOHO.LayerBounds(moho, layer, frame) 
    end
    -- returns a table containing a list of all the selected points in the specified mesh
    function MOHO.SelectedPointList(mesh)
    end 
    function MOHO.DeleteSelectedPoints(mesh) 
    end
    function MOHO.BuildGroupMenu(menu, mesh, baseMsg, dummyMsg)
    end
    function MOHO.BuildBoneMenu(menu, skel, baseMsg, dummyMsg)
    end
    function MOHO.BuildBoneChoiceMenu(menu, skel, baseMsg, exclude)
    end
    function MOHO.WeldPoint(moho, layer, mesh, weldCurveID, weldPointID, view, layerMatrix)
    end
    function MOHO.WeldFullCurve(moho, layer, mesh, weldCurveID, view)
    end
    -- This function Reduces points in a curve while maintaining shape
    function MOHO.ReduceCurve(moho, mesh, curveID)
    end 
    function MOHO.AngleBetween(CenterVec, VecA, VecB)
    end
    function MOHO.DrawToolSizeCircle(moho, view, centerVec, radius, fill)
    end
    function MOHO.SplitSelectedSegments(mesh, splitCount, frame)
    end
    -- converts a user value from 0..100 to a curve simplification epsilon value
    function MOHO.SimplifyEpsilon(userValue)
    end 
    MOHO.InterpSetting = {}
        ---instances of InterpSetting class can be created by this method locally
        ---@return InterpSetting
        function MOHO.InterpSetting:new_local()
        end
        ---instances of InterpSetting class can be created by this method globally
        ---@return InterpSetting
        function MOHO.InterpSetting:new()
        end
    MOHO.MohoLayer = {}
        ---instances of MohoLayer class can be created by this method locally
        ---@return MohoLayer
        function MOHO.MohoLayer:new_local()
        end
        ---instances of MohoLayer class can be created by this method globally
        ---@return MohoLayer  
        function MOHO.MohoLayer:new()
        end
    MOHO.MohoLayerRef = {}
        ---instances of MohoLayerRef class can be created by this method locally
        ---@return MohoLayerRef
        function MOHO.MohoLayerRef:new_local()
        end
        ---instances of MohoLayerRef class can be created by this method globally
        ---@return MohoLayerRef 
        function MOHO.MohoLayerRef:new()
        end
    MOHO.MohoLayerRefSyncOptions = {}
        ---instances of MohoLayerRefSyncOptions class can be created by this method locally
        ---@return MohoLayerRefSyncOptions
        function MOHO.MohoLayerRefSyncOptions:new_local()
        end
        ---instances of MohoLayerRefSyncOptions class can be created by this method globally
        ---@return MohoLayerRefSyncOptions 
        function MOHO.MohoLayerRefSyncOptions:new()
        end
    MOHO.MohoLayerChannel = {}
        ---instances of MohoLayerChannel class can be created by this method locally
        ---@return MohoLayerChannel
        function MOHO.MohoLayerChannel:new_local()
        end
        ---instances of MohoLayerChannel class can be created by this method globally
        ---@return MohoLayerChannel 
        function MOHO.MohoLayerChannel:new()
        end 
    ---@class AnimChannel  
    ---The AnimChannel class is the base class for all animation channels in Moho. You never use this class directly, but it provides various functions that are useful for working with any kind of animation channel.
    AnimChannel = {}
        ---Returns action channel by index
        --- | Ver: 12.0
        ---@param id number Ver: 12.0
        ---@return AnimChannel
        function AnimChannel:Action(id)
        end
        ---Returns the action channel.
        --- | Ver: 12.0
        ---@param name string Ver: 12.0
        ---@return AnimChannel|nil --Returns nil if the channel does not participate in action with this name.
        function AnimChannel:ActionByName(name)
        end
        ---Ver: 12.0
        ---@param id number Ver: 12.0
        ---@return string
        function AnimChannel:ActionName(id)
        end
        ---Creates action channel if not exist.
        ---Does not create action entry for channel's layer.
        ---If layer has no action with given name, the new created action channel would be invisible for user until adding action to layer.
        ---However, this operation turns on something like action editing mode, so don't forget to turn it off by calling this method with another action argument (for example, main timeline as "")
        --- | Ver: 12.0
        ---@param name string Ver: 12.0
        ---@return nil
        function AnimChannel:ActivateAction(name)
        end
        ---Adds a new keyframe at the given frame.
        --- | Ver: < 9.5
        ---@param when number Ver: < 9.5
        ---@return nil
        function AnimChannel:AddKey(when)
        end
        ---Returns the type of animation channel you're working with. See the channel types.
        --- | Ver: < 9.5
        ---@return number --the type of animation channel
        function AnimChannel:ChannelType()
        end
        ---Clears a channel, removing all keyframes.
        --- | Ver: < 9.5
        ---@param zeroFrame number Ver: < 9.5
        ---@return nil
        function AnimChannel:Clear(zeroFrame)
        end
        ---Ver: < 9.5
        ---@param frame number Ver: < 9.5
        ---@return nil
        function AnimChannel:ClearAfter(frame)
        end
        ---Ver: < 9.5
        ---@param frame number Ver: < 9.5
        ---@return nil
        function AnimChannel:ClearBefore(frame)
        end
        ---Ver: 12.0
        ---@return number
        function AnimChannel:CountActions()
        end
        ---Returns the number of keyframes in the channel.
        --- | Ver: < 9.5
        ---@return number --the number of keyframes in the channel
        function AnimChannel:CountKeys()
        end
        ---Ver: 12.0
        ---@return string
        function AnimChannel:CurrentAction()
        end
        ---Deletes a keyframe at the specified frame.
        --- | Ver: < 9.5
        ---@param when number Ver: < 9.5
        ---@return nil
        function AnimChannel:DeleteKey(when)
        end
        ---Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@return nil
        function AnimChannel:DeleteKeyByID(id)
        end
        ---Returns the duration of the animation channel (basically, the time of the last keyframe).
        --- | Ver: < 9.5
        ---@return number --the duration of the animation channel
        function AnimChannel:Duration()
        end
        ---Returns the closest keyframe to the left from the given frame. If the given frame has a key, then that keyframe is returned.
        --- | Ver: < 9.5
        ---@param when number Ver: < 9.5
        ---@return number --the closest keyframe id
        function AnimChannel:GetClosestKeyID(when)
        end
        ---This method accepts multiple input formats:
        --- AnimChannel:GetKeyInterp(when, interp) | AnimChannel:GetKeyInterp(when, interpMode, val1, val2)
        --- | Ver: < 9.5
        ---@param arg1 number Ver: < 9.5
        ---@param arg2 InterpSetting | number Ver: < 9.5
        ---@param arg3 number | nil Ver: < 9.5
        ---@param arg4 number | nil Ver: < 9.5
        ---@return nil
        function AnimChannel:GetKeyInterp(arg1, arg2, arg3, arg4)
        end
        ---Ver: < 9.5
        ---@param when number Ver: < 9.5
        ---@return number
        function AnimChannel:GetKeyInterpMode(when)
        end
        ---This method accepts multiple input formats:
        --- AnimChannel:GetKeyInterpByID(id, interp) | AnimChannel:GetKeyInterpByID(id, interpMode, val1, val2)
        --- | Ver: < 9.5
        ---@param arg1 number Ver: < 9.5
        ---@param arg2 InterpSetting | number Ver: < 9.5
        ---@param arg3 number | nil Ver: < 9.5
        ---@param arg4 number | nil Ver: < 9.5
        ---@return nil
        function AnimChannel:GetKeyInterpByID(arg1, arg2, arg3, arg4)
        end
        ---Ver: < 9.5
        ---@param id number The key id (starting from 0) | Ver: < 9.5
        ---@return number --Interpolation mode - see list of modes
        function AnimChannel:GetKeyInterpModeByID(id)
        end
        ---Returns the frame number for the key with the given id
        ---Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@return number
        function AnimChannel:GetKeyWhen(id)
        end
        ---Tests whether the channel has a keyframe at a given frame.
        --- | Ver: < 9.5
        ---@param when number Ver: < 9.5
        ---@return boolean --true if a keyframe exists, otherwise false
        function AnimChannel:HasKey(when)
        end
        ---Ver: 12.0
        ---@param name string Ver: 12.0
        ---@param when number Ver: 12.0
        ---@return nil
        function AnimChannel:InsertAction(name, when)
        end
        ---Ver: 12.0
        ---@param name string Ver: 12.0
        ---@param when number Ver: 12.0
        ---@return nil
        function AnimChannel:InsertActionByReference(name, when)
        end
        ---Ver: < 9.5
        ---@param when number Ver: < 9.5
        ---@return boolean
        function AnimChannel:IsKeySelected(when)
        end
        ---Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@return boolean
        function AnimChannel:IsKeySelectedByID(id)
        end
        ---Ver: 12.0
        ---@return boolean
        function AnimChannel:IsMuted()
        end
        ---make sure the keyframes are all in order according to time, and remove any that overlap
        --- | Ver: < 9.5
        ---@param deleteDuplicates boolean Ver: < 9.5
        ---@return nil
        function AnimChannel:MakeKeyframesConsistent(deleteDuplicates)
        end
        ---Ver: 12.0
        ---@param b boolean Ver: 12.0
        ---@return nil
        function AnimChannel:Mute(b)
        end
        ---Sets the value at a given frame to be the same as the value at frame 0.
        --- | Ver: < 9.5
        ---@param when number Frame number | Ver: < 9.5
        ---@return nil
        function AnimChannel:Reset(when)
        end
        ---This method accepts multiple input formats: 
        ---AnimChannel:SetKeyInterp(when, interpMode, val1, val2) | AnimChannel:SetKeyInterp(when, interp)
        ---Sets the interpolation mode at a specified frame. See also the InterpSetting class.
        --- | Ver: < 9.5
        ---@param arg1 number Frame number | Ver: < 9.5
        ---@param arg2 number | InterpSetting Interpolation mode | Ver: < 9.5
        ---@param arg3 number | nil Supplemental value 1 | Ver: < 9.5
        ---@param arg4 number | nil Supplemental value 2 | Ver: < 9.5
        ---@return nil
        function AnimChannel:SetKeyInterp(arg1, arg2, arg3, arg4)
        end
        ---This method accepts multiple input formats:
        ---AnimChannel:SetKeyInterpByID(id, interpMode, val1, val2) | AnimChannel:SetKeyInterpByID(id, interp)
        ---Sets the interpolation mode for a specified keyframe. See the available interpolation mode constants.
        --- | Ver: < 9.5
        ---@param arg1 number Keyframe id | Ver: < 9.5
        ---@param arg2 number | InterpSetting Interpolation mode | Ver: < 9.5
        ---@param arg3 number | nil Supplemental value 1 | Ver: < 9.5
        ---@param arg4 number | nil Supplemental value 2 | Ver: < 9.5
        ---@return nil
        function AnimChannel:SetKeyInterpByID(arg1, arg2, arg3, arg4)
        end
        ---Ver: < 9.5
        ---@param when number Ver: < 9.5
        ---@param sel boolean Ver: < 9.5
        ---@return nil
        function AnimChannel:SetKeySelected(when, sel)
        end
        ---Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@param sel boolean Ver: < 9.5
        ---@return nil
        function AnimChannel:SetKeySelectedByID(id, sel)
        end
        ---Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@param when number Ver: < 9.5
        ---@return nil
        function AnimChannel:SetKeyWhen(id, when)
        end
        ---allow keyframes to be set before/after each other, or even on top of each other
        --- | Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@param when number Ver: < 9.5
        ---@return nil
        function AnimChannel:SetKeyWhenNoRestriction(id, when)
        end
        ---Stores the current value as a keyframe. Each type of channel has a member variable called "value". If you change the value variable then call this function, you will add a new keyframe at the current time.
        --- | Ver: < 9.5
        ---@return nil
        function AnimChannel:StoreValue()
        end
        ---Ver: < 9.5
        ---@param id1 number Ver: < 9.5
        ---@param id2 number Ver: < 9.5
        ---@return nil
        function AnimChannel:SwapKeys(id1, id2)
        end
        ---@class AnimBool  
        ---@field value boolean --The current value of the channel. | Ver: < 9.5
        ---The AnimBool class represents an animated sequence of on/off values.
        AnimChannel.AnimBool = {}
            ---Return the value of this channel at a given frame.
            --- | Ver: < 9.5
            ---@param when number Frame number | Ver: < 9.5
            ---@return boolean --value at the given frame
            function AnimChannel.AnimBool:GetValue(when)
            end
            ---Ver: < 9.5
            ---@param id number Ver: < 9.5
            ---@return boolean
            function AnimChannel.AnimBool:GetValueByID(id)
            end
            ---Set the value at a given frame.
            --- | Ver: < 9.5
            ---@param when number Frame number | Ver: < 9.5
            ---@param val boolean The value to set | Ver: < 9.5
            ---@return nil
            function AnimChannel.AnimBool:SetValue(when, val)
            end
            ---Ver: < 9.5
            ---@param id number Ver: < 9.5
            ---@param val boolean Ver: < 9.5
            ---@return nil
            function AnimChannel.AnimBool:SetValueByID(id, val)
            end
        ---@class AnimColor 
        ---@field value rgb_color --The current value of the channel. | Ver: < 9.5 
        ---The AnimColor class represents an animated sequence of color values.
        AnimChannel.AnimColor = {}
            ---Return the value of this channel at a given frame.  
            --- | Ver: < 9.5  
            ---@param when number Frame number | Ver: < 9.5  
            ---@return ColorVector --value at the given frame  
            function AnimChannel.AnimColor:GetValue(when)  
            end  
            ---Ver: < 9.5  
            ---@param id number Ver: < 9.5  
            ---@return ColorVector  
            function AnimChannel.AnimColor:GetValueByID(id)  
            end  
            ---This method accepts multiple input formats:  
            ---AnimColor:SetValue(when, val: ColorVector) | AnimColor:SetValue(when, val: rgb_color)  
            ---Set the value at a given frame.  
            --- | Ver: < 9.5  
            ---@param when number Frame number | Ver: < 9.5  
            ---@param val ColorVector | rgb_color The value to set | Ver: < 9.5  
            ---@return nil  
            function AnimChannel.AnimColor:SetValue(when, val)  
            end  
            ---This method accepts multiple input formats:  
            ---AnimColor:SetValueByID(id, val: ColorVector) | AnimColor:SetValueByID(id, val: rgb_color)  
            --- | Ver: < 9.5  
            ---@param id number Ver: < 9.5  
            ---@param val ColorVector | rgb_color Ver: < 9.5  
            ---@return nil  
            function AnimChannel.AnimColor:SetValueByID(id, val)  
            end  
        ---@class AnimString  
        ---@field value String --The current value of the channel. | Ver: < 9.5
        ---The AnimString class represents an animated sequence of text values.
        AnimChannel.AnimString = {}
            ---Return the value of this channel at a given frame.
            --- | Ver: < 9.5
            ---@param when number Frame number | Ver: < 9.5
            ---@return string --value at the given frame
            function AnimChannel.AnimString:GetValue(when)
            end
            ---Ver: < 9.5
            ---@param id number Ver: < 9.5
            ---@return string
            function AnimChannel.AnimString:GetValueByID(id)
            end
            ---Set the value at a given frame.
            --- | Ver: < 9.5
            ---@param when number Frame number | Ver: < 9.5
            ---@param value string The value to set | Ver: < 9.5
            ---@return nil
            function AnimChannel.AnimString:SetValue(when, value)
            end
            ---Ver: < 9.5
            ---@param id number Ver: < 9.5
            ---@param value string Ver: < 9.5
            ---@return nil
            function AnimChannel.AnimString:SetValueByID(id, value)
            end
        ---@class AnimVal  
        ---@field value number --The current value of the channel. | Ver: < 9.5
        ---The AnimVal class represents an animated sequence of numerical values.
        AnimChannel.AnimVal = {}
            ---Return the value of this channel at a given frame.
            --- | Ver: < 9.5
            ---@param when number Frame number | Ver: < 9.5
            ---@return number --value at the given frame
            function AnimChannel.AnimVal:GetValue(when)
            end
            ---Ver: < 9.5
            ---@param id number Ver: < 9.5
            ---@return number
            function AnimChannel.AnimVal:GetValueByID(id)
            end
            ---Set the value at a given frame.
            --- | Ver: < 9.5
            ---@param when number Frame number | Ver: < 9.5
            ---@param val number The value to set | Ver: < 9.5
            ---@return nil
            function AnimChannel.AnimVal:SetValue(when, val)
            end
            ---Ver: < 9.5
            ---@param id number Ver: < 9.5
            ---@param val number Ver: < 9.5
            ---@return nil
            function AnimChannel.AnimVal:SetValueByID(id, val)
            end
        ---@class AnimVec2  
        ---@field value Vector2 --The current value of the channel. | Ver: < 9.5
        ---The AnimVec2 class represents an animated sequence of 2D vector values.
        AnimChannel.AnimVec2 = {}
            ---Return the value of this channel at a given frame.
            --- | Ver: < 9.5
            ---@param when number Frame number | Ver: < 9.5
            ---@return Vector2 --value at the given frame
            function AnimChannel.AnimVec2:GetValue(when)
            end
            ---Ver: < 9.5
            ---@param id number Ver: < 9.5
            ---@return Vector2
            function AnimChannel.AnimVec2:GetValueByID(id)
            end
            ---Set the value at a given frame.
            --- | Ver: < 9.5
            ---@param when number Frame number | Ver: < 9.5
            ---@param val Vector2 The value to set | Ver: < 9.5
            ---@return nil
            function AnimChannel.AnimVec2:SetValue(when, val)
            end
            ---Ver: < 9.5
            ---@param id number Ver: < 9.5
            ---@param val Vector2 Ver: < 9.5
            ---@return nil
            function AnimChannel.AnimVec2:SetValueByID(id, val)
            end
            --- | Ver: 12.0
            ---@return boolean
            function AnimChannel.AnimVec2:AreDimensionsSplit()
            end
            --- | Ver: 12.0
            ---@param dim number 0 for the X axis, 1 for the Y axis | Ver: 12.0
            ---@return AnimVal
            function AnimChannel.AnimVec2:DimensionChannel(dim)
            end
            --- | Ver: 12.0
            ---@param b boolean Ver: 12.0
            ---@return nil
            function AnimChannel.AnimVec2:SplitDimensions(b)
            end
            --- | Ver: 12.0
            ---@return nil
            function AnimChannel.AnimVec2:SyncSplitDimensionsBack()
            end
        ---@class AnimVec3  
        ---@field value Vector3 --The current value of the channel. | Ver: < 9.5
        ---The AnimVec3 class represents an animated sequence of 3D vector values.
        AnimChannel.AnimVec3 = {}
            ---Ver: 12.0
            ---@return boolean
            function AnimChannel.AnimVec3:AreDimensionsSplit()
            end
            ---Ver: 12.0
            ---@param dim number 0 for the X axis, 1 for the Y axis, 2 for the Z axis | Ver: 12.0
            ---@return AnimVal
            function AnimChannel.AnimVec3:DimensionChannel(dim)
            end
            ---Return the value of this channel at a given frame.
            --- | Ver: < 9.5
            ---@param when number Frame number | Ver: < 9.5
            ---@return Vector3 --value at the given frame
            function AnimChannel.AnimVec3:GetValue(when)
            end
            ---Ver: < 9.5
            ---@param id number Ver: < 9.5
            ---@return Vector3
            function AnimChannel.AnimVec3:GetValueByID(id)
            end
            ---Set the value at a given frame.
            --- | Ver: < 9.5
            ---@param when number Frame number | Ver: < 9.5
            ---@param val Vector3 The value to set | Ver: < 9.5
            ---@return nil
            function AnimChannel.AnimVec3:SetValue(when, val)
            end
            ---Ver: < 9.5
            ---@param id number Ver: < 9.5
            ---@param val Vector3 Ver: < 9.5
            ---@return nil
            function AnimChannel.AnimVec3:SetValueByID(id, val)
            end
            ---Ver: 12.0
            ---@param b boolean Ver: 12.0
            ---@return nil
            function AnimChannel.AnimVec3:SplitDimensions(b)
            end
            ---Ver: 12.0
            ---@return nil
            function AnimChannel.AnimVec3:SyncSplitDimensionsBack()
            end
    ---@class MohoLayer  
    ---@field CameraBoundsCol rgb_color --Added in version 9.5 | Ver: 9.5
    ---@field CameraShadeCol rgb_color --Added in version 9.5 | Ver: 9.5
    ---@field CameraTargetCol rgb_color --Added in version 9.5 | Ver: 9.5
    ---@field GuideCol rgb_color --Added in version 9.5 | Ver: 9.5
    ---@field fAlpha AnimVal --the layer's alpha animation channel | Ver: < 9.5
    ---@field fAmbientOcclusionRadius AnimVal --Added in version 12 | Ver: 12.0
    ---@field fBlur AnimVal --the layer's blur animation channel | Ver: < 9.5
    ---@field fFlipH AnimBool --the layer's horizontal flip animation channel | Ver: < 9.5
    ---@field fFlipV AnimBool --the layer's vertical flip animation channel | Ver: < 9.5
    ---@field fFollowing AnimVal --Ver: < 9.5
    ---@field fLayerColor AnimColor --Added in version 10 | Ver: 10.0
    ---@field fLayerColorOn AnimBool --Added in version 10 | Ver: 10.0
    ---@field fLayerOutline AnimBool --Ver: < 9.5
    ---@field fLayerShading AnimBool --the layer's shading on/off animation channel | Ver: < 9.5
    ---@field fLayerShadow AnimBool --the layer's shadow on/off animation channel | Ver: < 9.5
    ---@field fMotionBlur AnimBool --the layer's motion blur on/off animation channel | Ver: < 9.5
    ---@field fMotionBlurAlphaEnd AnimVal --Ver: < 9.5
    ---@field fMotionBlurAlphaStart AnimVal --Ver: < 9.5
    ---@field fMotionBlurExtendedFrames AnimVal --Added in version 12 | Ver: 12.0
    ---@field fMotionBlurFramePercentage AnimVal --Added in version 12 | Ver: 12.0
    ---@field fMotionBlurFrames AnimVal --Ver: < 9.5
    ---@field fMotionBlurRadius AnimVal --Ver: < 9.5
    ---@field fMotionBlurSkip AnimVal --Ver: < 9.5
    ---@field fMotionBlurSubframes AnimBool --Added in version 12 | Ver: 12.0
    ---@field fNoise AnimVal --Added in version 12 | Ver: 12.0
    ---@field fOutlineColor AnimColor --Ver: < 9.5
    ---@field fOutlineWidth AnimVal --Ver: < 9.5
    ---@field fPerspectiveBlur AnimVal --Ver: < 9.5
    ---@field fPerspectiveColor AnimColor --Ver: < 9.5
    ---@field fPerspectiveScale AnimVal --Ver: < 9.5
    ---@field fPerspectiveShadow AnimBool --Ver: < 9.5
    ---@field fPerspectiveShear AnimVal --Ver: < 9.5
    ---@field fPhysicsNudge AnimVec2 --Ver: < 9.5
    ---@field fPixelation AnimVal --Added in version 12 | Ver: 12.0
    ---@field fRotationX AnimVal --the layer's X rotation animation channel | Ver: < 9.5
    ---@field fRotationY AnimVal --the layer's Y rotation animation channel | Ver: < 9.5
    ---@field fRotationZ AnimVal --the layer's Z rotation animation channel | Ver: < 9.5
    ---@field fScale AnimVec3 --the layer's scale animation channel | Ver: < 9.5
    ---@field fShadingAngle AnimVal --Ver: < 9.5
    ---@field fShadingBlur AnimVal --Ver: < 9.5
    ---@field fShadingColor AnimColor --Ver: < 9.5
    ---@field fShadingContraction AnimVal --Ver: < 9.5
    ---@field fShadingNoiseAmp AnimVal --Ver: < 9.5
    ---@field fShadingNoiseScale AnimVal --Ver: < 9.5
    ---@field fShadingOffset AnimVal --Ver: < 9.5
    ---@field fShadowAngle AnimVal --Ver: < 9.5
    ---@field fShadowBlur AnimVal --Ver: < 9.5
    ---@field fShadowColor AnimColor --Ver: < 9.5
    ---@field fShadowExpansion AnimVal --Ver: < 9.5
    ---@field fShadowNoiseAmp AnimVal --Ver: < 9.5
    ---@field fShadowNoiseScale AnimVal --Ver: < 9.5
    ---@field fShadowOffset AnimVal --Ver: < 9.5
    ---@field fShear AnimVec3 --the layer's shear animation channel | Ver: < 9.5
    ---@field fThreshold AnimVal --Added in version 12 | Ver: 12.0
    ---@field fTimelineMarkers AnimString --Added in version 10 | Ver: 10.0
    ---@field fTranslation AnimVec3 --the layer's translation animation channel | Ver: < 9.5
    ---@field fVisibility AnimBool --the layer's visibility animation channel | Ver: < 9.5
    ---@field GridCol rgb_color --Added in version 9.5 | Ver: 9.5
    ---The MohoLayer class is the base class for all Moho layer types. All actual layers in Moho are sub-classes of MohoLayer (vector layers, image layers, group layers, etc.), but the MohoLayer class provides some basic properties that they all have in common.  
    ---The most practical way to create a new layer is using the ScriptInterface:CreateNewLayer method.
    MohoLayer = {} 
        ---Ver: < 9.5
        ---@param name string Ver: < 9.5
        ---@return number
        function MohoLayer:ActionDuration(name)
        end
        ---Ver: < 9.5
        ---@param name string Ver: < 9.5
        ---@return number
        function MohoLayer:ActionID(name)
        end
        ---Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@return string
        function MohoLayer:ActionName(id)
        end
        ---Activates an action for editing. If the named action does not exist, it is first created.
        --- | Ver: < 9.5
        ---@param name string The name of the action to edit. Use nil to deactivate the current action (return to Mainline) | Ver: < 9.5
        ---@return nil
        function MohoLayer:ActivateAction(name)
        end
        ---Ver: < 9.5
        ---@param frame number Ver: < 9.5
        ---@param recursive boolean Ver: < 9.5
        ---@param animatedChannelsOnly boolean Ver: < 9.5
        ---@return nil
        function MohoLayer:AddKey(frame, recursive, animatedChannelsOnly)
        end
        ---Ver: 9.5
        ---@param boneID number Ver: 9.5
        ---@return nil
        function MohoLayer:AddToFlexiBoneSubset(boneID)
        end
        --- | Ver: < 9.5 | Ver: 10.1
        ---@param frame number Ver: < 9.5
        ---@param doc MohoDoc Ver: < 9.5
        ---@param useLayerBounds boolean|nil Default: false | Ver: 10.1
        ---@return nil
        function MohoLayer:AlignWithCamera(frame, doc, useLayerBounds)
        end
        ---The nearest ancestor which is a direct child of a switch layer.
        --- | Ver: < 9.5
        ---@return MohoLayer
        function MohoLayer:AncestorSwitchChild()
        end
        ---The nearest ancestor which is a switch layer.
        --- | Ver: < 9.5
        ---@return SwitchLayer
        function MohoLayer:AncestorSwitchLayer()
        end
        ---Ver: < 9.5
        ---@return number
        function MohoLayer:AnimDuration()
        end
        ---Ver: 12.0
        ---@return boolean
        function MohoLayer:AreChannelsConsolidated()
        end
        ---This method blends multiple actions into the layer. It supports two modes: relative blending and blend mode specific blending.
        ---Usage:
        --- MohoLayer:BlendActions(frame, relativeToDefault, numToBlend, names, percentages)
        --- MohoLayer:BlendActions(frame, blendMode, numToBlend, names, percentages, frames)
        --- | Ver: < 9.5
        ---@param frame number The frame at which blending occurs | Ver: < 9.5
        ---@param relativeToDefault boolean | nil Use relative blending to default action. Leave nil if using blendMode | Ver: < 9.5
        ---@param blendMode number | nil The blend mode index. Leave nil if using relativeToDefault | Ver: < 9.5
        ---@param numToBlend number The number of actions to blend | Ver: < 9.5
        ---@param names table<string> A table of String objects holding action names | Ver: < 9.5
        ---@param percentages table<number> A table of real values indicating blend ratios | Ver: < 9.5
        ---@param frames table<number> | nil A table of frame indices for actions. Only used when providing blendMode | Ver: < 9.5
        ---@return nil
        function MohoLayer:BlendActions(frame, relativeToDefault, blendMode, numToBlend, names, percentages, frames)
        end
        ---Get the layer's blending mode. See the blending mode constants.
        --- | Ver: < 9.5
        ---@return number -- the layer's blending mode
        function MohoLayer:BlendingMode()
        end
        ---Ver: < 9.5
        ---@param frame number Ver: < 9.5
        ---@return BBox
        function MohoLayer:Bounds(frame)
        end
        ---Ver: 11.0
        ---@return nil
        function MohoLayer:BreakLayerReference()
        end
        ---Ver: < 9.5
        ---@param id number Channel index, zero based | Ver: < 9.5
        ---@param subID number Subchannel index, zero based | Ver: < 9.5
        ---@param doc MohoDoc Ver: < 9.5
        ---@return AnimChannel
        function MohoLayer:Channel(id, subID, doc)
        end
        ---Ver: < 9.5
        ---@param recursive boolean Ver: < 9.5
        ---@param fromFrame number Ver: < 9.5
        ---@param before boolean Ver: < 9.5
        ---@return nil
        function MohoLayer:ClearAnimation(recursive, fromFrame, before)
        end
        ---Ver: 9.5
        ---@return nil
        function MohoLayer:ClearFlexiBoneSubset()
        end
        ---Ver: < 9.5
        ---@return nil
        function MohoLayer:ClearLayerKeyCount()
        end
        ---Ver: 9.5
        ---@return boolean
        function MohoLayer:ConstructionCurvesOn()
        end
        ---Ver: 9.5
        ---@return BoneLayer
        function MohoLayer:ControllingBoneLayer()
        end
        ---Ver: 9.5
        ---@return M_Skeleton
        function MohoLayer:ControllingSkeleton()
        end
        ---Creates keys at toFrame in each of layer's channels (including ones with no animation) with values taken from fromFrame.
        --- | Ver: < 9.5
        ---@param fromFrame number Ver: < 9.5
        ---@param toFrame number Ver: < 9.5
        ---@param recursive boolean|nil Default: false | Ver: < 9.5
        ---@return nil
        function MohoLayer:CopyFrame(fromFrame, toFrame, recursive)
        end
        ---Ver: 14.0
        ---@param fromLayer MohoLayer Ver: 14.0
        ---@return nil
        function MohoLayer:CopyTransform(fromLayer)
        end
        ---Ver: < 9.5
        ---@return number
        function MohoLayer:CountActions()
        end
        ---Ver: < 9.5
        ---@return number
        function MohoLayer:CountChannels()
        end
        ---Ver: < 9.5
        ---@return number
        function MohoLayer:CountLayerKeys()
        end
        ---Ver: < 9.5
        ---@return number
        function MohoLayer:CurFrame()
        end
        ---Returns the name of the current action being edited.
        --- | Ver: < 9.5
        ---@return string --the name of the current action being edited
        function MohoLayer:CurrentAction()
        end
        ---Ver: < 9.5
        ---@param name string Ver: < 9.5
        ---@return nil
        function MohoLayer:DeleteAction(name)
        end
        ---Ver: < 9.5
        ---@param frame number Ver: < 9.5
        ---@param recursive boolean Ver: < 9.5
        ---@return nil
        function MohoLayer:DeleteFrame(frame, recursive)
        end
        ---Ver: < 9.5
        ---@param recursive boolean Ver: < 9.5
        ---@param frame number Ver: < 9.5
        ---@return nil
        function MohoLayer:DeleteKeysAtFrame(recursive, frame)
        end
        ---Call this function before deleting a bone in a parent bone layer.
        --- | Ver: < 9.5
        ---@param boneID number The id of the bone you're about to delete | Ver: < 9.5
        ---@return nil
        function MohoLayer:DeleteParentBone(boneID)
        end
        ---Ver: 10.0
        ---@param docFrame number Ver: < 9.5
        ---@return number
        function MohoLayer:DocToLayerFrame(docFrame)
        end
        ---Ver: < 9.5
        ---@return boolean
        function MohoLayer:DoesFaceCamera()
        end
        ---Ver: < 9.5
        ---@return boolean
        function MohoLayer:DoesRotateToFollow()
        end
        ---Ver: < 9.5
        ---@return number
        function MohoLayer:FaceCameraMode()
        end
        ---Ver: 12.0
        ---@return nil
        function MohoLayer:FreeCachedImage()
        end
        ---Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@param channelInfo MohoLayerChannel Ver: < 9.5
        ---@return nil
        function MohoLayer:GetChannelInfo(id, channelInfo)
        end
        ---Ver: 12.0
        ---@return MohoLayer
        function MohoLayer:GetDistortionMeshLayer()
        end
        ---Ver: < 9.5
        ---@return M_Curve
        function MohoLayer:GetFollowingCurve()
        end
        ---Ver: < 9.5
        ---@return MohoLayer
        function MohoLayer:GetFollowingLayer()
        end
        ---Gets the layer's complete transformation at a given frame, including the effects of its parent layers.
        ---Full transform includes camera - to ignore the camera settings set the doc argument to nil layer:GetFullTransform (frame , matrix, nil)
        --- | Ver: < 9.5
        ---@param frame number Frame number | Ver: < 9.5
        ---@param matrix Matrix Transform matrix to fill in | Ver: < 9.5
        ---@param doc MohoDoc The document object | Ver: < 9.5
        ---@return nil
        function MohoLayer:GetFullTransform(frame, matrix, doc)
        end
        ---Ver: < 9.5
        ---@param keyID number Ver: < 9.5
        ---@return number
        function MohoLayer:GetLayerKeyWhen(keyID)
        end
        ---Ver: 11.0
        ---@param refInfo MohoLayerRef Ver: 11.0
        ---@return nil
        function MohoLayer:GetLayerRefInfo(refInfo)
        end
        ---Gets the layer's transformation at a given frame. The layer transform includes things like layer scale, rotation, and translation. For layers with no parent it also includes some information about camera transform, course the resulting matrix is the layer transformation premultiplied with the camera matrix ( MohoDoc : GetCameraMatrix )
        --- | Ver: < 9.5
        ---@param frame number Frame number | Ver: < 9.5
        ---@param matrix Matrix Transform matrix to fill in | Ver: < 9.5
        ---@param doc MohoDoc The document object | Ver: < 9.5
        ---@return nil
        function MohoLayer:GetLayerTransform(frame, matrix, doc)
        end
        ---Ver: < 9.5
        ---@param frame number Ver: < 9.5
        ---@param matrix Matrix Ver: < 9.5
        ---@param doc MohoDoc Ver: < 9.5
        ---@return nil
        function MohoLayer:GetParentBoneTransform(frame, matrix, doc)
        end
        ---Gets the parent layer's transformation at a given frame.
        --- | Ver: < 9.5
        ---@param frame number Frame number | Ver: < 9.5
        ---@param matrix Matrix Transform matrix to fill in | Ver: < 9.5
        ---@param doc MohoDoc The document object | Ver: < 9.5
        ---@return nil
        function MohoLayer:GetParentTransform(frame, matrix, doc)
        end
        ---Added in version 14.0 to replace MohoLayer:GetDistortionMeshLayer
        ---Ver: 14.0
        ---@return MohoLayer
        function MohoLayer:GetWarpLayer()
        end
        ---Ver: < 9.5
        ---@param name string Ver: < 9.5
        ---@return boolean
        function MohoLayer:HasAction(name)
        end
        ---Ver: < 9.5
        ---@return boolean
        function MohoLayer:HasAnimatedLayerEffects()
        end
        ---Ver: < 9.5
        ---@return boolean
        function MohoLayer:HasScaleCompensation()
        end
        ---Returns a file path to the layer's HSV modifier image.
        --- | Ver: < 9.5
        ---@param outAccess LM_SecureAccess|nil Default: nil | Ver: < 9.5
        ---@return string
        function MohoLayer:HsvImage(outAccess)
        end
        ---Inserts an action into the current active timeline.
        --- | Ver: < 9.5
        ---@param name string The name of the action to insert | Ver: < 9.5
        ---@param frame number Ver: < 9.5
        ---@param byReference boolean True if by reference, false if by copy | Ver: < 9.5
        ---@return nil
        function MohoLayer:InsertAction(name, frame, byReference)
        end
        ---True if any of layer's ancestors is selected. Always false for a layer with no parent.
        --- | Ver: 11.1
        ---@return boolean
        function MohoLayer:IsAncestorSelected()
        end
        ---Ver: < 9.5
        ---@return boolean
        function MohoLayer:IsAudioType()
        end
        ---true if a bone sub-type, otherwise false
        ---Test whether a layer is a bone layer, or sub-type of bone layer (switch).
        --- | Ver: < 9.5
        ---@return boolean
        function MohoLayer:IsBoneType()
        end
        ---	Getter method for the "Don't render this layer" checkbox in the layer settings
        ---true if the setting is on, false if it's off
        --- | Ver: < 9.5
        ---@return boolean
        function MohoLayer:IsEditOnly()
        end
        ---true if a group sub-type, otherwise false
        ---Test whether a layer is a group layer, or sub-type of group layer (bone, particle, or switch).
        --- | Ver: < 9.5
        ---@return boolean
        function MohoLayer:IsGroupType()
        end
        ---Ver: 13.5.2
        ---@return boolean
        function MohoLayer:IsIgnoredByLayerPicker()
        end
        ---Ver: < 9.5
        ---@return boolean
        function MohoLayer:IsImmuneToCamera()
        end
        ---Ver: < 9.5
        ---@return boolean
        function MohoLayer:IsImmuneToDof()
        end
        ---Ver: 12.2
        ---@param boneID number Ver: 12.2
        ---@return boolean
        function MohoLayer:IsIncludedInFlexiBoneSubset(boneID)
        end
        ---Ver: < 9.5
        ---@return boolean
        function MohoLayer:IsLocked()
        end
        ---Ver: < 9.5
        ---@param frame number Ver: < 9.5
        ---@return boolean
        function MohoLayer:IsPhysicsInEffect(frame)
        end
        ---Ver: 11.0
        ---@return boolean
        function MohoLayer:IsReferencedLayer()
        end
        ---Ver: 11.0
        ---@return boolean
        function MohoLayer:IsReferenceExternal()
        end
        ---Ver: 11.0
        ---@return boolean
        function MohoLayer:IsReferenceOutdated()
        end
        ---Getter method for the "Hide in editing view" checkbox in the layer settings
        ---Ver: < 9.5
        ---@return boolean --true if the setting is on, false if it's off
        function MohoLayer:IsRenderOnly()
        end
        ---Ver: 12.0
        ---@return boolean
        function MohoLayer:IsShownOnTimeline()
        end
        ---Ver: < 9.5
        ---@param name string Ver: < 9.5
        ---@return boolean
        function MohoLayer:IsSmartBoneAction(name)
        end
        ---visibility (non-animated value)
        ---Ver: < 9.5
        ---@return boolean --Non-animated value set with the Layer Window visibility column controls
        function MohoLayer:IsVisible()
        end
        ---Ver: 11.0
        ---@return number
        function MohoLayer:LabelColor()
        end
        ---The duration of the video, audio, or image sequence
        ---Ver: < 9.5
        ---@return number
        function MohoLayer:LayerDuration()
        end
        ---Get the id number of the layer's controlling parent bone.
        --- | Ver: < 9.5
        ---@return number --id number of bone, -1 for none, -2 for flexi-binding, or -3 for flexi-binding to bone subset
        function MohoLayer:LayerParentBone()
        end
        ---Ver: < 9.5
        ---@param outAccess LM_SecureAccess|nil Default: nil | Ver: < 9.5
        ---@return string
        function MohoLayer:LayerScript(outAccess)
        end
        ---Use this function to find out what type of layer you're dealing with
        ---Ver: < 9.5
        ---@return number --a layer type identifier Use this function to find out what type of layer you're dealing with. See the layer types constants.
        function MohoLayer:LayerType()
        end
        ---Ver: 10.0
        ---@param b boolean Ver: 10.0
        ---@return nil
        function MohoLayer:MarkFlexiBoneSubsetAsElbow(b)
        end
        ---Use this method to refresh the reference
        ---Ver: 11.0
        ---@return nil
        function MohoLayer:MarkReferenceOutdated()
        end
        ---Ver: < 9.5
        ---@return boolean
        function MohoLayer:MaskExpansion()
        end
        ---Get the layer's masking mode
        ---Ver: < 9.5
        ---@return number --the layer's masking mode Get the layer's masking mode. See the masking mode constants.
        function MohoLayer:MaskingMode()
        end
        ---Deprecated in favour of the ScriptData object -- consider this reserved for AS use.
        --- | Ver: < 9.5
        ---@return LM_Message
        function MohoLayer:Metadata()
        end
        ---Ver: < 9.5
        ---@return string --name of the layer Returns the name of the layer.
        function MohoLayer:Name()
        end
        ---Ver: < 9.5
        ---@return Vector2 --the origin point of the layer Get the origin point of the layer.
        function MohoLayer:Origin()
        end
        ---Get the layer's parent layer (if any).
        ---Ver: < 9.5
        ---@return GroupLayer --the layer's parent (nil if the layer is a top-level layer with no parent)
        function MohoLayer:Parent()
        end
        ---Ver: < 9.5
        ---@return MohoPhysicsOptions
        function MohoLayer:PhysicsOptions()
        end
        ---Ver: < 9.5
        ---@param frame number Ver: < 9.5
        ---@return GroupLayer
        function MohoLayer:PhysicsParent(frame)
        end
        ---Sum of the Quality Flags constants.
        ---Note: For the versions 9.5 - 13.5.2, the returned type is int32. Starting from 14.0 the returned value is uint32.
        ---Ver: 9.5 - 13.5.2 | Ver: 14.0
        ---@return number
        function MohoLayer:QualityFlags()
        end
        ---Ver: 9.5
        ---@param boneID number Ver: 9.5
        ---@return nil
        function MohoLayer:RemoveFromFlexiBoneSubset(boneID)
        end
        ---Ver: < 9.5
        ---@param oldName string Ver: < 9.5
        ---@param newName string Ver: < 9.5
        ---@return nil
        function MohoLayer:RenameAction(oldName, newName)
        end
        ---Ver: < 9.5
        ---@param moveName string Ver: < 9.5
        ---@param placeAfterName string Ver: < 9.5
        ---@return nil
        function MohoLayer:ReorderAction(moveName, placeAfterName)
        end
        ---Ver: < 9.5
        ---@param recursive boolean Ver: < 9.5
        ---@param animatedChannelsOnly boolean|nil Default: false | Ver: < 9.5
        ---@return nil
        function MohoLayer:ResetAnimation(recursive, animatedChannelsOnly)
        end
        ---Ver: < 9.5
        ---@return nil
        function MohoLayer:RunLayerScript()
        end
        ---The method applies the scale normalization factor that has been set by MohoLayer : SetScaleNormalization and returns that value.
        ---If no value has been set the default value of 1 is returned.
        --- | Ver: 11.0
        ---@return number --The method applies the scale normalization factor that has been set by MohoLayer : SetScaleNormalization and returns that value. If no value has been set the default value of 1 is returned.
        function MohoLayer:ScaleNormalization()
        end
        ---Added in version 11 - use this and not MetaData
        ---example use cases are below. Note that it is good practice to identify data items by the script that creates them. Consider the keys to be Globals and prefix them with a unique identifier MyScriptID -- e.g. for the hayasidist utility function the prefix might be HS_Utility_ and keys named such as HS_Utility_Int Value.
        --- local ScriptInfo = moho.layer:ScriptData()
        --- ScriptInfo:Set("MyScriptID_Int Value", 42)
        --- ScriptInfo:Set("MyScriptID_Bool Value", true)
        --- ScriptInfo:Set("MyScriptID_StringName", "This is a saved string.")
        --- local v = LM.Vector2:new_local()
        --- v:Set(5.6, 7.8)
        --- ScriptInfo:Set("MyScriptID_Saved Vector2", v)
        --- local v = LM.Vector3:new_local()
        --- v:Set(3.4, 5.6, 7.8)
        --- ScriptInfo:Set("MyScriptID_Saved Vector3", v)
        --- if (ScriptInfo:HasKey("MyScriptID_Saved Vector3")) then
        ---  local v = ScriptInfo:GetVector3("MyScriptID_Saved Vector3")
        ---  print(v.x, v.y, v.z)
        --- end
        --- print("Layer Script Data")
        --- for i = 0, ScriptInfo:CountKeys() - 1 do
        ---  local keyName = ScriptInfo:GetKeyName(i)
        ---  print(keyName, ScriptInfo:GetString(keyName))
        --- end
        --- | Ver: 11.0
        ---@return LM_Message
        function MohoLayer:ScriptData()
        end
        ---True if the layer itself is selected, no matter how many layers are selected or if this is the working (active) layer or not.
        --- | Ver: < 9.5
        ---@return boolean --True if the layer itself is selected, no matter how many layers are selected or if this is the working (active) layer or not.
        function MohoLayer:SecondarySelection()
        end
        ---Ver: < 9.5
        ---@param b boolean Ver: < 9.5
        ---@return nil
        function MohoLayer:SetAnimatedLayerEffects(b)
        end
        ---Set the layer's blending mode. See the blending mode constants.
        --- | Ver: < 9.5
        ---@param mode number The layer's blending mode | Ver: < 9.5
        ---@return nil
        function MohoLayer:SetBlendingMode(mode)
        end
        ---Ver: 12.0
        ---@param b boolean Ver: 12.0
        ---@return nil
        function MohoLayer:SetConsolidatedChannels(b)
        end
        ---Sets the layer's smart warp layer. In Moho 14.0, replaced by MohoLayer:SetWarpLayer
        ---Ver: 12.0
        ---@param layer MohoLayer Ver: 12.0
        ---@return nil
        function MohoLayer:SetDistortionMeshLayer(layer)
        end
        ---	An equivalent of the "Don't render this layer" checkbox in the layer settings
        ---Ver: < 9.5
        ---@param b boolean Ver: < 9.5
        ---@return nil
        function MohoLayer:SetEditOnly(b)
        end
        ---Ver: < 9.5
        ---@param b boolean Ver: < 9.5
        ---@param mode number|nil Default: 2 | Ver: < 9.5
        ---@return nil
        function MohoLayer:SetFaceCamera(b, mode)
        end
        ---Ver: < 9.5
        ---@param layer MohoLayer Ver: < 9.5
        ---@param curve M_Curve Ver: < 9.5
        ---@param startingCurvePercentage number Ver: < 9.5
        ---@param bendWithCurve boolean Ver: < 9.5
        ---@return nil
        function MohoLayer:SetFollowingCurve(layer, curve, startingCurvePercentage, bendWithCurve)
        end
        ---Sets the file path to the layer's HSV modifier image.
        --- | Ver: < 9.5
        ---@param path string File path | Ver: < 9.5
        ---@return nil
        function MohoLayer:SetHsvImage(path)
        end
        ---Ver: 13.5.2
        ---@param b boolean Ver: 13.5.2
        ---@return nil
        function MohoLayer:SetIgnoredByLayerPicker(b)
        end
        ---Ver: < 9.5
        ---@param b boolean Ver: < 9.5
        ---@return nil
        function MohoLayer:SetImmuneToCamera(b)
        end
        ---Sets whether the layer is immune to depth of field effects.
        --- | Ver: < 9.5
        ---@param b boolean Ver: < 9.5
        ---@return nil
        function MohoLayer:SetImmuneToDof(b)
        end
        ---colID: 0 = Plain 1 = Red 2 = Orange 3 = Yellow 4 = Green 5 = Blue 6 = Purple 7 = Tan 8 = Pink 9 = Turquoise 10 = Cadet Blue 11 = Coral
        --- | Ver: 11.0
        ---@param colID number Ver: 11.0
        ---@return nil
        function MohoLayer:SetLabelColor(colID)
        end
        ---Set the id number of the layer's controlling parent bone.
        --- | Ver: < 9.5
        ---@param id number Id number of bone, -1 for none, or -2 for flexi-binding | Ver: < 9.5
        ---@return nil
        function MohoLayer:SetLayerParentBone(id)
        end
        ---Ver: < 9.5
        ---@param path string Ver: < 9.5
        ---@return nil
        function MohoLayer:SetLayerScript(path)
        end
        ---Ver: < 9.5
        ---@param b boolean Ver: < 9.5
        ---@return nil
        function MohoLayer:SetLocked(b)
        end
        ---Ver: < 9.5
        ---@param b boolean Ver: < 9.5
        ---@return nil
        function MohoLayer:SetMaskExpansion(b)
        end
        ---Set the layer's masking mode. See the masking mode constants.
        ---Ver: < 9.5
        ---@param mode number The layer's masking mode | Ver: < 9.5
        ---@return nil
        function MohoLayer:SetMaskingMode(mode)
        end
        ---Sets a layer's name.
        --- | Ver: < 9.5
        ---@param name string The new name | Ver: < 9.5
        ---@return nil
        function MohoLayer:SetName(name)
        end
        ---Set the origin point of the layer.
        --- | Ver: < 9.5
        ---@param origin Vector2 The origin point of the layer | Ver: < 9.5
        ---@return nil
        function MohoLayer:SetOrigin(origin)
        end
        ---Ver: < 9.5
        ---@param origin Vector2 Ver: < 9.5
        ---@return nil
        function MohoLayer:SetOriginWithTransformCorrection(origin)
        end
        ---Ver: 9.5 | Ver: 14.0
        ---@param flags number Sum of the Quality Flags constants | Ver: 9.5
        ---@param recursive boolean|nil Default: false | Ver: 14.0
        ---@return nil
        function MohoLayer:SetQualityFlags(flags, recursive)
        end
        ---An equivalent of the "Hide in editing view" checkbox in the layer settings
        ---Ver: < 9.5
        ---@param b boolean Ver: < 9.5
        ---@return nil
        function MohoLayer:SetRenderOnly(b)
        end
        ---Ver: < 9.5
        ---@param b boolean Ver: < 9.5
        ---@return nil
        function MohoLayer:SetRotateToFollow(b)
        end
        ---Ver: < 9.5
        ---@param b boolean Ver: < 9.5
        ---@return nil
        function MohoLayer:SetScaleCompensation(b)
        end
        ---This routine sets the scale normalization factor that is to be applied subsequently by MohoLayer:ScaleNormalization. Only the normalization factor for the selected layer is set. If the layer has ancestors or descendants and it is required to change their individual scale normalization factors this must be done separately for each layer. Whilst any value may be set, a typical use would be to set the inverse of the amount of scaling done on a layer. For example: take the average the scale x,y values and then divide 1 by this value - e.g. SetScaleNormalization ( 1/((scale.x+scale.y)/2) )
        --- | Ver: 11.0
        ---@param f number Ver: 11.0
        ---@return nil
        function MohoLayer:SetScaleNormalization(f)
        end
        ---Ver: < 9.5
        ---@param sel boolean Ver: < 9.5
        ---@return nil
        function MohoLayer:SetSecondarySelection(sel)
        end
        ---Ver: 12.0
        ---@param b boolean Ver: 12.0
        ---@return nil
        function MohoLayer:SetShownOnTimeline(b)
        end
        ---Moves the layer start time
        ---The sequencer in the UI allows a user to drag a layer so that it starts at some frame other than timeline frame 0. This method is used to shift the layer start frame programmatically. The timingOffset is the number that, when added to the layer start frame, produces zero. That is: a negative offset moves the layer start time LATER in the timeline (e.g. a timingOffset of -25 means the layer's "frame 0" is timeline frame 25) see also MohoLayer:TimingOffset and MohoLayer:TotalTimingOffset
        --- | Ver: < 9.5
        ---@param timingOffset number number of frames between the layer's frame zero and timeline frame zero (signed integer) | Ver: < 9.5
        ---@return nil
        function MohoLayer:SetTimingOffset(timingOffset)
        end
        ---Ver: 12.0
        ---@param comments string Ver: 12.0
        ---@return nil
        function MohoLayer:SetUserComments(comments)
        end
        ---Ver: 12.0
        ---@param tags string Ver: 12.0
        ---@return nil
        function MohoLayer:SetUserTags(tags)
        end
        ---	Sets the layer's visibility in the workarea
        ---This determines if the layer is visible (vis = true) or not in the workarea. See also the animated property layer:fVisibility in class:MohoLayer
        --- | Ver: < 9.5
        ---@param vis boolean Ver: < 9.5
        ---@return nil
        function MohoLayer:SetVisible(vis)
        end
        ---Added in version 14.0 to replace MohoLayer:SetDistortionMeshLayer
        ---Ver: 14.0
        ---@param layer MohoLayer Ver: 14.0
        ---@return nil
        function MohoLayer:SetWarpLayer(layer)
        end
        ---Toggle whether to display construction curves for this layer.
        --- | Ver: < 9.5
        ---@param b boolean True to display construction curves, false to hide them | Ver: < 9.5
        ---@return nil
        function MohoLayer:ShowConstructionCurves(b)
        end
        ---Get this layer's timingOffset See also MohoLayer:SetTimingOffset and MohoLayer:TotalTimingOffset
        --- | Ver: < 9.5
        ---@return number
        function MohoLayer:TimingOffset()
        end
        ---Get the total timing offset for a layer, accumulating all parent layer offsets. For example, the layer has an offset of -4 and its only parent layer has an offset of -25 - so the total timing offset is -4 + (-25) = -29 See also MohoLayer:TimingOffset and MohoLayer:SetTimingOffset
        --- | Ver: < 9.5
        ---@return number
        function MohoLayer:TotalTimingOffset()
        end
        ---Recalculates animated properties for this layer. Use this when you've made a lot of changes that need to be re-processed.
        --- | Ver: < 9.5
        ---@param extended boolean|nil Perform an extended update | Default: false | Ver: < 9.5
        ---@return nil
        function MohoLayer:UpdateCurFrame(extended)
        end
        ---Ver: 11.0
        ---@param syncOptions MohoLayerRefSyncOptions|nil Default: NULL | Ver: 11.0
        ---@return boolean
        function MohoLayer:UpdateReferencedLayer(syncOptions)
        end
        ---Ver: 12.0
        ---@return string
        function MohoLayer:UserComments()
        end
        ---Ver: 12.0
        ---@return string
        function MohoLayer:UserTags()
        end
        ---Ver: < 9.5
        ---@return string
        function MohoLayer:UUID()
        end
        ---@class AudioLayer  
        ---@field fAudioLevel AnimVal --Ver: < 9.5
        ---@field fJumpToFrame AnimVal --Ver: < 9.5
        MohoLayer.AudioLayer = {}
            ---Ver: < 9.5
            ---@param outAccess LM_SecureAccess|nil Added in version 9.5 | Default: NULL | Ver: < 9.5
            ---@return string
            function MohoLayer.AudioLayer:AudioFile(outAccess)
            end
            ---Ver: < 9.5
            ---@return string
            function MohoLayer.AudioLayer:AudioText()
            end
            ---Ver: < 9.5
            ---@param frame number Ver: < 9.5
            ---@param frameDuration number Ver: < 9.5
            ---@return number
            function MohoLayer.AudioLayer:FrameToAudioTime(frame, frameDuration)
            end
            ---Ver: < 9.5
            ---@param startTime number Ver: < 9.5
            ---@param duration number Ver: < 9.5
            ---@return number
            function MohoLayer.AudioLayer:GetAmplitude(startTime, duration)
            end
            --- | Ver: < 9.5 | Ver: 10.0
            ---@param frame number Ver: < 9.5
            ---@param forRendering boolean|nil Default: false | Ver: 10.0
            ---@return number
            function MohoLayer.AudioLayer:GetAudioLevel(frame, forRendering)
            end
            ---Ver: < 9.5
            ---@param startTime number Ver: < 9.5
            ---@param duration number Ver: < 9.5
            ---@return number
            function MohoLayer.AudioLayer:GetMaxAmplitude(startTime, duration)
            end
            ---Ver: < 9.5
            ---@param startTime number Ver: < 9.5
            ---@param duration number Ver: < 9.5
            ---@return number
            function MohoLayer.AudioLayer:GetRMSAmplitude(startTime, duration)
            end
            ---Ver: < 9.5
            ---@param frame number Ver: < 9.5
            ---@return number
            function MohoLayer.AudioLayer:GetStereoPosition(frame)
            end
            ---Ver: < 9.5
            ---@return boolean
            function MohoLayer.AudioLayer:IsSilent()
            end
            ---Ver: < 9.5
            ---@return number
            function MohoLayer.AudioLayer:MaxAmplitude()
            end
            ---Ver: < 9.5
            ---@param path string Ver: < 9.5
            ---@return nil
            function MohoLayer.AudioLayer:SetAudioFile(path)
            end
            ---Ver: < 9.5
            ---@param text string Ver: < 9.5
            ---@return nil
            function MohoLayer.AudioLayer:SetAudioText(text)
            end
            ---Ver: < 9.5
            ---@param b boolean Ver: < 9.5
            ---@return nil
            function MohoLayer.AudioLayer:SetSpatialPositioning(b)
            end
            ---Ver: < 9.5
            ---@return boolean
            function MohoLayer.AudioLayer:SpatialPositioning()
            end
            ---@class ImageLayer
            MohoLayer.AudioLayer.ImageLayer = {}
            ---Ver: < 9.5
            ---@param seedPt Vector2 Ver: < 9.5
            ---@param tolerance number Ver: < 9.5
            ---@param reverseMask boolean Ver: < 9.5
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:AddMaskingPoint(seedPt, tolerance, reverseMask)
            end
            ---Ver: < 9.5
            ---@param pos Vector2 Ver: < 9.5
            ---@param frame number Ver: < 9.5
            ---@return TrackingPoint
            function MohoLayer.AudioLayer.ImageLayer:AddTrackingPoint(pos, frame)
            end
            ---Ver: < 9.5
            ---@param tolerance number Ver: < 9.5
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:AdjustMaskingPointTolerance(tolerance)
            end
            ---Ver: < 9.5
            ---@return boolean
            function MohoLayer.AudioLayer.ImageLayer:AviHasAlpha()
            end
            ---Ver: 10.1
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:ClearCroppingBounds()
            end
            ---Ver: < 9.5
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:ClearMaskingPoints()
            end
            ---Ver: < 9.5
            ---@return number
            function MohoLayer.AudioLayer.ImageLayer:CountTrackingPoints()
            end
            ---Ver: 10.1
            ---@return BBox
            function MohoLayer.AudioLayer.ImageLayer:CroppingBounds()
            end
            ---Ver: < 9.5
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:DeleteSelectedTrackingPoints()
            end
            ---Ver: < 9.5
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:FinalizeMaskingPoint()
            end
            ---Ver: < 9.5
            ---@param id number Ver: < 9.5
            ---@return TrackingPoint
            function MohoLayer.AudioLayer.ImageLayer:GetTrackingPoint(id)
            end
            ---Ver: < 9.5
            ---@param minEdgeThreshold number Ver: < 9.5
            ---@param maxEdgeThreshold number Ver: < 9.5
            ---@param grayThreshold number Ver: < 9.5
            ---@param blackThreshold number Ver: < 9.5
            ---@param saturation number Ver: < 9.5
            ---@param lightness number Ver: < 9.5
            ---@param quantizeLevels number Ver: < 9.5
            ---@return boolean
            function MohoLayer.AudioLayer.ImageLayer:HasToonEffect(minEdgeThreshold, maxEdgeThreshold, grayThreshold, blackThreshold, saturation, lightness, quantizeLevels)
            end
            ---Ver: 10.1
            ---@return number
            function MohoLayer.AudioLayer.ImageLayer:Height()
            end
            ---Ver: < 9.5
            ---@param pixel Vector2 Ver: < 9.5
            ---@param layer Vector2 Ver: < 9.5
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:ImageToLayer(pixel, layer)
            end
            ---Ver: 12.1
            ---@return boolean
            function MohoLayer.AudioLayer.ImageLayer:IsImageSequenceLayer()
            end
            ---Ver: < 9.5
            ---@return boolean
            function MohoLayer.AudioLayer.ImageLayer:IsMovieLayer()
            end
            ---Check if the image layer is using a PSD file
            ---Returns true if the image layer is using a PSD file, false otherwise.
            --- | Ver: 11.2
            ---@return boolean --true if the image layer is using a PSD file, false otherwise
            function MohoLayer.AudioLayer.ImageLayer:IsPSDImage()
            end
            ---Ver: 10.0
            ---@return boolean
            function MohoLayer.AudioLayer.ImageLayer:IsUnwrapped()
            end
            ---Ver: < 9.5
            ---@param layer Vector2 Ver: < 9.5
            ---@param pixel Vector2 Ver: < 9.5
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:LayerToImage(layer, pixel)
            end
            ---Ver: < 9.5
            ---@return number
            function MohoLayer.AudioLayer.ImageLayer:MovieDuration()
            end
            ---Ver: < 9.5
            ---@return number
            function MohoLayer.AudioLayer.ImageLayer:MovieFps()
            end
            ---Ver: < 9.5
            ---@return boolean
            function MohoLayer.AudioLayer.ImageLayer:MovieLooping()
            end
            ---Ver: < 9.5
            ---@return boolean
            function MohoLayer.AudioLayer.ImageLayer:MovieReversed()
            end
            ---Ver: < 9.5
            ---@return number
            function MohoLayer.AudioLayer.ImageLayer:PixelHeight()
            end
            ---Ver: < 9.5
            ---@return number
            function MohoLayer.AudioLayer.ImageLayer:PixelWidth()
            end
            ---Ver: < 9.5
            ---@return boolean
            function MohoLayer.AudioLayer.ImageLayer:PremultipliedMovie()
            end
            ---Removed in 13.5.2.
            ---Returns the layer ID by order that is being used
            --- | Ver: 11.2
            ---@return number --the layer ID by order that is being used
            function MohoLayer.AudioLayer.ImageLayer:PSDLayerOrderID()
            end
            ---Ver: 10.0
            ---@return number
            function MohoLayer.AudioLayer.ImageLayer:QualityLevel()
            end
            ---Ver: 13.5.2
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:ResetPSDBounds()
            end
            ---Ver: < 9.5
            ---@return number
            function MohoLayer.AudioLayer.ImageLayer:SamplingMode()
            end
            ---Ver: < 9.5
            ---@param b boolean Ver: < 9.5
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:SetAviAlpha(b)
            end
            ---Ver: 10.1
            ---@param bounds BBox Ver: 10.1
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:SetCroppingBounds(bounds)
            end
            ---Ver: < 9.5
            ---@param fps number Ver: < 9.5
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:SetMovieFps(fps)
            end
            ---Ver: < 9.5
            ---@param b boolean Ver: < 9.5
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:SetMovieLooping(b)
            end
            ---Ver: < 9.5
            ---@param b boolean Ver: < 9.5
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:SetPremultipliedMovie(b)
            end
            ---	Removed in 13.5.2.
            ---Set the layer to use based on its order in the PSD file (0 is the bottom layer, then 1, 2, 3, etc.)
            --- | Ver: 11.2
            ---@param layerOrderID number Ver: 11.2
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:SetPSDLayerOrderID(layerOrderID)
            end
            ---Ver: 10.0
            ---@param q number currently this should be 1 or 2 (low or high) | Ver: 10.0
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:SetQualityLevel(q)
            end
            ---Ver: < 9.5
            ---@param b boolean Ver: < 9.5
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:SetReverseMovie(b)
            end
            ---Change the image layer's sampling mode
            ---Ver: < 9.5
            ---@param mode number See the image sampling constants | Ver: < 9.5
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:SetSamplingMode(mode)
            end
            ---Assign an image file to the image layer.
            --- | Ver: < 9.5
            ---@param path string File path to the image file. You can use filename.psd*5* notation if you want to specify the layer number in PSD | Ver: < 9.5
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:SetSourceImage(path)
            end
            ---Ver: < 9.5
            ---@param b boolean Ver: < 9.5
            ---@param minEdgeThreshold number Ver: < 9.5
            ---@param maxEdgeThreshold number Ver: < 9.5
            ---@param grayThreshold number Ver: < 9.5
            ---@param blackThreshold number Ver: < 9.5
            ---@param saturation number Ver: < 9.5
            ---@param lightness number Ver: < 9.5
            ---@param quantizeLevels number Ver: < 9.5
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:SetToonEffect(b, minEdgeThreshold, maxEdgeThreshold, grayThreshold, blackThreshold, saturation, lightness, quantizeLevels)
            end
            ---Removed in 13.5.2.
            ---Set the unique layer ID that you want to use
            --- | Ver: 11.2
            ---@param layerPSDID number Ver: 11.2
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:SetUniquePSDLayerID(layerPSDID)
            end
            ---Ver: 10.0
            ---@param b boolean Ver: 10.0
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:SetUnwrap(b)
            end
            ---Return the source image's file path. *5* notation will have been stripped off.
            --- | Ver: < 9.5
            ---@param outAccess LM_SecureAccess|nil Default: NULL | Ver: < 9.5
            ---@return string --file path to the image file
            function MohoLayer.AudioLayer.ImageLayer:SourceImage(outAccess)
            end
            ---Ver: < 9.5
            ---@param point TrackingPoint Ver: < 9.5
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:StartTracking(point)
            end
            ---Removed in 13.5.2.
            ---Returns the unique ID
            --- | Ver: 11.2
            ---@return number --Returns the unique ID
            function MohoLayer.AudioLayer.ImageLayer:UniquePSDLayerID()
            end
            ---This method unloads the image from memory - AS will load it back in if and when it needs to use it.
            --- | Ver: 11.2
            ---@return nil
            function MohoLayer.AudioLayer.ImageLayer:UnloadImage()
            end
            ---Ver: 10.1
            ---@return number
            function MohoLayer.AudioLayer.ImageLayer:Width()
            end
        ---@class GroupLayer
        MohoLayer.GroupLayer = {}
            ---Returns the number of sub-layers in the group.
            --- | Ver: < 9.5
            ---@return number --the number of sub-layers in the group
            function MohoLayer.GroupLayer:CountLayers()
            end
            ---Perform an incremental depth sort on the group's sub-layers.
            --- | Ver: < 9.5
            ---@param doc MohoDoc|nil The Anime Studio document | Default: nil | Ver: < 9.5
            ---@return nil
            function MohoLayer.GroupLayer:DepthSort(doc)
            end
            ---Ver: < 9.5
            ---@param b boolean Ver: < 9.5
            ---@return nil
            function MohoLayer.GroupLayer:EnableLayerOrdering(b)
            end
            ---Ver: < 9.5
            ---@param frame number Ver: < 9.5
            ---@param b boolean Ver: < 9.5
            ---@return nil
            function MohoLayer.GroupLayer:EnablePhysics(frame, b)
            end
            ---Expands or contracts the group in the Moho Layers window.
            --- | Ver: < 9.5
            ---@param b boolean True to expand, false to contract | Ver: < 9.5
            ---@return nil
            function MohoLayer.GroupLayer:Expand(b)
            end
            ---Perform a full depth sort on the group's sub-layers.
            --- | Ver: < 9.5
            ---@param doc MohoDoc|nil The Anime Studio document | Default: nil | Ver: < 9.5
            ---@return nil
            function MohoLayer.GroupLayer:FullDepthSort(doc)
            end
            ---Returns the group's masking mode. See group masking mode constants.
            --- | Ver: < 9.5
            ---@return number --the group's masking mode
            function MohoLayer.GroupLayer:GetGroupMask()
            end
            ---This accesses the layer ordering animation channel which is an AnimString
            ---The return types are as from the AnimString and AnimChannel classes.
            ---For this method, AnimString is a list of layer UUID (MohoLayer:UUID) separated by the "|" character. See the AnimString and AnimChannel classes for the operations available.
            ---For example:
            --- local i
            --- local layer = moho.layer
            --- if not layer:IsGroupType() then
            ---     return
            --- end
            --- local groupLayer = moho:LayerAsGroup(layer)
            --- local layersSet = ""
            ---
            --- -- Print layer UUIDs in a group and build a string in a format usable
            --- -- to set the order at the given frame
            ---
            --- for i = 0,  groupLayer:CountLayers()-1 do
            ---     Print (groupLayer:Layer(i):Name() .. has UUID .. groupLayer:Layer(i):UUID())
            ---     layersSet = layersSet ..groupLayer:Layer(i):UUID() .. "|"
            --- end
            --- groupLayer:GetLayerOrdering():SetValue(moho.frame,layersSet)
            ---
            --- -- Get the layer order (i.e. string of delineated UUIDs) at the current frame
            --- local testSort = groupLayer:GetLayerOrdering():GetValue(moho.frame)
            ---
            --- --See if there's a key at a given frame - print it if so or set one if not
            --- if groupLayer:GetLayerOrdering():HasKey(moho.frame) then
            ---     testSort = groupLayer:GetLayerOrdering():GetValue(moho.frame)
            ---     Print ("frame " .. tostring(moho.layerFrame) .. " Has a key: " .. testSort)
            --- else
            ---     groupLayer:GetLayerOrdering():SetValue(moho.frame, layersSet)
            --- end
            --- | Ver: < 9.5
            ---@return AnimString
            function MohoLayer.GroupLayer:GetLayerOrdering()
            end
            ---Ver: < 9.5
            ---@return boolean
            function MohoLayer.GroupLayer:IsDepthSorted()
            end
            ---Ver: < 9.5
            ---@return boolean
            function MohoLayer.GroupLayer:IsDistanceSorted()
            end
            ---Tests whether the group is expanded in the Moho Layers window.
            --- | Ver: < 9.5
            ---@return boolean --true if expanded, otherwise false
            function MohoLayer.GroupLayer:IsExpanded()
            end
            ---Ver: < 9.5
            ---@return boolean
            function MohoLayer.GroupLayer:IsLayerOrdered()
            end
            ---Ver: < 9.5
            ---@return boolean
            function MohoLayer.GroupLayer:IsLayerOrderingEnabled()
            end
            ---Verifies if the given layer is located inside the group, with any nesting depth.
            ---This method accepts multiple input formats:
            ---GroupLayer:IsLayerValid(MohoLayer) | GroupLayer:IsLayerValid(string)
            --- | Ver: < 9.5
            ---@param arg1 MohoLayer | string The layer to verify | UUID string of the layer | Ver: < 9.5
            ---@return boolean
            function MohoLayer.GroupLayer:IsLayerValid(arg1)
            end
            ---Ver: < 9.5
            ---@param childLayer MohoLayer Ver: < 9.5
            ---@return boolean
            function MohoLayer.GroupLayer:IsMyChild(childLayer)
            end
            ---	if frame is < 0, the return value is true if physics is *ever* enabled during the course of the animation
            ---Ver: < 9.5
            ---@param frame number Ver: < 9.5
            ---@return boolean
            function MohoLayer.GroupLayer:IsPhysicsEnabled(frame)
            end
            ---Returns a layer in the group (starting at 0 for the first layer).
            --- | Ver: < 9.5
            ---@param id number Index number of the layer | Ver: < 9.5
            ---@return MohoLayer --a layer in the group
            function MohoLayer.GroupLayer:Layer(id)
            end
            ---Returns a layer in the group, in order from furthest to nearest (starting at 0 for the furthest layer).
            --- | Ver: < 9.5
            ---@param id number Depth index number of the layer | Ver: < 9.5
            ---@return MohoLayer --a layer in the group
            function MohoLayer.GroupLayer:LayerByDepth(id)
            end
            ---Ver: < 9.5
            ---@param name string Ver: < 9.5
            ---@return MohoLayer
            function MohoLayer.GroupLayer:LayerByName(name)
            end
            ---returns the most recent keyframe (before or at the given frame) for physics enablement
            ---Ver: < 9.5
            ---@param frame number Ver: < 9.5
            ---@return number
            function MohoLayer.GroupLayer:MostRecentPhysicsKeyframe(frame)
            end
            ---Ver: < 9.5
            ---@return boolean
            function MohoLayer.GroupLayer:MultiplePhysicsKeys()
            end
            ---Ver: < 9.5
            ---@return Vector2
            function MohoLayer.GroupLayer:PhysicsGravity()
            end
            ---Ver: < 9.5
            ---@param b boolean Ver: < 9.5
            ---@return nil
            function MohoLayer.GroupLayer:SetDepthSorted(b)
            end
            ---Ver: < 9.5
            ---@param b boolean Ver: < 9.5
            ---@return nil
            function MohoLayer.GroupLayer:SetDistanceSorted(b)
            end
            ---	Sets the group masking mode. 0=none, 1=all visible, 2=all invisible
            ---Ver: < 9.5
            ---@param mask number The group's masking mode | Ver: < 9.5
            ---@return nil
            function MohoLayer.GroupLayer:SetGroupMask(mask)
            end
            ---Ver: < 9.5
            ---@param gravity Vector2 Ver: < 9.5
            ---@return nil
            function MohoLayer.GroupLayer:SetPhysicsGravity(gravity)
            end
            ---Ver: 12.0
            ---@return nil
            function MohoLayer.GroupLayer:StripUserComments()
            end
            ---Ver: 12.0
            ---@return nil
            function MohoLayer.GroupLayer:StripUserTags()
            end
            ---Ver: < 9.5
            ---@param b boolean Ver: < 9.5
            ---@return nil
            function MohoLayer.GroupLayer:UseBakedPhysics(b)
            end
            ---Ver: < 9.5
            ---@return boolean
            function MohoLayer.GroupLayer:UsingBakedPhysics()
            end
            ---@class BoneLayer
            ---@field fGravityDirection AnimVal --Added in version 14.0 | Ver: 14.0
            ---@field fGravityStrength AnimVal --Added in version 14.0 | Ver: 14.0
            ---@field fWindDirection AnimVal --Ver: 13.5.2
            ---@field fWindStrength AnimVal --Ver: 13.5.2
            ---@field fWindTurbulentAmplitude AnimVal --Ver: 13.5.2
            ---@field fWindTurbulentFrequency AnimVal --Ver: 13.5.2
            MohoLayer.GroupLayer.BoneLayer = {}
                ---Ver: 14.2
                ---@param doc MohoDoc Ver: 14.2
                ---@param db XGraphics Ver: 14.2
                ---@param boneID number Ver: 14.2
                ---@return nil
                function MohoLayer.GroupLayer.BoneLayer:DrawHighlightedBone(doc, db, boneID)
                end
                ---Ver: 13.5.2
                ---@param location Vector2 Ver: 13.5.2
                ---@return number
                function MohoLayer.GroupLayer.BoneLayer:EvaluateWindTurbulentAngle(location)
                end
                ---Ver: 13.5.2
                ---@param location Vector2 Ver: 13.5.2
                ---@return number
                function MohoLayer.GroupLayer.BoneLayer:EvaluateWindTurbulentStrength(location)
                end
                ---Get nested layer control setting (Added in version 11)
                ---Indicates if bone layer has "Allow nested layer control" enabled.
                ---For example:
                --- if (moho.layer:LayerType() == MOHO.LT_BONE) then
                ---  print(tostring(moho:LayerAsBone(moho.layer):IsGrandpaBone()))
                --- end
                --- | Ver: 11.0
                ---@return boolean
                function MohoLayer.GroupLayer.BoneLayer:IsGrandpaBone()
                end
                ---Set nested layer control (Added in version 11)
                ---Set whether bone layer allows nested layer control.
                ---For example:
                --- if (moho.layer:LayerType() == MOHO.LT_BONE) then
                ---  moho:LayerAsBone(moho.layer):SetGrandpaBone(false)
                --- end
                --- | Ver: 11.0
                ---@param value boolean Ver: 11.0
                ---@return nil
                function MohoLayer.GroupLayer.BoneLayer:SetGrandpaBone(value)
                end
                ---Ver: < 9.5
                ---@return M_Skeleton
                function MohoLayer.GroupLayer.BoneLayer:Skeleton()
                end
        ---@class Mesh3DLayer
        ---@field f3DOptions Mesh3DOptions --Ver: < 9.5
        MohoLayer.Mesh3DLayer = {}
            ---Returns the edge offset used when drawing the edges of this 3D object.
            --- | Ver: < 9.5
            ---@return number --edge offset value
            function MohoLayer.Mesh3DLayer:EdgeOffset()
            end
            ---Removed in 13.5.2.
            ---Ver: < 9.5
            ---@param id number Ver: < 9.5
            ---@return PoserActorGroup
            function MohoLayer.Mesh3DLayer:GetPoserActorGroup(id)
            end
            ---Removed in 13.5.2.
            ---Ver: < 9.5
            ---@return boolean
            function MohoLayer.Mesh3DLayer:IsPoserLayer()
            end
            ---Ver: 11.0
            ---@param id number Ver: 11.0
            ---@return M_Face3D
            function MohoLayer.Mesh3DLayer:PickedFaceByID(id)
            end
            ---Removed in 13.5.2.
            ---Ver: < 9.5
            ---@return number
            function MohoLayer.Mesh3DLayer:SelectedPoserActorGroup()
            end
            ---Removed in 13.5.2.
            ---Ver: 10.0
            ---@param figureName string Ver: 10.0
            ---@return nil
            function MohoLayer.Mesh3DLayer:SelectPoserBody(figureName)
            end
            ---Sets the edge offset used when drawing the edges of this 3D object.
            --- | Ver: < 9.5
            ---@param offset number Edge offset value | Ver: < 9.5
            ---@return nil
            function MohoLayer.Mesh3DLayer:SetEdgeOffset(offset)
            end
            ---Removed in 13.5.2.
            ---Ver: < 9.5
            ---@param id number Ver: < 9.5
            ---@return nil
            function MohoLayer.Mesh3DLayer:SetSelectedPoserActorGroup(id)
            end
            ---Loads a 3D OBJ file from disk as the layer's 3D mesh.
            --- | Ver: < 9.5
            ---@param path string File path to an OBJ file | Ver: < 9.5
            ---@return nil
            function MohoLayer.Mesh3DLayer:SetSourceMesh(path)
            end
            ---Returns the path to the 3D OBJ file used by this layer.
            --- | Ver: < 9.5
            ---@param outAccess LM_SecureAccess|nil Default: nil | Ver: < 9.5
            ---@return string --file path to an OBJ file
            function MohoLayer.Mesh3DLayer:SourceMesh(outAccess)
            end
            ---Removed in 13.5.2.
            ---Ver: < 9.5
            ---@return nil
            function MohoLayer.Mesh3DLayer:UpdatePoserParameters()
            end
        ---@class MeshLayer
        MohoLayer.MeshLayer = {}
            ---Ver: 13.5.2
            ---@return boolean
            function MohoLayer.MeshLayer:ContinuousTriangulation()
            end
            ---Ver: 12.0
            ---@param doc MohoDoc Ver: 12.0
            ---@param db XGraphics Ver: 12.0
            ---@param selectedOnly boolean Ver: 12.0
            ---@return nil
            function MohoLayer.MeshLayer:DrawHandles(doc, db, selectedOnly)
            end
            ---Ver: < 9.5
            ---@param outAccess LM_SecureAccess|nil Default: NULL | Ver: < 9.5
            ---@return string
            function MohoLayer.MeshLayer:FillTexture(outAccess)
            end
            ---Ver: 14.0
            ---@return boolean
            function MohoLayer.MeshLayer:IsCurver()
            end
            ---Ver: < 9.5
            ---@param shape M_Shape Ver: < 9.5
            ---@return boolean
            function MohoLayer.MeshLayer:IsShapeClosed(shape)
            end
            ---Ver: 14.0
            ---@return boolean
            function MohoLayer.MeshLayer:IsWarpLayer()
            end
            ---Ver: 14.0
            ---@param curverOnly boolean|nil Default: false | Ver: 14.0
            ---@return boolean
            function MohoLayer.MeshLayer:IsWarpLayerCandidate(curverOnly)
            end
            ---Ver: < 9.5
            ---@param outAccess LM_SecureAccess|nil Default: NULL | Ver: < 9.5
            ---@return string
            function MohoLayer.MeshLayer:LineTexture(outAccess)
            end
            ---Ver: 14.0
            ---@param b boolean Ver: 14.0
            ---@return nil
            function MohoLayer.MeshLayer:MarkAsCurver(b)
            end
            ---Ver: 14.0
            ---@param b boolean Ver: 14.0
            ---@param target MohoLayer Ver: 14.0
            ---@return nil
            function MohoLayer.MeshLayer:MarkAsWarpLayer(b, target)
            end
            ---Returns the 2D mesh of the layer
            --- | Ver: < 9.5
            ---@return M_Mesh --the 2D mesh object
            function MohoLayer.MeshLayer:Mesh()
            end
            ---Ver: 14.0
            ---@param b boolean Ver: 14.0
            ---@return nil
            function MohoLayer.MeshLayer:PauseContinuousTriangulation(b)
            end
            ---Ver: < 9.5
            ---@return nil
            function MohoLayer.MeshLayer:Purge3DLayer()
            end
            ---Ver: 13.5.2
            ---@param b boolean Ver: 13.5.2
            ---@return nil
            function MohoLayer.MeshLayer:SetContinuousTriangulation(b)
            end
            ---Ver: < 9.5
            ---@param path string Ver: < 9.5
            ---@return nil
            function MohoLayer.MeshLayer:SetFillTexture(path)
            end
            ---Ver: < 9.5
            ---@param path string Ver: < 9.5
            ---@return nil
            function MohoLayer.MeshLayer:SetLineTexture(path)
            end
            ---Added in version 14.0 to replace MeshLayer:SetNeedsWarpMeshUpdate
            ---Ver: 14.0
            ---@return nil
            function MohoLayer.MeshLayer:SetNeedsWarpLayerUpdate()
            end
            ---	Added in version 13.5.2. In Moho 14.0, replaced by MeshLayer:SetNeedsWarpLayerUpdate
            ---Ver: 13.5.2
            ---@return nil
            function MohoLayer.MeshLayer:SetNeedsWarpMeshUpdate()
            end
            ---	Added in version 14.0 to replace MeshLayer:UpdateWarpMesh
            ---Ver: 14.0
            ---@return nil
            function MohoLayer.MeshLayer:UpdateWarpLayer()
            end
            ---	Added in version 13.5.2. In Moho 14.0, replaced by MeshLayer:UpdateWarpLayer
            ---Ver: 13.5.2
            ---@return nil
            function MohoLayer.MeshLayer:UpdateWarpMesh()
            end
        ---@class NoteLayer
        MohoLayer.NoteLayer = {}
            ---Ver: < 9.5
            ---@return string
            function MohoLayer.NoteLayer:NoteText()
            end
            ---Ver: < 9.5
            ---@param text string Ver: < 9.5
            ---@return nil
            function MohoLayer.NoteLayer:SetNoteText(text)
            end
        ---@class ParticleLayer
        MohoLayer.ParticleLayer = {}
            ---Ver: < 9.5
            ---@return number
            function MohoLayer.ParticleLayer:Damping()
            end
            ---Ver: < 9.5
            ---@return boolean
            function MohoLayer.ParticleLayer:EvenlySpaced()
            end
            ---Call this function after making any changes to the particle parameters. This function will re-evaluate all the parameters to start a new particle simulation.
            --- | Ver: < 9.5
            ---@return nil
            function MohoLayer.ParticleLayer:FinalizeSettings()
            end
            ---the free-floating status of the particles
            ---Tests whether particles are free-floating or not.
            --- | Ver: < 9.5
            ---@return boolean
            function MohoLayer.ParticleLayer:FreeFloating()
            end
            ---Ver: < 9.5
            ---@return boolean
            function MohoLayer.ParticleLayer:FullSpeedStart()
            end
            ---This method returns 2 values: 1: real Angle 2: real Rate
            --- | Ver: < 9.5
            ---@param angle number Ver: < 9.5
            ---@param rate number Ver: < 9.5
            ---@return number --Angle
            ---@return number --Rate
            function MohoLayer.ParticleLayer:GetAcceleration(angle, rate)
            end
            ---This method returns 2 values: 1: real Angle 2: real Spread
            --- | Ver: < 9.5
            ---@param angle number Ver: < 9.5
            ---@param spread number Ver: < 9.5
            ---@return number --Angle
            ---@return number --Spread
            function MohoLayer.ParticleLayer:GetDirection(angle, spread)
            end
            ---This method returns 2 values: 1: int32 num 2: int32 displayNum
            ---Find out how many numbers are in the particle system.
            --- | Ver: < 9.5
            ---@param num number Number of particles for rendering | Ver: < 9.5
            ---@param displayNum number Number of particles to display in the editing view | Ver: < 9.5
            ---@return number --num
            ---@return number --displayNum
            function MohoLayer.ParticleLayer:GetNumParticles(num, displayNum)
            end
            ---This method returns 2 values: 1: real v 2: real spread
            --- | Ver: < 9.5
            ---@param v number Ver: < 9.5
            ---@param spread number Ver: < 9.5
            ---@return number --v
            ---@return number --spread
            function MohoLayer.ParticleLayer:GetVelocity(v, spread)
            end
            ---the lifetime of an individual particle
            ---Find out the lifetime of an individual particle.
            --- | Ver: < 9.5
            ---@return number --the lifetime of an individual particle
            function MohoLayer.ParticleLayer:Lifetime()
            end
            ---the orientation status of the particles
            ---Tests whether the particles are oriented or not.
            --- | Ver: < 9.5
            ---@return boolean --the orientation status of the particles
            function MohoLayer.ParticleLayer:Orientation()
            end
            ---Ver: < 9.5
            ---@return boolean
            function MohoLayer.ParticleLayer:RandomStartTime()
            end
            ---the particle on/off animation channel
            ---Returns the animation channel that represents the on/off status of the particle simulation.
            --- | Ver: < 9.5
            ---@return AnimBool --the particle on/off animation channel
            function MohoLayer.ParticleLayer:RunningTrack()
            end
            ---Ver: < 9.5
            ---@param angle number Ver: < 9.5
            ---@param rate number Ver: < 9.5
            ---@return nil
            function MohoLayer.ParticleLayer:SetAcceleration(angle, rate)
            end
            ---Ver: < 9.5
            ---@param d number Ver: < 9.5
            ---@return nil
            function MohoLayer.ParticleLayer:SetDamping(d)
            end
            ---Ver: < 9.5
            ---@param angle number Ver: < 9.5
            ---@param spread number Ver: < 9.5
            ---@return nil
            function MohoLayer.ParticleLayer:SetDirection(angle, spread)
            end
            ---Ver: < 9.5
            ---@param evenlySpaced boolean Ver: < 9.5
            ---@return nil
            function MohoLayer.ParticleLayer:SetEvenlySpaced(evenlySpaced)
            end
            ---Set whether particles should be free-floating.
            --- | Ver: < 9.5
            ---@param freeFloating boolean True for free-floating, otherwise false | Ver: < 9.5
            ---@return nil
            function MohoLayer.ParticleLayer:SetFreeFloating(freeFloating)
            end
            ---Ver: < 9.5
            ---@param b boolean Ver: < 9.5
            ---@return nil
            function MohoLayer.ParticleLayer:SetFullSpeedStart(b)
            end
            ---Set the lifetime of an individual particle.
            --- | Ver: < 9.5
            ---@param lifetime number Lifetime in frames | Ver: < 9.5
            ---@return nil
            function MohoLayer.ParticleLayer:SetLifetime(lifetime)
            end
            ---Set the number of particles.
            --- | Ver: < 9.5
            ---@param num number Number of particles for rendering | Ver: < 9.5
            ---@param displayNum number Number of particles to display in the editing view | Ver: < 9.5
            ---@return nil
            function MohoLayer.ParticleLayer:SetNumParticles(num, displayNum)
            end
            ---Set whether the particles should orient themselves to follow their path of motion.
            --- | Ver: < 9.5
            ---@param orient boolean True to orient the particles, otherwise false | Ver: < 9.5
            ---@return nil
            function MohoLayer.ParticleLayer:SetOrientation(orient)
            end
            ---Re-randomizes the particle simulation. Enter a random number for a random particle effect, or some known number to get back to an older simulation.
            ---For example:
            --- moho.document:PrepUndo(moho.layer, true)
            --- moho.document:SetDirty()
            --- particleLayer:SetRandomSeed(math.random(1, 16777216))
            --- moho:SetCurFrame(moho.frame) -- force the particles to be recalculated
            --- | Ver: < 9.5
            ---@param seed number The random seed | Ver: < 9.5
            ---@return nil
            function MohoLayer.ParticleLayer:SetRandomSeed(seed)
            end
            ---Ver: < 9.5
            ---@param randomStartTime boolean Ver: < 9.5
            ---@return nil
            function MohoLayer.ParticleLayer:SetRandomStartTime(randomStartTime)
            end
            ---Sets the shape of the source region of the particle system.
            --- | Ver: < 9.5
            ---@param v Vector3 Ver: < 9.5
            ---@return nil
            function MohoLayer.ParticleLayer:SetSourceDimensions(v)
            end
            ---Ver: 10.0
            ---@param baseAsSource boolean Ver: 10.0
            ---@return nil
            function MohoLayer.ParticleLayer:SetUseBaseLayerAsSource(baseAsSource)
            end
            ---Ver: < 9.5
            ---@param v number Ver: < 9.5
            ---@param spread number Ver: < 9.5
            ---@return nil
            function MohoLayer.ParticleLayer:SetVelocity(v, spread)
            end
            ---the dimensions of the particle source region
            ---Returns the dimensions of the particle source region.
            --- | Ver: < 9.5
            ---@return Vector3 --the dimensions of the particle source region
            function MohoLayer.ParticleLayer:SourceDimensions()
            end
            ---Ver: 10.0
            ---@return boolean
            function MohoLayer.ParticleLayer:UseBaseLayerAsSource()
            end
        ---@class SwitchLayer
        MohoLayer.SwitchLayer = {}
            ---Ver: < 9.5
            ---@return boolean
            function MohoLayer.SwitchLayer:ContainsVisemes()
            end
            ---Returns the switch value at a given frame. The value of a switch layer represents which sub-layer is active at that frame.
            --- | Ver: < 9.5
            ---@param frame number Frame number | Ver: < 9.5
            ---@return string --the switch value at a given frame
            function MohoLayer.SwitchLayer:GetValue(frame)
            end
            ---Test whether sub-layer interpolation is on or off.
            --- | Ver: < 9.5
            ---@return boolean --true if the sub-layers are interpolated during switching, otherwise false
            function MohoLayer.SwitchLayer:InterpMode()
            end
            ---Ver: 12.0
            ---@return boolean
            function MohoLayer.SwitchLayer:IsFBFLayer()
            end
            ---Ver: < 9.5
            ---@param phoneme string Ver: < 9.5
            ---@return string
            function MohoLayer.SwitchLayer:PhonemeToViseme(phoneme)
            end
            ---Ver: < 9.5
            ---@param viseme string Ver: < 9.5
            ---@return number
            function MohoLayer.SwitchLayer:RankViseme(viseme)
            end
            ---Ver: 12.0
            ---@param b boolean Ver: 12.0
            ---@return nil
            function MohoLayer.SwitchLayer:SetFBFLayer(b)
            end
            ---Turn on or off sub-layer interpolation.
            --- | Ver: < 9.5
            ---@param b boolean True to turn on sub-layer interpolation, false to turn it off | Ver: < 9.5
            ---@return nil
            function MohoLayer.SwitchLayer:SetInterpMode(b)
            end
            ---Assign a switch data file to the switch layer.
            --- | Ver: < 9.5
            ---@param path string File path to the switch data file | Ver: < 9.5
            ---@return nil
            function MohoLayer.SwitchLayer:SetSourceFile(path)
            end
            ---Set the switch value at a given frame. The value of a switch layer represents which sub-layer is active at that frame.
            --- | Ver: < 9.5
            ---@param frame number Frame number | Ver: < 9.5
            ---@param value string The name of the sub-layer to activate at that frame | Ver: < 9.5
            ---@return nil
            function MohoLayer.SwitchLayer:SetValue(frame, value)
            end
            ---Returns the animation channel associated with the switching function.
            --- | Ver: < 9.5
            ---@return string --switch animation channel
            function MohoLayer.SwitchLayer:SwitchValues()
            end
    ---@class InputDeviceEvent
    ---@field altKey boolean --Ver: < 9.5
    ---@field ctrlKey boolean --Ver: < 9.5
    ---@field inputData LM_Message --Ver: < 9.5
    ---@field shiftKey boolean --Ver: < 9.5
    ---@field view MohoView --Ver: < 9.5
    InputDeviceEvent = {}
    ---@class InterpSetting
    ---@field flags number --Ver: < 9.5
    ---@field hold number --how many frames to hold the previous value before interpolating to the next | Ver: < 9.5
    ---@field interpMode number --See the Interpolation Modes constants. | Ver: < 9.5
    ---@field interval number --1 to interpolate every frame, 2 to animate on 2's, 3 to animate on 3's, etc. | Ver: < 9.5
    ---@field stagger boolean --Added in version 10 | Ver: 10.0
    ---@field tags number --Used for keyframe color, and possibly other stuff in the future. The color codes are the same as for bone tags. | Ver: < 9.5
    ---@field val1 number --Depends on the interpMode. See the class description. | Ver: < 9.5
    ---@field val2 number --Depends on the interpMode. See the class description. | Ver: < 9.5
    InterpSetting = {}
        ---Ver: < 9.5
        ---@param component number Ver: < 9.5
        ---@return number
        function InterpSetting:BezierInAngle(component)
        end
        ---Ver: < 9.5
        ---@param component number Ver: < 9.5
        ---@return number
        function InterpSetting:BezierInPercentage(component)
        end
        ---Ver: < 9.5
        ---@param component number Ver: < 9.5
        ---@return number
        function InterpSetting:BezierOutAngle(component)
        end
        ---Ver: < 9.5
        ---@param component number Ver: < 9.5
        ---@return number
        function InterpSetting:BezierOutPercentage(component)
        end
        ---	Returns true if the cycle is additive.
        ---Ver: 12.0
        ---@return boolean
        function InterpSetting:IsAdditiveCycle()
        end
        ---Ver: < 9.5
        ---@return nil
        function InterpSetting:Reset()
        end
        ---	Sets the cycle to be additive or not
        ---Ver: 12.0
        ---@param b boolean Ver: 12.0
        ---@return nil
        function InterpSetting:SetAdditiveCycle(b)
        end
        ---Ver: < 9.5
        ---@param component number Ver: < 9.5
        ---@param angle number Ver: < 9.5
        ---@return nil
        function InterpSetting:SetBezierInAngle(component, angle)
        end
        ---Ver: < 9.5
        ---@param component number Ver: < 9.5
        ---@param percent number Ver: < 9.5
        ---@return nil
        function InterpSetting:SetBezierInPercentage(component, percent)
        end
        ---Ver: < 9.5
        ---@param component number Ver: < 9.5
        ---@param angle number Ver: < 9.5
        ---@return nil
        function InterpSetting:SetBezierOutAngle(component, angle)
        end
        ---Ver: < 9.5
        ---@param component number Ver: < 9.5
        ---@param percent number Ver: < 9.5
        ---@return nil
        function InterpSetting:SetBezierOutPercentage(component, percent)
        end
    ---@class KeyEvent  
    ---@field altKey boolean --True if the alt key is down, otherwise false. | Ver: < 9.5
    ---@field ctrlKey boolean --True if the ctrl key is down, otherwise false. | Ver: < 9.5
    ---@field key string --The key character that was pressed. | Ver: < 9.5
    ---@field keyCode number --A keycode to indicate which special key was pressed. See keycode constants in the LM.GUI module. | Ver: < 9.5
    ---@field shiftKey boolean --True if the shift key is down, otherwise false. | Ver: < 9.5
    ---@field view MohoView --A reference to the Moho editing view. | Ver: < 9.5
    ---The KeyEvent class is provided to your script when a keyboard event occurs. It contains information about the event.
    KeyEvent = {}
    ---@class LayerComp  
    ---This class represents the Layer Comp object from the Layers panel.
    LayerComp = {}
        ---Add a layer to the comp
        ---Ver: 10.1
        ---@param layer MohoLayer Ver: 10.1
        ---@return nil
        function LayerComp:AddLayer(layer)
        end
        ---AutoAddChildLayers adds child layers to the comp as needed according to the settings for the comp.
        ---Returns true if the comp was modified.
        ---To scan all layers in the doc, pass NULL for parent.
        --- | Ver: < 9.5
        ---@param doc MohoDoc|nil Default: nil | Ver: < 9.5
        ---@param parent GroupLayer|nil Default: nil | Ver: < 9.5
        ---@return boolean
        function LayerComp:AutoAddChildLayers(doc, parent)
        end
        ---	Getter method for group layer children
        ---Ver: < 9.5
        ---@return boolean
        function LayerComp:AutoAddsGroupLayerChildren()
        end
        ---Getter method for switch layer children
        ---Ver: < 9.5
        ---@return boolean
        function LayerComp:AutoAddsSwitchLayerChildren()
        end
        ---	Check if the comp contains the layer
        ---Ver: 10.1
        ---@param layer MohoLayer Ver: 10.1
        ---@return boolean
        function LayerComp:ContainsLayer(layer)
        end
        ---	Count the number of layers in the comp
        ---Ver: 10.1
        ---@return number
        function LayerComp:CountLayers()
        end
        ---	Get the name of the comp
        ---Ver: 10.1
        ---@return string
        function LayerComp:Name()
        end
        ---Remove all layers from the comp
        ---Ver: 10.1
        ---@return nil
        function LayerComp:RemoveAllLayers()
        end
        ---	Remove a single layer from the comp
        ---Ver: 10.1
        ---@param layer MohoLayer Ver: 10.1
        ---@return nil
        function LayerComp:RemoveLayer(layer)
        end
        ---Setter method for group layer children
        ---Ver: < 9.5
        ---@param autoAdd boolean Ver: < 9.5
        ---@return nil
        function LayerComp:SetAutoAddsGroupLayerChildren(autoAdd)
        end
        ---	Setter method for switch layer children
        ---Ver: < 9.5
        ---@param autoAdd boolean Ver: < 9.5
        ---@return nil
        function LayerComp:SetAutoAddsSwitchLayerChildren(autoAdd)
        end
        ---Set the name of the comp
        ---Ver: 10.1
        ---@param inName string Ver: 10.1
        ---@return nil
        function LayerComp:SetName(inName)
        end
    ---@class LM_FontPreview  
    ---The LM_FontPreview class is a user interface class that is used in a dialog to preview what a font will look like.
    LM_View.LM_FontPreview = {}
        ---Force the preview to redraw itself.
        --- | Ver: < 9.5
        ---@return nil
        function LM_View.LM_FontPreview:Refresh()
        end
        ---Set the name of the font to be used when displaying the preview.
        --- | Ver: < 9.5
        ---@param font string Font name | Ver: < 9.5
        ---@return nil
        function LM_View.LM_FontPreview:SetFontName(font)
        end
        ---Set the text to be displayed in the preview.
        --- | Ver: < 9.5
        ---@param text string Preview text | Ver: < 9.5
        ---@return nil
        function LM_View.LM_FontPreview:SetPreviewText(text)
        end
    ---@class LM_MeshPreview  
    ---The LM_MeshPreview class is a user interface class that is used in a dialog to preview what a vector mesh will look like. This can be used by a script that generates 2D vector shapes, giving the user a chance to see the result before it is inserted into the Moho document.
    LM_View.LM_MeshPreview = {}
        ---Automatically zooms in on the mesh so that it fills the preview region.
        --- | Ver: < 9.5
        ---@return nil
        function LM_View.LM_MeshPreview:AutoZoom()
        end
        ---Creates a shape out of the selected points in the mesh.
        --- | Ver: < 9.5
        ---@param filled boolean True for a filled shape, false for just an outline | Ver: < 9.5
        ---@return boolean --true if successful, otherwise false
        function LM_View.LM_MeshPreview:CreateShape(filled)
        end
        ---Returns the M_Mesh object associated with the preview object. You can then modify the mesh to change what is displayed in the preview region.
        --- | Ver: < 9.5
        ---@return M_Mesh --a mesh object
        function LM_View.LM_MeshPreview:Mesh()
        end
        ---Forces the preview region to re-draw itself. Call this function after making changes to the mesh.
        --- | Ver: < 9.5
        ---@return nil
        function LM_View.LM_MeshPreview:Refresh()
        end
    ---@class LM_SecureAccess  
    ---This class was added in version 9.5.
    LM_SecureAccess = {}
        ---Ver: 9.5
        ---@return boolean
        function LM_SecureAccess:AddUser()
        end
        ---Ver: 9.5
        ---@return nil
        function LM_SecureAccess:RemoveUser()
        end
    ---@class Mesh3DOptions
    ---@field fBackfaceRemoval boolean --Ver: < 9.5
    ---@field fCreaseAngle number --Ver: < 9.5
    ---@field fCreaseEdges boolean --Ver: < 9.5
    ---@field fEdgeExtension number --Ver: < 9.5
    ---@field fMaterialEdges boolean --Ver: < 9.5
    ---@field fResetZ boolean --Ver: < 9.5
    ---@field fShadingColor rgb_color --Ver: < 9.5
    ---@field fShadingDensity number --from 0 to 100 | Ver: < 9.5
    ---@field fShadingMode number --Ver: < 9.5
    ---@field fSilhouetteEdges boolean --Ver: < 9.5
    Mesh3DOptions = {}
    ---@class MohoDoc  
    ---@field fCameraPanTilt AnimVec2 --the camera pan/tilt animation channel. the x component is tilt (i.e. rotation around the camera's x axis); the y component is pan | Ver: < 9.5
    ---@field fCameraRoll AnimVal --the camera roll animation channel (i.e. rotation around the camera's z axis) | Ver: < 9.5
    ---@field fCameraTrack AnimVec3 --the camera tracking animation channel | Ver: < 9.5
    ---@field fCameraZoom AnimVal --the camera zoom animation channel. The value in this channel is: Base FoV(v)/FoV(v). [FoV(v) is Field of View (vertical). Base FoV(v) is defined by the global constant LM_ZoomCamera.DEFAULT_FOV = 60 degrees (1.0472 Rad) in AS11 and previous] | Ver: < 9.5
    ---@field fTimelineMarkers AnimString --Added in version 10.1 | Ver: 10.1
    ---The MohoDoc class represents the current Moho document object.  
    --- d = moho.document returns the current document object open in Moho.  
    --- d is of type MohoDoc - see the ScriptInterface class.
    MohoDoc = {}
        ---Ver: 10.0
        ---@param comp LayerComp Ver: 10.0
        ---@return nil
        function MohoDoc:AddLayerComp(comp)
        end
        ---Ver: 12.0
        ---@param name string Ver: 12.0
        ---@return LayerComp
        function MohoDoc:AddLayerCompWithName(name)
        end
        ---This method accepts multiple input formats:
        --- MohoDoc:AddStyle(style) | MohoDoc:AddStyle(styleName)
        --- | Ver: < 9.5 | Ver: 14.0
        ---@param arg1 M_Style|string The style object to add | The name of the style to add | Ver: < 9.5 | 14.0
        ---@return M_Style|nil
        function MohoDoc:AddStyle(arg1)
        end
        ---Ver: < 9.5
        ---@return boolean
        function MohoDoc:Allow3D()
        end
        ---Returns the duration of the document's animation. This is basically the frame number of the last keyframe in the entire document. Depending on the animation, this can be much less or much more than the document's official end frame.
        --- | Ver: < 9.5
        ---@return number --the duration of the document's animation
        function MohoDoc:AnimDuration()
        end
        ---This method accepts multiple input formats:
        --- MohoDoc:ApplyLayerComp(comp, state) | MohoDoc:ApplyLayerComp(comp, state, layer)
        --- for the second argument you can create an enum: "typedef enum {LAYER_COMP_ACTIVATE, LAYER_COMP_DEACTIVATE,LAYER_COMP_SET} LayerCompState"
        --- --LAYER_COMP_ACTIVATE// make layers in the layer comp visible - adding to existing visible layers
        --- --LAYER_COMP_DEACTIVATE// make layers in the layer comp invisible
        --- --LAYER_COMP_SET// make layers in the layer comp visible - all others invisible
        --- | Ver: 10.0
        ---@param comp LayerComp Ver: 10.0
        ---@param state number Ver: 10.0
        ---@param layer MohoLayer|nil Ver: 10.0
        ---@return nil
        function MohoDoc:ApplyLayerComp(comp, state, layer)
        end
        ---Returns the aspect ratio of the final rendered output.
        --- | Ver: < 9.5
        ---@return number --aspect ratio of the final rendered output
        function MohoDoc:AspectRatio()
        end
        ---Ver: < 9.5
        ---@return rgb_color
        function MohoDoc:BackCol()
        end
        ---Ver: 10.0
        ---@return nil
        function MohoDoc:CleanLayerComps()
        end
        ---Ver: < 9.5
        ---@param fromFrame number Ver: < 9.5
        ---@param before boolean Ver: < 9.5
        ---@return nil
        function MohoDoc:ClearAnimation(fromFrame, before)
        end
        ---Ver: < 9.5
        ---@param group GroupLayer|nil Default: nil | Ver: < 9.5
        ---@return nil
        function MohoDoc:ClearSecondarySelection(group)
        end
        ---Ver: 10.0
        ---@return number
        function MohoDoc:CountLayerComps()
        end
        ---Returns the number of top-level layers in the document.
        --- | Ver: < 9.5
        ---@return number --the number of top-level layers in the document
        function MohoDoc:CountLayers()
        end
        ---Returns number of selected layers in the moho document with NULL argument. Returns number of selected layers in the group layer.
        --- | Ver: < 9.5
        ---@param group GroupLayer|nil Default: nil | Ver: < 9.5
        ---@return number --number of selected layers in the document or group
        function MohoDoc:CountSelectedLayers(group)
        end
        ---Ver: < 9.5
        ---@return number
        function MohoDoc:CountStyles()
        end
        ---	Returns the name of the current action, or an empty string for the mainline
        ---Ver: 10.0
        ---@return string
        function MohoDoc:CurrentDocAction()
        end
        ---Ver: < 9.5
        ---@param frame number Ver: < 9.5
        ---@return nil
        function MohoDoc:DeleteKeysAtFrame(frame)
        end
        ---Ver: 10.0
        ---@param inID number Ver: 10.0
        ---@return nil
        function MohoDoc:DeleteLayerComp(inID)
        end
        ---Performs a depth sort on all layers in the document.
        --- | Ver: < 9.5
        ---@return nil
        function MohoDoc:DepthSort()
        end
        ---Ver: < 9.5
        ---@param origLayer MohoLayer Ver: < 9.5
        ---@return MohoLayer
        function MohoDoc:DuplicateLayer(origLayer)
        end
        ---Returns the document's end frame.
        --- | Ver: < 9.5
        ---@return number --document end frame
        function MohoDoc:EndFrame()
        end
        ---Returns the frame rate of the document (frames per second).
        --- | Ver: < 9.5
        ---@return number --document fps
        function MohoDoc:Fps()
        end
        ---Ver: < 9.5
        ---@return Vector3
        function MohoDoc:GetCameraDirection()
        end
        ---Ver: < 9.5
        ---@param frame number Ver: < 9.5
        ---@param m Matrix Ver: < 9.5
        ---@return nil
        function MohoDoc:GetCameraMatrix(frame, m)
        end
        ---For example, to print all the LayerComps in a document:
        --- for i = 0, moho.document:CountLayerComps()-1 do
        ---  print(moho.document:GetLayerComp(i):Name())
        --- end
        --- | Ver: 10.0
        ---@param id number Ver: 10.0
        ---@return LayerComp
        function MohoDoc:GetLayerComp(id)
        end
        ---Returns the transformation matrix for viewing the scene from the "orbit" view.
        --- | Ver: < 9.5
        ---@param m Matrix Orbit transform matrix | Ver: < 9.5
        ---@return nil
        function MohoDoc:GetOutsideViewMatrix(m)
        end
        ---Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@return MohoLayer
        function MohoDoc:GetSelectedLayer(id)
        end
        ---Ver: < 9.5
        ---@return MohoRenderStyle
        function MohoDoc:GlobalRenderStyle()
        end
        ---pixel height of the final rendered output
        ---Returns the pixel height of the final rendered output.
        --- | Ver: < 9.5
        ---@return number
        function MohoDoc:Height()
        end
        ---This method accepts multiple input formats:
        ---MohoDoc:IsLayerValid(MohoLayer) | MohoDoc:IsLayerValid(string)
        --- | Ver: < 9.5
        ---@param arg1 MohoLayer | string The layer to verify, or the UUID string of the layer | Ver: < 9.5
        ---@return boolean
        function MohoDoc:IsLayerValid(arg1)
        end
        ---true if orbiting, false if viewing through the camera
        ---Returns true if the user is orbiting the workspace, and not viewing the scene through the camera.
        --- | Ver: < 9.5
        ---@return boolean --true if orbiting, false if viewing through the camera
        function MohoDoc:IsOutsideViewEnabled()
        end
        ---Ver: < 9.5
        ---@return boolean
        function MohoDoc:IsRedoable()
        end
        ---Ver: < 9.5
        ---@param style M_Style Ver: < 9.5
        ---@param layer MohoLayer|nil The Layer to check - if layer is a group type all descendent child layers are included in the scope | Default: NULL | Ver: < 9.5
        ---@return boolean
        function MohoDoc:IsStyleUsed(style, layer)
        end
        ---Ver: < 9.5
        ---@return boolean
        function MohoDoc:IsUndoable()
        end
        ---Access an individual layer in the document.
        --- | Ver: < 9.5
        ---@param id number Id number of the layer (0 is the lowest layer in the document) | Ver: < 9.5
        ---@return MohoLayer --a layer in the document
        function MohoDoc:Layer(id)
        end
        ---	Returns layer's absolute ID no matter its hierarchy level
        ---Each layer in a scene has an unique absolute ID, starting with 0. Child layers' IDs are followed immediately by their parents. Siblings are numerated from the bottom to top.
        --- | Ver: < 9.5
        ---@param layer MohoLayer Ver: < 9.5
        ---@return number --Each layer in a scene has an unique absolute ID, starting with 0. Child layers' IDs are followed immediately by their parents. Siblings are numerated from the bottom to top.
        function MohoDoc:LayerAbsoluteID(layer)
        end
        ---Returns a layer by its current absolute ID
        ---Each layer in a scene has an unique absolute ID, starting with 0. Child layers' IDs are followed immediately by their parents. Siblings are numerated from the bottom to top. Moho does not currently provide a count of ALL layers in a document. To iterate over all layers, start at layer 0 and continue until moho does not return a layer.
        --- | Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@return MohoLayer
        function MohoDoc:LayerByAbsoluteID(id)
        end
        ---Finds a root level layer by name. Layer in a folder can not be found.
        --- | Ver: < 9.5
        ---@param name string Ver: < 9.5
        ---@return MohoLayer
        function MohoDoc:LayerByName(name)
        end
        ---Returns document's top level layer's ID
        ---Any deeper layer (i.e. layers inside groups) will return -1.
        --- | Ver: < 9.5
        ---@param layer MohoLayer Ver: < 9.5
        ---@return number
        function MohoDoc:LayerID(layer)
        end
        ---Ver: 10.0
        ---@return LM_Message
        function MohoDoc:Metadata()
        end
        ---Ver: < 9.5
        ---@return string
        function MohoDoc:Name()
        end
        ---Ver: < 9.5
        ---@return number
        function MohoDoc:NoiseGrain()
        end
        ---returns the full path for the current document
        ---If the document has not been saved, the returned path is a string with zero length. If the document has been saved, the full path is returned. an LM_SecureAccess block is also returned  (e.g.) path, access = MohoDoc:Path(outAccess)
        --- | Ver: < 9.5
        ---@param outAccess LM_SecureAccess|nil Default: NULL | Ver: < 9.5
        ---@return string --If the document has not been saved, the returned path is a string with zero length. If the document has been saved, the full path is returned. an LM_SecureAccess block is also returned  (e.g.) path, access = MohoDoc:Path(outAccess)
        function MohoDoc:Path(outAccess)
        end
        ---	Marks an Undo point for multiple selected layers.
        ---Ver: 11.0
        ---@param shallow boolean|nil Default: false | Ver: 11.0
        ---@return nil
        function MohoDoc:PrepMultiUndo(shallow)
        end
        ---This method accepts multiple input formats: ColorVector:Set(0-1, 0-1, 0-1) | ColorVector:Set(0-1, 0-1, 0-1, 0-1) | ColorVector:Set(rgb_color) | ColorVector:Set(ColorVector)
        ---Mark a point in editing that the user can "undo" to return to. NOTE: The original Moho 5.6 documentation stated that passing nil as the argument marks a forthcoming operation as "not undoable". However, it looks like in modern versions, perhaps from version 11, passing nil works for the entire document, not a single layer.
        --- | Ver: < 9.5 | Ver: 12.2
        ---@param arg1 string Ver: 12.2
        ---@param arg2 MohoLayer The layer that is about to be modified | Ver: < 9.5
        ---@param arg3 boolean|nil Default: false | Ver: 11.0
        ---@param arg4 boolean|nil Default: false | Ver: 14.0
        ---@return nil
        function MohoDoc:PrepUndo(arg1, arg2, arg3, arg4)
        end
        ---Ver: < 9.5
        ---@return nil
        function MohoDoc:Redo()
        end
        ---Ver: 9.5
        ---@return nil
        function MohoDoc:Refresh()
        end
        ---Ver: < 9.5
        ---@param layer MohoLayer|nil Default: nil | Ver: < 9.5
        ---@return nil
        function MohoDoc:RelinkStyles(layer)
        end
        ---Ver: < 9.5
        ---@param style M_Style Ver: < 9.5
        ---@param layer MohoLayer|nil Default: nil | Ver: < 9.5
        ---@return nil
        function MohoDoc:RemoveStyle(style, layer)
        end
        ---Ver: < 9.5
        ---@param oldName string Ver: < 9.5
        ---@param newName string Ver: < 9.5
        ---@param layer MohoLayer|nil Default: nil | Ver: < 9.5
        ---@return nil
        function MohoDoc:RenameStyle(oldName, newName, layer)
        end
        ---Ver: < 9.5
        ---@param b boolean Ver: < 9.5
        ---@return nil
        function MohoDoc:SetAllow3D(b)
        end
        ---Ver: < 9.5
        ---@param col rgb_color Ver: < 9.5
        ---@return nil
        function MohoDoc:SetBackCol(col)
        end
        ---Ver: 10.0
        ---@param poseName string Use nil to deactivate the current action (return to Mainline) | Ver: 10.0
        ---@return nil
        function MohoDoc:SetCurrentDocAction(poseName)
        end
        ---Marks the document as "dirty" or modified, prompting the user to save if they try to quit.
        --- | Ver: < 9.5
        ---@return nil
        function MohoDoc:SetDirty()
        end
        ---Sets the document's end frame.
        --- | Ver: < 9.5
        ---@param frame number Document end frame | Ver: < 9.5
        ---@return nil
        function MohoDoc:SetEndFrame(frame)
        end
        ---Ver: < 9.5
        ---@param fps number Ver: < 9.5
        ---@return nil
        function MohoDoc:SetFps(fps)
        end
        ---Ver: < 9.5
        ---@param ng number Ver: < 9.5
        ---@return nil
        function MohoDoc:SetNoiseGrain(ng)
        end
        ---Set's the pixel width and height of the final rendered output.
        --- | Ver: < 9.5
        ---@param w number Width | Ver: < 9.5
        ---@param h number Height | Ver: < 9.5
        ---@return nil
        function MohoDoc:SetShape(w, h)
        end
        ---Sets the document's start frame.
        --- | Ver: < 9.5
        ---@param frame number Document start frame | Ver: < 9.5
        ---@return nil
        function MohoDoc:SetStartFrame(frame)
        end
        ---Ver: < 9.5
        ---@param ss number Ver: < 9.5
        ---@return nil
        function MohoDoc:SetStereoSeparation(ss)
        end
        ---Ver: 12.0
        ---@param comments string Ver: 12.0
        ---@return nil
        function MohoDoc:SetUserComments(comments)
        end
        ---Ver: 12.0
        ---@param tags string Ver: 12.0
        ---@return nil
        function MohoDoc:SetUserTags(tags)
        end
        ---Returns the document's start frame (typically 1).
        --- | Ver: < 9.5
        ---@return number --document start frame
        function MohoDoc:StartFrame()
        end
        ---Ver: < 9.5
        ---@return number
        function MohoDoc:StereoSeparation()
        end
        ---	Strips comments from document and all layers
        ---Ver: 12.0
        ---@return nil
        function MohoDoc:StripUserComments()
        end
        ---	Strips tags from document and all layers
        ---Ver: 12.0
        ---@return nil
        function MohoDoc:StripUserTags()
        end
        ---	This method is working as specified when called with a character string
        --- | Ver: < 9.5
        ---@param styleName string Ver: < 9.5
        ---@return M_Style
        function MohoDoc:Style(styleName)
        end
        ---Ver: 12.0
        ---@param id number Ver: 12.0
        ---@return M_Style
        function MohoDoc:StyleByID(id)
        end
        ---Ver: < 9.5
        ---@return nil
        function MohoDoc:Undo()
        end
        ---Ver: 12.0
        ---@return string
        function MohoDoc:UserComments()
        end
        ---Ver: 12.0
        ---@return string
        function MohoDoc:UserTags()
        end
        ---Returns the pixel width of the final rendered output.
        --- | Ver: < 9.5
        ---@return number --pixel width of the final rendered output
        function MohoDoc:Width()
        end
    ---@class MohoGlobals  
    ---@field AutoFreezeKeys boolean --Added in version 12 | Ver: 12.0
    ---@field AutoLockKeys boolean --Ver: < 9.5
    ---@field AutoNaming boolean --Ver: < 9.5
    ---@field BackCol rgb_color --The "Background" color set in Editor Colors preferences | Ver: < 9.5
    ---@field BeginnerMode boolean --Ver: < 9.5
    ---@field CameraBoundsCol rgb_color --Added in version 9.5 | Ver: < 9.5
    ---@field CameraShadeCol rgb_color --Added in version 9.5 | Ver: < 9.5
    ---@field CameraTargetCol rgb_color --Added in version 9.5 | Ver: < 9.5
    ---@field CharWizLineWidth number --Ver: < 9.5
    ---@field ConfirmRevertDocument boolean --Added in version 10. Removed in versin 14.0 | Ver: 10.0
    ---@field ConstructionCurvesOn boolean --Ver: < 9.5
    ---@field CurveInterpretation number --Ver: < 9.5
    ---@field DefaultInterp number --Ver: < 9.5
    ---@field DisableAudioFeedback boolean --Ver: < 9.5
    ---@field DisableDrawingToolsNonZero boolean --Ver: < 9.5
    ---@field EditMultipleKeys boolean --Added in version 9.5 | Ver: < 9.5
    ---@field ElemCol rgb_color --The "Object" color set in Editor Colors preferences | Ver: < 9.5
    ---@field EnableBoneDynamics boolean --Added in version 10.1 | Ver: 10.1
    ---@field FBXPreviewMode boolean --Added in version 10.1 | Ver: 10.1
    ---@field FillCol rgb_color --The "Default fill" color set in Layer/Objects preferences | Ver: < 9.5
    ---@field FrameSkipping boolean --Ver: < 9.5
    ---@field GapFilling boolean --Ver: < 9.5
    ---@field GridCol rgb_color --Added in version 9.5 | Ver: < 9.5
    ---@field GridOn boolean --Ver: < 9.5
    ---@field GridSize number --Ver: < 9.5
    ---@field GridSnapping boolean --Ver: < 9.5
    ---@field GuideCol rgb_color --Added in version 9.5 | Ver: < 9.5
    ---@field HideControlledBones boolean --Removed in version 10 | Ver: < 9.5
    ---@field HideOffscreen boolean --Ver: < 9.5
    ---@field HighlightSelectedShapes boolean --Added in version 9.5 | Ver: < 9.5
    ---@field ImageSamplingMode number --Ver: < 9.5
    ---@field InacCol rgb_color --The "Inactive" color set in Editor Colors preferences | Ver: < 9.5
    ---@field InlineLayerNaming boolean --Ver: < 9.5
    ---@field InsertText InsertTextParams --Ver: < 9.5
    ---@field LineCol rgb_color --The "Default stroke" color set in Layer/Objects preferences | Ver: < 9.5
    ---@field MarkZero boolean --Ver: < 9.5
    ---@field MingSplitWidth number --Added in version 9.5 | Ver: < 9.5
    ---@field MingViewMode number --0 director, 1 camera, 2 panel | Ver: < 9.5
    ---@field MuteSoundtrack boolean --Ver: < 9.5
    ---@field NewShapeProperties M_Shape --Ver: < 9.5
    ---@field NTSCSafe boolean --Ver: < 9.5
    ---@field PlayEnd number --Added in version 10.1 | Ver: 10.1
    ---@field PlayStart number --Added in version 10.1 | Ver: 10.1
    ---@field PreferredFormat number --Ver: < 9.5
    ---@field ProjAntialiasing boolean --Added in version 12 | Ver: 12.0
    ---@field ProjBackCol rgb_color --The "Background Color" for current project | Ver: < 9.5
    ---@field ProjDepthSort boolean --Ver: < 9.5
    ---@field ProjDistanceSort boolean --Ver: < 9.5
    ---@field ProjDofBlur number --Ver: < 9.5
    ---@field ProjDofDistance number --Ver: < 9.5
    ---@field ProjDofEnabled boolean --Ver: < 9.5
    ---@field ProjDofRange number --Ver: < 9.5
    ---@field ProjEnable3D boolean --Ver: < 9.5
    ---@field ProjEnd number --Ver: < 9.5
    ---@field ProjFPS number --Ver: < 9.5
    ---@field ProjHeight number --Ver: < 9.5
    ---@field ProjNoise number --Ver: < 9.5
    ---@field ProjPixelation number --Added in version 12 | Ver: 12.0
    ---@field ProjRenderStyleFill number --Ver: < 9.5
    ---@field ProjRenderStyleLayer number --Ver: < 9.5
    ---@field ProjRenderStyleLine number --Ver: < 9.5
    ---@field ProjStart number --Ver: < 9.5
    ---@field ProjWidth number --Ver: < 9.5
    ---@field QualityFlags number --Ver: < 9.5
    ---@field RescaleSoundtrack boolean --Ver: < 9.5
    ---@field ScaleCompensation boolean --Ver: < 9.5
    ---@field SelCol rgb_color --The "Selection" color set in Editor Colors preferences | Ver: < 9.5
    ---@field SelectedShapeCheckerboard boolean --Added in version 9.5 | Ver: < 9.5
    ---@field ShowDocumentTabs boolean --Added in version 10 | Ver: 10.0
    ---@field ShowPlayContolsAndViewSettings boolean --Added in version 10.1 | Ver: 10.1
    ---@field TopStatusBar boolean --Ver: < 9.5
    ---@field UseDegreesInTimeline boolean --Ver: < 9.5
    ---@field UseMasterAnimChannel boolean --Ver: < 9.5
    ---@field UseSMPTE boolean --Ver: < 9.5
    ---@field ZoomBox boolean --Ver: < 9.5
    ---You can access these variables with 'MOHO.MohoGlobals' prefix, e.g. MOHO.MohoGlobals.GridOn
    MOHO.MohoGlobals = {}
    ---@class MohoLayerChannel 
    ---@field channelID number --Channel code, see Channel codes | Ver: < 9.5
    ---@field name String --Ver: < 9.5
    ---@field selectionBased boolean --Ver: < 9.5
    ---@field separableDimensions boolean --Added in version 12 | Ver: 12.0
    ---@field subChannelCount number --Ver: < 9.5
    ---Channel description, can be obtained using the MohoLayer:GetChannelInfo method.
    MohoLayerChannel = {}
    ---@class MohoLayerRef
    ---@field fDocPath string --Ver: 11.0
    ---@field fLayerUUID String --Ver: 11.0
    ---@field fSameDoc boolean --Ver: 11.0
    MohoLayerRef = {}
    ---@class MohoLayerRefSyncOptions
    ---@field fAddNewLayers boolean --Ver: 11.0
    ---@field fRemoveMissingLayers boolean --Ver: 11.0
    ---@field fReplaceMismatchedBones boolean --Ver: 11.0
    ---@field fReplaceMismatchedVectors boolean --Ver: 11.0
    MohoLayerRefSyncOptions = {}
    ---@class MohoPhysicsOptions
    ---@field fDensity number --Ver: < 9.5
    ---@field fEnableMotor boolean --Ver: < 9.5
    ---@field fForceObject boolean --Ver: < 9.5
    ---@field fForceVector AnimVec2 --Ver: < 9.5
    ---@field fFriction number --Ver: < 9.5
    ---@field fInitialVelocity Vector2 --Ver: < 9.5
    ---@field fMotorSpeed AnimVal --Ver: < 9.5
    ---@field fMotorTorque number --Ver: < 9.5
    ---@field fPhysicsEnabled boolean --Ver: < 9.5
    ---@field fPivotOnOrigin boolean --Ver: < 9.5
    ---@field fRespawnInterval number --Ver: < 9.5
    ---@field fRestitution number --Ver: < 9.5
    ---@field fSleeping boolean --Ver: < 9.5
    ---@field fStaticBody boolean --Ver: < 9.5
    MohoPhysicsOptions = {}
    ---@class MohoRenderStyle
    ---@field fFillStyle number       --Ver: < 9.5
    ---@field fLayerStyle number      --Ver: < 9.5
    ---@field fLineStyle number     --Ver: < 9.5
    MohoRenderStyle = {}
    ---@class MohoView
    ---The MohoView class is a script's interface to the Moho editing view.
    LM_View.MohoView = {}
        --- Causes the view to completely redraw the current scene.
        --- Moho scripts use this a lot after making changes to the contents of the document.
        --- | Ver: < 9.5
        ---@return nil
        function LM_View.MohoView:DrawMe()
        end
        --- Draws a "preview" shape by evaluating the selected points in the current vector layer as a shape, and drawing it in a highlighted mode. This is used by the Create Shape tool to preview a shape before the user hits the spacebar to finalize the shape.
        --- | Ver: < 9.5
        ---@param fill boolean|nil Default: true | Ver: 10.0
        ---@param outline boolean|nil Default: true | Ver: 10.0
        ---@return nil
        function LM_View.MohoView:DrawPreviewShape(fill, outline)
        end
        ---Ver: < 9.5
        ---@param where Point Ver: < 9.5
        ---@param tolerance number Ver: < 9.5
        ---@param skipImageEdges boolean|nil Default: true | Ver: 10.0
        ---@param curveEndExtensionDistance number|nil Default: 20 | Ver: 14.0
        ---@param useShapeStrokes boolean Default: false | Ver: 14.0
        ---@param expansionOnStrokes number|nil Default: 0 | Ver: 14.0
        ---@return nil
        function LM_View.MohoView:FloodSelect(where, tolerance, skipImageEdges, curveEndExtensionDistance, useShapeStrokes, expansionOnStrokes)
        end
        ---Ver: 10.0
        ---@return boolean
        function LM_View.MohoView:GPUMode()
        end
        ---Returns a graphics object to use for custom drawing in the view
        ---Ver: < 9.5
        ---@return XGraphics --a graphics drawing object
        function LM_View.MohoView:Graphics()
        end
        ---Ver: < 9.5
        ---@return boolean
        function LM_View.MohoView:IsStereoEnabled()
        end
        ---Ver: < 9.5
        ---@return boolean
        function LM_View.MohoView:IsStereoOn()
        end
        ---Ver: < 9.5
        ---@return boolean
        function LM_View.MohoView:IsTracingEnabled()
        end
        ---Ver: < 9.5
        ---@return boolean
        function LM_View.MohoView:IsTracingOn()
        end
        ---Ver: < 9.5
        ---@param where Point Ver: < 9.5
        ---@return nil
        function LM_View.MohoView:OrbitDown(where)
        end
        ---Ver: < 9.5
        ---@param where Point Ver: < 9.5
        ---@return nil
        function LM_View.MohoView:OrbitMoved(where)
        end
        ---Ver: < 9.5
        ---@param where Point Ver: < 9.5
        ---@return nil
        function LM_View.MohoView:OrbitUp(where)
        end
        ---Ver: < 9.5
        ---@param where Point Ver: < 9.5
        ---@return nil
        function LM_View.MohoView:PanDown(where)
        end
        ---Ver: < 9.5
        ---@param where Point Ver: < 9.5
        ---@return nil
        function LM_View.MohoView:PanMoved(where)
        end
        ---Ver: < 9.5
        ---@param where Point Ver: < 9.5
        ---@return nil
        function LM_View.MohoView:PanUp(where)
        end
        ---Picks a bone near the given pixel location.
        --- | Ver: < 9.5 | Ver: 12.0
        ---@param where Point Pixel location on the screen | Ver: < 9.5
        ---@param clickVec Vector2 Vector location of the click | Ver: < 9.5
        ---@param layer MohoLayer The bone layer you wish to pick from | Ver: < 9.5
        ---@param exact boolean True if only exact picks are accepted, false if the closest bone can be clicked even if not exact | Ver: < 9.5
        ---@param pickWidth number|nil Default: 3 | Ver: < 9.5
        ---@param ignoreBone number|nil Default: -1 | Ver: 12.0
        ---@return number --id of the picked bone
        function LM_View.MohoView:PickBone(where, clickVec, layer, exact, pickWidth, ignoreBone)
        end
        ---Picks an edge near the given pixel location.
        ---This method returns 2 values: 1: int32 curve ID 2: int32 segment ID
        --- | Ver: < 9.5
        ---@param where Point Pixel location on the screen | Ver: < 9.5
        ---@param curveID number Id of an M_Curve object in the current vector layer, returned by this function | Ver: < 9.5
        ---@param segID number Which segment on the curve was picked, returned by this function | Ver: < 9.5
        ---@param pickWidth number|nil Default: 3 | Ver: < 9.5
        ---@return number --curve ID
        ---@return number --segment ID
        function LM_View.MohoView:PickEdge(where, curveID, segID, pickWidth)
        end
        ---Ver: 12.0
        ---@param cpt1 Point Ver: 12.0
        ---@param cpt2 Point Ver: 12.0
        ---@param cpt3 Point Ver: 12.0
        ---@param cpt4 Point Ver: 12.0
        ---@param curveID number Ver: 12.0
        ---@param segID number Ver: 12.0
        ---@param skipMinSegID number Ver: 12.0
        ---@param skipMaxSegID number Ver: 12.0
        ---@param skipBeginning boolean Ver: 12.0
        ---@param skipEnd boolean Ver: 12.0
        ---@return Point
        function LM_View.MohoView:PickEdgeAlongCurve(cpt1, cpt2, cpt3, cpt4, curveID, segID, skipMinSegID, skipMaxSegID, skipBeginning, skipEnd)
        end
        ---Ver: < 9.5
        ---@param lineEnd1 Point Ver: < 9.5
        ---@param lineEnd2 Point Ver: < 9.5
        ---@param curveID number Ver: < 9.5
        ---@param segID number Ver: < 9.5
        ---@param skipMinSegID number Ver: < 9.5
        ---@param skipMaxSegID number Ver: < 9.5
        ---@return Point
        function LM_View.MohoView:PickEdgeAlongLine(lineEnd1, lineEnd2, curveID, segID, skipMinSegID, skipMaxSegID)
        end
        ---Ver: < 9.5
        ---@param where Point Ver: < 9.5
        ---@param pickWidth number|nil Default: 3 | Ver: < 9.5
        ---@return number
        function LM_View.MohoView:PickFace(where, pickWidth)
        end
        ---This method returns 3 values: 1: MohoLayer Layer 2: int32 Curve ID 3: int32 Segment ID
        --- | Ver: < 9.5
        ---@param where Point Ver: < 9.5
        ---@param curveID number Ver: < 9.5
        ---@param segID number Ver: < 9.5
        ---@param pickWidth number|nil Default: 3 | Ver: < 9.5
        ---@return MohoLayer
        ---@return number
        ---@return number
        function LM_View.MohoView:PickGlobalEdge(where, curveID, segID, pickWidth)
        end
        ---Ver: < 9.5
        ---@param where Point Ver: < 9.5
        ---@param pickWidth number|nil Default: 3 | Ver: < 9.5
        ---@return MohoLayer
        function LM_View.MohoView:PickGlobalLayer(where, pickWidth)
        end
        ---Ver: < 9.5
        ---@param where Point Ver: < 9.5
        ---@param noHigherThan number|nil Default: -1 | Ver: < 9.5
        ---@param pickWidth number|nil Default: 3 | Ver: < 9.5
        ---@return M_Shape
        function LM_View.MohoView:PickGlobalShape(where, noHigherThan, pickWidth)
        end
        ---Picks a point near the given pixel location.
        --- | Ver: < 9.5
        ---@param where Point Pixel location on the screen | Ver: < 9.5
        ---@param pickWidth number|nil Default: 3 | Ver: < 9.5
        ---@return number --id of an M_Point object in the current vector layer
        function LM_View.MohoView:PickPoint(where, pickWidth)
        end
        ---Picks a shape near the given pixel location.
        --- | Ver: < 9.5
        ---@param where Point Pixel location on the screen | Ver: < 9.5
        ---@param noHigherThan number|nil Id of the highest shape to pick (to allow picking lower, hidden shapes), -1 to ignore this option | Default: -1 | Ver: < 9.5
        ---@param pickWidth number|nil Default: 3 | Ver: < 9.5
        ---@return number --id of an M_Shape in the current vector layer
        function LM_View.MohoView:PickShape(where, noHigherThan, pickWidth)
        end
        ---Converts a pixel location to a 2D vector location, given a specific transformation matrix. This is used to convert between mouse coordinates and 2D layer coordinates.
        --- | Ver: < 9.5
        ---@param where Point Pixel location on the screen | Ver: < 9.5
        ---@param layerM Matrix|nil The transformation matrix for the layer in question | Default: NULL | Ver: < 9.5
        ---@return Vector2 --2D vector point in a layer's coordinate system
        function LM_View.MohoView:Point2Vec(where, layerM)
        end
        ---Get the current display quality of the viewport
        ---The return value is a sum of the Display Quality constants. For example, if the value is 12, it means that only the fills and strokes are enabled (12 = 4 + 8, which is MOHO.LDQ_FILLS + MOHO.LDQ_OUTLINES).
        --- | Ver: < 9.5
        ---@return number --Sum of Display Quality constants
        function LM_View.MohoView:QualityFlags()
        end
        ---Ver: < 9.5
        ---@return nil
        function LM_View.MohoView:RefreshView()
        end
        ---Reset or zoom in on the view.
        ---Ver: < 9.5
        ---@param mode number|nil 0:zoom in on all the points in the layer, 1:zoom in on all the selected points in the layer, 2:zoom out to default view | Default: 2 | Ver: < 9.5
        ---@return nil
        function LM_View.MohoView:ResetView(mode)
        end
        ---Ver: < 9.5
        ---@param where Point Ver: < 9.5
        ---@return nil
        function LM_View.MohoView:RotateDown(where)
        end
        ---Ver: < 9.5
        ---@param where Point Ver: < 9.5
        ---@return nil
        function LM_View.MohoView:RotateMoved(where)
        end
        ---Ver: < 9.5
        ---@param where Point Ver: < 9.5
        ---@return nil
        function LM_View.MohoView:RotateUp(where)
        end
        ---Ver: 9.5
        ---@param where Point Ver: 9.5
        ---@return rgb_color
        function LM_View.MohoView:SampleColor(where)
        end
        ---Set the display quality of the viewport
        ---The argument is a sum of the Display Quality constants. For example, if you need to only enable fills and strokes, use the following: moho.view:SetQualityFlags(MOHO.LDQ_FILLS + MOHO.LDQ_OUTLINES)
        ---Ver: < 9.5
        ---@param qualityFlags number Sum of Display Quality constants | Ver: < 9.5
        ---@return nil
        function LM_View.MohoView:SetQualityFlags(qualityFlags)
        end
        ---Ver: < 9.5
        ---@param path string Ver: < 9.5
        ---@return nil
        function LM_View.MohoView:SetTracingImage(path)
        end
        ---Ver: < 9.5
        ---@param b boolean Ver: < 9.5
        ---@return nil
        function LM_View.MohoView:TurnStereoOn(b)
        end
        ---Ver: < 9.5
        ---@param b boolean Ver: < 9.5
        ---@return nil
        function LM_View.MohoView:TurnTracingOn(b)
        end
        ---Ver: < 9.5
        ---@param where Point Ver: < 9.5
        ---@return nil
        function LM_View.MohoView:ZoomDown(where)
        end
        ---Ver: < 9.5
        ---@param where Point Ver: < 9.5
        ---@return nil
        function LM_View.MohoView:ZoomMoved(where)
        end
        ---Ver: < 9.5
        ---@param where Point Ver: < 9.5
        ---@return nil
        function LM_View.MohoView:ZoomUp(where)
        end
    ---@class MouseEvent  
    ---@field altKey boolean --True if the alt key is down, otherwise false. | Ver: < 9.5
    ---@field ctrlKey boolean --True if the ctrl key is down, otherwise false. | Ver: < 9.5
    ---@field doubleClick boolean --Ver: < 9.5
    ---@field drawingStartVec Vector2 --Added in version 11 | Ver: 11.0
    ---@field drawingVec Vector2 --Added in version 11 | Ver: 11.0
    ---@field eraser boolean --Added in version 10 | Ver: 10.0
    ---@field penPressure number --The current pen pressure (if a drawing tablet is being used). Pen pressure can vary from 0.0 to 1.0. If the user is working with a mouse, and not a drawing tablet, the pen pressure will always be 0.0. | Ver: < 9.5
    ---@field pt Point --The pixel location of the current mouse event. | Ver: < 9.5
    ---@field shiftKey boolean --True if the shift key is down, otherwise false. | Ver: < 9.5
    ---@field startPt Point --The pixel location of the initial mouse down event. | Ver: < 9.5
    ---@field startVec Vector2 --The 2D layer location of the initial mouse down event. | Ver: < 9.5
    ---@field vec Vector2 --The 2D layer location of the current mouse event. | Ver: < 9.5
    ---@field view MohoView --A reference to the Moho editing view. | Ver: < 9.5
    ---The MouseEvent class is provided to your script when a mouse event occurs. It contains information about the event.
    MouseEvent = {}
    ---@class M_Bone  
    ---@field fAngle number --the bone's current angle | Ver: < 9.5
    ---@field fAngleControlDelay number --Added in version 14.0 | Ver: 14.0
    ---@field fAngleControlParent number --an id number to the bone's angle control bone (-1 if the bone has no angle control) | Ver: < 9.5
    ---@field fAngleControlScale number --the scale value for the bone's angle control | Ver: < 9.5
    ---@field fAngleDampingForce number --Added in version 14.0 to replace fDampingForce | Ver: 14.0
    ---@field fAngleDynamics boolean --Added in version 14.0 | Ver: 14.0
    ---@field fAngleSpringForce number --Added in version 14.0 to replace fSpringForce | Ver: 14.0
    ---@field fAngleTorqueForce number --Added in version 14.0 to replace fTorqueForce | Ver: 14.0
    ---@field fAnimAngle AnimVal --the bon'e animated angle | Ver: < 9.5
    ---@field fAnimParent AnimVal --the bon'e animated parent ID | Ver: 11.0
    ---@field fAnimPos AnimVec2 --the bone's animated position | Ver: < 9.5
    ---@field fAnimScale AnimVal --the bone's animated scale | Ver: < 9.5
    ---@field fBoneDynamics AnimBool --true if bone dynamics are on for this bone | Ver: < 9.5
    ---@field fConstraints boolean --true if the bone has min/max angle constraints, otherwise false | Ver: < 9.5
    ---@field fDampingForce number --the influence of damping on bone dynamics | Ver: < 9.5
    ---@field fFixedAngle boolean --true if the bone's Independent Angle toggle is on | Ver: 10.0
    ---@field fFlipH AnimBool --the bone's animated end flip | Ver: 11.0
    ---@field fFlipV AnimBool --the bone's animated side flip | Ver: 11.0
    ---@field fHidden boolean --true if bone is hidden | Ver: 10.0
    ---@field fIgnoredByIK boolean --true if bone is ignored by IK | Ver: 12.0
    ---@field fIKGlobalAngle AnimVal --the bone's IK angle animation channel | Ver: < 9.5
    ---@field fIKLock AnimBool --the bone's IK lock animation channel | Ver: < 9.5
    ---@field fIKParentTarget AnimVec2 --the bone's IK target animation channel | Ver: < 9.5
    ---@field fLength number --the bone's length | Ver: < 9.5
    ---@field fMaxAutoScaling number --the bone's Maximum IK Stretching value | Ver: 10.0
    ---@field fMaxConstraint number --maximum angle | Ver: < 9.5
    ---@field fMinConstraint number --minimum angle | Ver: < 9.5
    ---@field fMovedMatrix Matrix --the transformation matrix for the bone's current alignment | Ver: < 9.5
    ---@field fName String --the name of the bone | Ver: < 9.5
    ---@field fOffset Vector2 --the bone's 2D offset value | Ver: < 9.5
    ---@field fParent number --an id number to the bone's parent bone (-1 if the bone has no parent) | Ver: < 9.5
    ---@field fPhysicsLockTip boolean --Ver: < 9.5
    ---@field fPhysicsMotorSpeed AnimVal --Ver: < 9.5
    ---@field fPhysicsRadius number --Ver: < 9.5
    ---@field fPhysicsReturnToZero boolean --Ver: < 9.5
    ---@field fPhysicsTorque number --Ver: < 9.5
    ---@field fPos Vector2 --the bone's current position | Ver: < 9.5
    ---@field fPosControlDelay number --Added in version 14.0 | Ver: 14.0
    ---@field fPosControlParent number --an id number to the bone's position control bone (-1 if the bone has no position control) | Ver: < 9.5
    ---@field fPosControlScale Vector2 --the scale value for the bone's position control | Ver: < 9.5
    ---@field fPosDampingForce number --Added in version 14.0 | Ver: 14.0
    ---@field fPosDynamics boolean --Added in version 14.0 | Ver: 14.0
    ---@field fPosSpringForce number --Added in version 14.0 | Ver: 14.0
    ---@field fPosTorqueForce number --Added in version 14.0 | Ver: 14.0
    ---@field fPtMatrix Matrix --the transformation matrix for the influence the bone has over vector points | Ver: < 9.5
    ---@field fRestMatrix Matrix --the transformation matrix for the bone at rest | Ver: < 9.5
    ---@field fScale number --the bone's current scale | Ver: < 9.5
    ---@field fScaleControlDelay number --Added in version 14.0 | Ver: 14.0
    ---@field fScaleControlParent number --an id number to the bone's scale control bone (-1 if the bone has no scale control) | Ver: < 9.5
    ---@field fScaleControlScale number --the scale value for the bone's scale control | Ver: < 9.5
    ---@field fScaleDampingForce number --Added in version 14.0 | Ver: 14.0
    ---@field fScaleDynamics boolean --Added in version 14.0 | Ver: 14.0
    ---@field fScaleSpringForce number --Added in version 14.0 | Ver: 14.0
    ---@field fScaleTorqueForce number --Added in version 14.0 | Ver: 14.0
    ---@field fScalingMode number --Added in version 10. Values: 0 - manual, 1 - two-dimensional, 2 - squash/stretch | Ver: 10.0
    ---@field fSelected boolean --true if the bone is selected, otherwise false | Ver: < 9.5
    ---@field fShy boolean --true if the bone is a shy bone | Ver: 10.0
    ---@field fSpringForce number --the influence of springiness on bone dynamics | Ver: < 9.5
    ---@field fSquashStretchScaling number --the bone's Squash-Stretch Scaling factor | Ver: 10.0
    ---@field fStrength number --the bone's strength | Ver: < 9.5
    ---@field fTargetBone AnimVal --ID of the current target bone. Added in version 10 | Ver: 10.0
    ---@field fTempAngle number --a temporary variable to store the angle | Ver: < 9.5
    ---@field fTempLength number --a temporary variable to store the length | Ver: < 9.5
    ---@field fTempPos Vector2 --a temporary variable to store the position | Ver: < 9.5
    ---@field fTempScale number --a temporary variable to store the scale | Ver: < 9.5
    ---@field fTorqueForce number --the influence of torque on bone dynamics | Ver: < 9.5
    ---@field fWindDynamics boolean --Ver: 13.5.2
    ---The M_Bone class represents one bone in a skeleton.
    M_Bone = {}
        ---Ver: 14.0
        ---@return number
        function M_Bone:AngleWeight()
        end
        ---Ver: 14.0
        ---@return boolean
        function M_Bone:AreDynamicsActive()
        end
        ---Ver: 11.0
        ---@return nil
        function M_Bone:DeselectKeys()
        end
        ---	Tells scripts how wide a particular bone will be displayed on screen. Takes into account the scaling of the layer, overall canvas zoom, etc.
        ---In 13.5.4 the width can be adjusted by the user in preferences, and this function will reflect that.
        --- | Ver: 13.5.3
        ---@param displayScale number Ver: 14.0
        ---@return number
        function M_Bone:DisplayWidth(displayScale)
        end
        ---Ver: 11.0
        ---@param b boolean Ver: 11.0
        ---@return nil
        function M_Bone:EnableArcSolver(b)
        end
        ---Ver: 11.0
        ---@return boolean
        function M_Bone:HasSelectedKeys()
        end
        ---Ver: 11.0
        ---@return boolean
        function M_Bone:IsArcSolverEnabled()
        end
        ---Ver: 13.5.2
        ---@return boolean
        function M_Bone:IsGroupVisible()
        end
        ---Ver: 9.5
        ---@return boolean
        function M_Bone:IsLabelShowing()
        end
        ---Ver: 12.0
        ---@return boolean
        function M_Bone:IsZeroLength()
        end
        ---Returns the name of the bone.
        --- | Ver: < 9.5
        ---@return string --the name of the bone
        function M_Bone:Name()
        end
        ---Ver: 11.0
        ---@return number
        function M_Bone:ParentalFlipFactor()
        end
        ---Ver: 14.0
        ---@return number
        function M_Bone:PosWeight()
        end
        ---Ver: 14.0
        ---@return number
        function M_Bone:ScaleWeight()
        end
        ---Ver: 14.0
        ---@param w number Ver: 14.0
        ---@return nil
        function M_Bone:SetAngleWeight(w)
        end
        ---Sets the name of the bone.
        --- | Ver: < 9.5
        ---@param name string Ver: < 9.5
        ---@return nil
        function M_Bone:SetName(name)
        end
        ---Ver: 14.0
        ---@param w number Ver: 14.0
        ---@return nil
        function M_Bone:SetPosWeight(w)
        end
        ---Ver: 14.0
        ---@param w number Ver: 14.0
        ---@return nil
        function M_Bone:SetScaleWeight(w)
        end
        ---Ver: 9.5
        ---@param tags number Bone color IDs Ver: < 9.5
        ---@return nil
        function M_Bone:SetTags(tags)
        end
        ---Ver: < 9.5
        ---@param b boolean Ver: < 9.5
        ---@return nil
        function M_Bone:ShowLabel(b)
        end
        ---Bone color identifier
        --- | Ver: 9.5
        ---@return number --Bone color identifier
        function M_Bone:Tags()
        end
    ---@class M_BoneGroup  
    ---@field fActiveBone AnimVal --Ver: 13.5.2
    ---@field fEnabled boolean --Ver: 13.5.2
    ---Vitruvian bone group  
    --- Version added: 13.5.2
    M_BoneGroup = {}
        ---Ver: 13.5.2
        ---@return M_Bone
        function M_BoneGroup:ActiveBone()
        end
        ---Ver: 13.5.2
        ---@param boneID number Ver: 13.5.2
        ---@return nil
        function M_BoneGroup:AddBone(boneID)
        end
        ---Ver: 13.5.2
        ---@param id number Ver: 13.5.2
        ---@return M_Bone
        function M_BoneGroup:Bone(id)
        end
        ---Ver: 13.5.2
        ---@param boneID number Ver: 13.5.2
        ---@return boolean
        function M_BoneGroup:ContainsBoneID(boneID)
        end
        ---Ver: 13.5.2
        ---@return number
        function M_BoneGroup:CountBones()
        end
        ---Ver: 13.5.2
        ---@return string
        function M_BoneGroup:Name()
        end
        ---Ver: 13.5.2
        ---@param boneID number Ver: 13.5.2
        ---@return nil
        function M_BoneGroup:RemoveBone(boneID)
        end
        ---Ver: 13.5.2
        ---@param name string Ver: 13.5.2
        ---@return nil
        function M_BoneGroup:SetName(name)
        end
    ---@class M_Curve  
    ---@field fClosed boolean --true if the curve is closed, otherwise false | Ver: < 9.5
    ---@field fEndPercent AnimVal --This is actually end ratio in the range 0 to 1 inclusive - 0 is none and 1 is total | Ver: < 9.5
    ---@field fProfileOffset AnimVal --Added in version 14.2 | Ver: 14.2
    ---@field fProfileRepeat number --Ver: < 9.5
    ---@field fStartPercent AnimVal --This is actually end ratio in the range 0 to 1 inclusive - 0 is none and 1 is total | Ver: < 9.5
    ---The M_Curve class represents a curve in a vector layer.
    M_Curve = {}
        ---Ver: 12.0
        ---@param ptID number Ver: 12.0
        ---@param frame number Ver: 12.0
        ---@param prePoint boolean Ver: 12.0
        ---@return nil
        function M_Curve:AimControlHandleAtNeighbor(ptID, frame, prePoint)
        end
        ---Ver: < 9.5 | Ver: 12.0
        ---@param segID number Ver: < 9.5
        ---@param vec Vector2 Ver: < 9.5
        ---@param forDrawing boolean|nil Default: false | Ver: < 9.5
        ---@param extraPrecision boolean|nil Default: false | Ver: 12.0
        ---@return Vector2
        function M_Curve:ClosestPointOnSegment(segID, vec, forDrawing, extraPrecision)
        end
        ---Ver: 12.0
        ---@param ptID number Ver: 12.0
        ---@param frame number Ver: 12.0
        ---@param allFrames boolean|nil Default: false | Ver: 12.0
        ---@return nil
        function M_Curve:CorrectControlHandleAngles(ptID, frame, allFrames)
        end
        ---Returns the number of points that make up the curve.
        --- | Ver: < 9.5
        ---@return number --the number of points that make up the curve
        function M_Curve:CountPoints()
        end
        ---Returns the number of segments on the curve.
        ---A segment is a section of curve between two control points.
        --- | Ver: < 9.5
        ---@return number --the number of segments on the curve
        function M_Curve:CountSegments()
        end
        ---Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@return AnimVal
        function M_Curve:Curvature(id)
        end
        ---Ver: < 9.5
        ---@return number
        function M_Curve:CurveLength()
        end
        ---Ver: 14.0
        ---@param id number Ver: 14.0
        ---@param when number Ver: 14.0
        ---@return nil
        function M_Curve:DeleteCurvatureKey(id, when)
        end
        ---Ver: 11.0
        ---@return nil
        function M_Curve:DeselectCurvePoints()
        end
        ---Returns bezier handle absolute position, after applying all smartbones (just like fPos does for point position)
        --- | Ver: 12.0
        ---@param ptID number Ver: 12.0
        ---@param frame number Ver: 12.0
        ---@param prePoint boolean Ver: 12.0
        ---@return Vector2
        function M_Curve:GetControlHandle(ptID, frame, prePoint)
        end
        ---Returns the control points for the given segment id
        ---Curves created in Anime Studio 7 and later, curves are defined by a cubic Bézier function. (Curves created in Moho 6 and earlier use a different 'legacy curve' algorithm.)
        ---M_Curve:GetControlPoints() returns the four points P1, P2, P3 and P4 to define the path between the two mesh points that define a segment of the curve.
        ---The curve segment starts at P1 (where there is a mesh point) going toward P2 and arrives at P4 (where there is another mesh point) coming from the direction of P3.
        ---P2 and P3 are only there to provide directional information.
        --- | Ver: < 9.5
        ---@param segID number the segment in the curve (the id of the first segemnt is zero) Ver: < 9.5
        ---@param p1 Vector2 the location of the start of the segment Ver: < 9.5
        ---@param p2 Vector2 the position of the control point that controls the curvature of the segement from p1 Ver: < 9.5
        ---@param p3 Vector2 the position of the control point that controls the curvature of the segement to p4 Ver: < 9.5
        ---@param p4 Vector2 the location of the end of the segment Ver: < 9.5
        ---@param forDrawing boolean|nil Default: false | Ver: < 9.5
        ---@return nil
        function M_Curve:GetControlPoints(segID, p1, p2, p3, p4, forDrawing)
        end
        ---Returns the curvature through a given point on the curve.
        --- | Ver: < 9.5
        ---@param ptID number Which point on the curve Ver: < 9.5
        ---@param frame number At what time Ver: < 9.5
        ---@return number --curvature
        function M_Curve:GetCurvature(ptID, frame)
        end
        ---	Returns one of the handles' offsets through a given point on the curve
        ---Offset for given handle in radians from default 0 (0 is curve tangent in given point)
        ---Returns handle offset stored in CHANNEL_CURVE animation channel. Smartbones' influences are not included.
        --- | Ver: 12.0
        ---@param ptID number Ver: 12.0
        ---@param frame number Ver: 12.0
        ---@param prePoint boolean Ver: 12.0
        ---@return number --Offset for given handle in radians from default 0 (0 is curve tangent in given point) Returns handle offset stored in CHANNEL_CURVE animation channel. Smartbones' influences are not included.
        function M_Curve:GetOffset(ptID, frame, prePoint)
        end
        ---Ver: < 9.5
        ---@param percent number Ver: < 9.5
        ---@return Vector2
        function M_Curve:GetPercentLocation(percent)
        end
        ---Ver: < 9.5
        ---@param percent number Ver: < 9.5
        ---@return Vector2
        function M_Curve:GetPercentTangent(percent)
        end
        ---Returns startPercent and endPercent for given segment ID. Return values are from 0 to 1: 0 for the first curve's point, 1 for the last point, 0.5 for a point on a half of curve's length. From Moho 12.5 onwards (and potentially in earlier versions): this method also has a valid syntax:  startPercent, endPercent = M_Curve:GetSegmentRange(segID)
        --- | Ver: < 9.5
        ---@param segID number Ver: < 9.5
        ---@param startPercent number This argument is not required in Moho 12.5 and later. Ver: < 9.5
        ---@param endPercent number This argument is not required in Moho 12.5 and later. Ver: < 9.5
        ---@return number --Start percent
        ---@return number --End percent
        function M_Curve:GetSegmentRange(segID, startPercent, endPercent)
        end
        ---Returns one of the handles' offsets through a given point on the curve
        ---Factor to multiply common point's curvature to alter handle length for an individual handle | Returns handle weight stored in CHANNEL_CURVE animation channel. Smartbones' influences are not included.
        --- | Ver: 12.0
        ---@param ptID number Ver: 12.0
        ---@param frame number Ver: 12.0
        ---@param prePoint boolean Ver: 12.0
        ---@return number --handle weight
        function M_Curve:GetWeight(ptID, frame, prePoint)
        end
        ---	Are any points on this curve selected?
        ---A curve is partially selected if one or more points making up the curve are selected. Note that if all points are selected (M_Curve:IsSelected== true) that this routine will also return "true". Note also that a partially selected curve may have no segments selected.
        --- | Ver: < 9.5
        ---@return boolean
        function M_Curve:IsPartiallySelected()
        end
        ---Returns true if the given point is on the given curve segment.
        --- | Ver: < 9.5
        ---@param ptID number A point identifier in the underlying mesh | Ver: < 9.5
        ---@param segID number A segment of the curve (starting with 0) | Ver: < 9.5
        ---@return boolean
        function M_Curve:IsPointOnSegment(ptID, segID)
        end
        ---Tests whether a given curve segment is on or not. Segments that are "off" do not appear in rendered output.
        --- | Ver: < 9.5
        ---@param segID number Segment identifier | Ver: < 9.5
        ---@return boolean
        function M_Curve:IsSegmentOn(segID)
        end
        ---Tests whether a segment is selected. (A curve segment is considered selected if the points at either end of it are selected.)
        --- | Ver: < 9.5
        ---@param segID number A segment of the curve (starting with 0) | Ver: < 9.5
        ---@return boolean --true if the segment is selected, otherwise false
        function M_Curve:IsSegmentSelected(segID)
        end
        ---	Are all points on this curve selected?
        --- | Ver: < 9.5
        ---@return boolean
        function M_Curve:IsSelected()
        end
        ---Returns the M_Point object for the identified curve point. Note that the curve point numbers are not the same as mesh point numbers. In a curve, the points are numbered sequentially from start to end. It is possible that curve points 0, 1, 2, 3, ... map to mesh points 0, 5, 3, 4 ... or, indeed, any other sequence. If the curve is branched, a mesh point may appear more than once in a curve. e.g. curve points 0,1,2,3,4,5 could map to mesh points 0,3,1,2,3,4 (here, curve points 1 and 4 are the same mesh point) Also note that a mesh point may be part of more than one curve. (see class M_Point )
        --- | Ver: < 9.5
        ---@param id number Id number of the point | Ver: < 9.5
        ---@return M_Point --a point on the curve
        function M_Curve:Point(id)
        end
        ---	Curve point ID for a given point object
        --- | Ver: 12.0
        ---@param point M_Point Ver: 12.0
        ---@return number
        function M_Curve:PointID(point)
        end
        ---Returns the location of a point on a segment.
        --- | Ver: < 9.5
        ---@param segID number A segment of the curve (starting with 0) | Ver: < 9.5
        ---@param percent number Where on the segment to locate the point (from 0 to 1) | Ver: < 9.5
        ---@param forDrawing boolean|nil Default: false | Ver: < 9.5
        ---@return Vector2 --a point located on the segment
        function M_Curve:PointOnSegment(segID, percent, forDrawing)
        end
        --- | Ver: 12.0
        ---@param ptID number Ver: 12.0
        ---@param frame number Ver: 12.0
        ---@return nil
        function M_Curve:ResetControlHandles(ptID, frame)
        end
        --- | Ver: < 9.5
        ---@param segID number Ver: < 9.5
        ---@return number
        function M_Curve:SegmentLength(segID)
        end
        --- | Ver: 11.0
        ---@return nil
        function M_Curve:SelectCurvePoints()
        end
        ---	Sets position for one of two bezier handles through a given point on the curve
        ---This stores values in CHANNEL_CURVE animation channel, which have separate subchannels for Curvature, Weight and Offset. There are no special channels for handle position as Vector2, this argument is used to calculate another values. No matter syncAngles argument passed is true or false, curvature channel value would be set to 0.3
        --- | Ver: 12.0
        ---@param ptID number Ver: 12.0
        ---@param handle Vector2 Ver: 12.0
        ---@param frame number Ver: 12.0
        ---@param prePoint boolean Ver: 12.0
        ---@param syncAngles boolean Ver: 12.0
        ---@return nil
        function M_Curve:SetControlHandle(ptID, handle, frame, prePoint, syncAngles)
        end
        ---Sets the curvature through a given point on the curve.
        --- | Ver: < 9.5
        ---@param ptID number Which point on the curve | Ver: < 9.5
        ---@param curvature number Curvature | Ver: < 9.5
        ---@param frame number At what time | Ver: < 9.5
        ---@return nil
        function M_Curve:SetCurvature(ptID, curvature, frame)
        end
        ---	Sets offset for one of two bezier handles through a given point on the curve
        --- | Ver: 12.0
        ---@param ptID number Ver: 12.0
        ---@param offset number Ver: 12.0
        ---@param frame number Ver: 12.0
        ---@param prePoint boolean Ver: 12.0
        ---@return nil
        function M_Curve:SetOffset(ptID, offset, frame, prePoint)
        end
        --- | Ver: < 9.5
        ---@param layer MohoLayer Ver: < 9.5
        ---@param curve M_Curve Ver: < 9.5
        ---@return nil
        function M_Curve:SetProfileCurve(layer, curve)
        end
        ---Turns a curve segment on or off.
        --- | Ver: < 9.5
        ---@param segID number Segment identifier | Ver: < 9.5
        ---@param b boolean True to turn the segment on, false to turn it off | Ver: < 9.5
        ---@return nil
        function M_Curve:SetSegmentOn(segID, b)
        end
        ---Sets weight for one of two bezier handles through a given point on the curve
        --- | Ver: 12.0
        ---@param ptID number Ver: 12.0
        ---@param weight number Ver: 12.0
        ---@param frame number Ver: 12.0
        ---@param prePoint boolean Ver: 12.0
        ---@return nil
        function M_Curve:SetWeight(ptID, weight, frame, prePoint)
        end
    ---@class M_Face3D  
    ---@field groupTag number --Ver: < 9.5
    ---@field matID number --An index to the mesh material that should be used on this face. | Ver: < 9.5
    ---@field n1 number --Ver: < 9.5
    ---@field n2 number --Ver: < 9.5
    ---@field n3 number --Ver: < 9.5
    ---@field n4 number --Ver: < 9.5
    ---@field normal Vector3 --Ver: < 9.5
    ---@field numP number --The number of points in the face (can be 3 or 4). | Ver: < 9.5
    ---@field p1 number --An index to the first point in the mesh that makes up this face. | Ver: < 9.5
    ---@field p2 number --An index to the second point in the mesh. | Ver: < 9.5
    ---@field p3 number --An index to the third point in the mesh. | Ver: < 9.5
    ---@field p4 number --An index to the fourth point in the mesh. | Ver: < 9.5
    ---@field t1 number --Added in version 9.5 | Ver: 9.5
    ---@field t2 number --Added in version 9.5 | Ver: 9.5
    ---@field t3 number --Added in version 9.5 | Ver: 9.5
    ---@field t4 number --Added in version 9.5 | Ver: 9.5
    ---The M_Face3D class represents a face in a 3D mesh.
    M_Face3D = {}
    ---@class M_Material3D  
    ---@field color rgb_color --The color of the material. | Ver: < 9.5
    ---@field drawEdges boolean --Ver: < 9.5
    ---@field edgeColor rgb_color --Ver: < 9.5
    ---@field edgeWidth number --Ver: < 9.5
    ---@field name String --Ver: < 9.5
    ---@field opacityPath string --Ver: < 9.5
    ---@field selected boolean --Ver: < 9.5
    ---@field texturePath string --Ver: < 9.5
    ---The M_Material3D class represents the material properties for 3D mesh objects. For now, these properties are extremely basic.
    M_Material3D = {}
        --- | Ver: < 9.5
        ---@param outAccess LM_SecureAccess|nil Default: NULL | Ver: < 9.5
        ---@return string
        function M_Material3D:GetOpacityPath(outAccess)
        end
        --- | Ver: < 9.5
        ---@param outAccess LM_SecureAccess|nil Default: NULL | Ver: < 9.5
        ---@return string
        function M_Material3D:GetTexturePath(outAccess)
        end
        --- | Ver: < 9.5
        ---@param path string Ver: < 9.5
        ---@param copyFromAccess LM_SecureAccess|nil Default: NULL | Ver: < 9.5
        ---@return nil
        function M_Material3D:SetOpacityPath(path, copyFromAccess)
        end
        --- | Ver: < 9.5
        ---@param path string Ver: < 9.5
        ---@param copyFromAccess LM_SecureAccess|nil Default: NULL | Ver: < 9.5
        ---@return nil
        function M_Material3D:SetTexturePath(path, copyFromAccess)
        end
    ---@class M_Mesh  
    ---The M_Mesh class is a container for the actual vector shapes in a vector layer. It contains points, curves, and fill and outline shapes.
    M_Mesh = {}
        ---Add a new point group, made up of the currently selected points.
        --- | Ver: < 9.5
        ---@param name string Name for the new group | Ver: < 9.5
        ---@return nil
        function M_Mesh:AddGroup(name)
        end
        ---Adds a lone, unconnected point to the mesh. Lone points are not normally allowed in Moho, so this function must be followed by one or more calls to the AppendPoint function.
        --- | Ver: < 9.5
        ---@param pos Vector2 Location of the new point | Ver: < 9.5
        ---@param frame number Frame number | Ver: < 9.5
        ---@return nil
        function M_Mesh:AddLonePoint(pos, frame)
        end
        ---This method accepts multiple input formats:
        ---M_Mesh:AddPoint(pos, attachID, frame) | M_Mesh:AddPoint(pos, attachCurve, attachSeg, correctBezierHandles, frame, preserveCorners)
        ---Add a new point to the mesh. This function can behave in three ways: Start a new curve separate from any existing curve (attachID = -1); Insert a point into an existing curve (and thus splitting a segment) (attachID is not a curve endpoint). Extend an existing curve (attachID is a curve endpoint).
        ---Calling AddPoint after AddLonePoint appears to cause moho to crash (use AppendPoint to extend the curve). It does not appear to be possible to create a branched curve using this function alone (it seems to be necessary to create a new curve and weld it to an existing curve point).
        --- | Ver: < 9.5 | Ver: 12.0
        ---@param arg1 Vector2 Location of the new point | Ver: < 9.5
        ---@param arg2 number If -1, then start a new curve; otherwise attach the new point to this point id | Ver: < 9.5
        ---@param arg3 number|nil Curve segment id to attach to. Only valid when using the six-argument overload | Ver: < 9.5
        ---@param arg4 boolean|nil Default: true | Ver: 12.0
        ---@param arg5 number Frame number | Ver: < 9.5
        ---@param arg6 boolean|nil Default: false | Ver: 12.0
        ---@return nil
        function M_Mesh:AddPoint(arg1, arg2, arg3, arg4, arg5, arg6)
        end
        --- | Ver: 12.0
        ---@param curveID number Ver: 12.0
        ---@param frame number Ver: 12.0
        ---@param reductionEpsilon number|nil Default: 0.01f | Ver: 12.0
        ---@param curveEpsilon number|nil Default: 0.01f | Ver: 12.0
        ---@param splitThreshold number|nil Default: 0.1f | Ver: 12.0
        ---@return nil
        function M_Mesh:AdvancedCurveSimplification(curveID, frame, reductionEpsilon, curveEpsilon, splitThreshold)
        end
        --- | Ver: 11.0
        ---@return boolean
        function M_Mesh:AnimatedShapeOrder()
        end
        ---Adds a new point, connecting it with a curve to either the AddLonePoint point, or the last call to AppendPoint.
        --- | Ver: < 9.5
        ---@param pos Vector2 Location of the new point | Ver: < 9.5
        ---@param frame number Frame number | Ver: < 9.5
        ---@return nil
        function M_Mesh:AppendPoint(pos, frame)
        end
        ---Tests whether two points are adjacent on a curve.
        --- | Ver: < 9.5
        ---@param p1 number Point identifier | Ver: < 9.5
        ---@param p2 number Point identifier | Ver: < 9.5
        ---@return boolean --true if adjacent, otherwise false
        function M_Mesh:ArePointsAdjacent(p1, p2)
        end
        --- | Ver: 14.0
        ---@param shape M_Shape Ver: 14.0
        ---@param keepOriginals boolean Ver: 14.0
        ---@return number
        function M_Mesh:BakeCombinedShapes(shape, keepOriginals)
        end
        ---Completely empties a mesh of all points and curves.
        --- | Ver: < 9.5
        ---@return nil
        function M_Mesh:Clear()
        end
        ---Returns the closest point to the given location.
        --- | Ver: < 9.5 | Ver: 12.0
        ---@param pos Vector2 A 2D location in the layer | Ver: < 9.5
        ---@param ignoreID number|nil Point identifier - ignore this point when testing for the closest point | Default: -1 | Ver: < 9.5
        ---@param maxPointID number|nil Point identifier - ignore points with identifiers higher than this id | Default: -1 | Ver: < 9.5
        ---@param endpointsOnly boolean|nil Default: false | Ver: 12.0
        ---@return number --point identifier
        function M_Mesh:ClosestPoint(pos, ignoreID, maxPointID, endpointsOnly)
        end
        --- | Ver: 14.0
        ---@param shape1 M_Shape Ver: 14.0
        ---@param shape2 M_Shape Ver: 14.0
        ---@param comboMode number Ver: 14.0
        ---@param blendFactor number Ver: 14.0
        ---@param keepOriginals boolean Ver: 14.0
        ---@return number
        function M_Mesh:CombineShapes(shape1, shape2, comboMode, blendFactor, keepOriginals)
        end
        ---Returns the number of curves in the mesh.
        --- | Ver: < 9.5
        ---@return number --number of curves
        function M_Mesh:CountCurves()
        end
        ---Returns the number of point groups in the mesh.
        --- | Ver: < 9.5
        ---@return number --number of point groups
        function M_Mesh:CountGroups()
        end
        ---Returns the number of points in the mesh.
        --- | Ver: < 9.5
        ---@return number --number of points
        function M_Mesh:CountPoints()
        end
        ---Returns the number of shapes in the mesh.
        --- | Ver: < 9.5
        ---@return number --number of shapes
        function M_Mesh:CountShapes()
        end
        --- | Ver: 10.1
        ---@return number
        function M_Mesh:CountVisiblePoints()
        end
        ---Access a curve in the mesh.
        --- | Ver: < 9.5
        ---@param id number Curve identifier | Ver: < 9.5
        ---@return M_Curve --a curve object
        function M_Mesh:Curve(id)
        end
        --- | Ver: < 9.5
        ---@param curve M_Curve Ver: < 9.5
        ---@return number
        function M_Mesh:CurveID(curve)
        end
        ---Returns the curve interpretation type (legacy or v7+)
        ---See curve type constants . The setter method is M_Mesh : SetCurveInterpretation .
        --- | Ver: < 9.5
        ---@return number
        function M_Mesh:CurveInterpretation()
        end
        ---Delete an edge of a curve.
        --- | Ver: < 9.5 | Ver: 12.0
        ---@param curveID number Curve identifier | Ver: < 9.5
        ---@param segID number Which segment on the curve to delete | Ver: < 9.5
        ---@param frame number Frame number | Ver: < 9.5
        ---@param correctBezierHandles boolean|nil Default: true | Ver: 12.0
        ---@return nil
        function M_Mesh:DeleteEdge(curveID, segID, frame, correctBezierHandles)
        end
        ---Delete a point group from the mesh.
        --- | Ver: < 9.5
        ---@param name string Name of the group to delete | Ver: < 9.5
        ---@return nil
        function M_Mesh:DeleteGroup(name)
        end
        ---Deletes a point from the mesh.
        --- | Ver: < 9.5 | Ver: 14.0
        ---@param id number Point identifier | Ver: < 9.5
        ---@param preserveHandles boolean|nil Default: true | Ver: 14.0
        ---@return nil
        function M_Mesh:DeletePoint(id, preserveHandles)
        end
        --- | Ver: 12.0
        ---@return nil
        function M_Mesh:DeleteSelectedPoints()
        end
        ---Delete a fill or outline shape.
        --- | Ver: < 9.5
        ---@param id number Shape identifier | Ver: < 9.5
        ---@return nil
        function M_Mesh:DeleteShape(id)
        end
        ---	Deselects just points (Added in version 10)
        --- | Ver: 10.0
        ---@return nil
        function M_Mesh:DeselectPoints()
        end
        --- | Ver: 14.0
        ---@param on boolean Ver: 14.0
        ---@param guaranteedClean boolean|nil Default: false | Ver: 14.0
        ---@return nil
        function M_Mesh:EnableCleanup(on, guaranteedClean)
        end
        ---Access a point group in the mesh.
        --- | Ver: < 9.5
        ---@param id number Point group identifier | Ver: < 9.5
        ---@return M_PointGroup --a point group object
        function M_Mesh:Group(id)
        end
        --- | Ver: < 9.5
        ---@param group M_PointGroup Ver: < 9.5
        ---@return number
        function M_Mesh:GroupID(group)
        end
        --- | Ver: < 9.5
        ---@param curve M_Curve Ver: < 9.5
        ---@return number
        function M_Mesh:IsCurveValid(curve)
        end
        --- | Ver: 12.0
        ---@param shape M_Shape Ver: 12.0
        ---@return number
        function M_Mesh:IsShapeValid(shape)
        end
        ---Lower a shape in the stacking order.
        --- | Ver: < 9.5
        ---@param id number Shape identifier | Ver: < 9.5
        ---@param toBottom boolean True to lower it to the bottom, false to lower it one step | Ver: < 9.5
        ---@return nil
        function M_Mesh:LowerShape(id, toBottom)
        end
        --- | Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@param frame number Ver: < 9.5
        ---@return nil
        function M_Mesh:MakePointConsistentWithNeighbors(id, frame)
        end
        --- | Ver: 10.0
        ---@param shapeIDToMove number Ver: 10.0
        ---@param shapeIDFixed number Ver: 10.0
        ---@return nil
        function M_Mesh:PlaceShapeAbove(shapeIDToMove, shapeIDFixed)
        end
        --- | Ver: 10.0
        ---@param shapeIDToMove number Ver: 10.0
        ---@param shapeIDFixed number Ver: 10.0
        ---@return nil
        function M_Mesh:PlaceShapeBehind(shapeIDToMove, shapeIDFixed)
        end
        ---Access a point in the mesh.
        --- | Ver: < 9.5
        ---@param id number Point identifier | Ver: < 9.5
        ---@return M_Point --a point object
        function M_Mesh:Point(id)
        end
        --- | Ver: < 9.5
        ---@param point M_Point Ver: < 9.5
        ---@return number --id number of point or -1 if point is not in the mesh
        function M_Mesh:PointID(point)
        end
        --- | Ver: 12.0 | Ver: 14.0
        ---@param frame number Ver: 14.0
        ---@return nil
        function M_Mesh:PrepFixedHandles(frame)
        end
        ---	Call this before moving a group of points around
        ---Note that this method changes the fTempPos of all points in the mesh to their current fPos .
        --- | Ver: < 9.5
        ---@return nil
        function M_Mesh:PrepMovePoints()
        end
        --- | Ver: 12.0 | Ver: 14.0
        ---@param frame number Ver: 14.0
        ---@return nil
        function M_Mesh:PreserveHandlePositions(frame)
        end
        ---Raise a shape in the stacking order.
        --- | Ver: < 9.5
        ---@param id number Shape identifier | Ver: < 9.5
        ---@param toTop boolean True to raise it to the top, false to raise it one step | Ver: < 9.5
        ---@return nil
        function M_Mesh:RaiseShape(id, toTop)
        end
        ---Rotate the currently selected points.
        --- | Ver: < 9.5
        ---@param angle number Angle in radians | Ver: < 9.5
        ---@param centerVec Vector2 The center point of the rotation operation | Ver: < 9.5
        ---@return nil
        function M_Mesh:RotatePoints(angle, centerVec)
        end
        ---Scale the currently selected points.
        --- | Ver: < 9.5 | Ver: 12.0
        ---@param sx number X scale | Ver: < 9.5
        ---@param sy number Y scale | Ver: < 9.5
        ---@param centerVec Vector2 The center point of the scaling operation | Ver: < 9.5
        ---@param flipHandles boolean|nil Default: false | Ver: 12.0
        ---@return nil
        function M_Mesh:ScalePoints(sx, sy, centerVec, flipHandles)
        end
        ---Select all points.
        --- | Ver: < 9.5
        ---@return nil
        function M_Mesh:SelectAll()
        end
        ---Select all points that are connected by curves to any currently selected points.
        --- | Ver: < 9.5
        ---@return nil
        function M_Mesh:SelectConnected()
        end
        ---Returns the boundaries of the current selection.
        --- | Ver: < 9.5
        ---@param min Vector2 Minimum X and Y coordinates | Ver: < 9.5
        ---@param max Vector2 Maximum X and Y coordinates | Ver: < 9.5
        ---@return nil
        function M_Mesh:SelectedBounds(min, max)
        end
        ---Returns the location of the center point of the current group of selected points. Note that this is the mid point of the bounding box that contains the selected points (so that changing the selection by selecting or de-selecting points that are not at an x or y extremity will leave the reported centre point unchanged)
        --- | Ver: < 9.5
        ---@return Vector2 --center location
        function M_Mesh:SelectedCenter()
        end
        ---Select all the points in a given group.
        --- | Ver: < 9.5
        ---@param name string Name of the group to select | Ver: < 9.5
        ---@return nil
        function M_Mesh:SelectGroup(name)
        end
        ---Invert the current selection.
        --- | Ver: < 9.5
        ---@return nil
        function M_Mesh:SelectInverse()
        end
        ---deselects points and shapes
        --- | Ver: < 9.5
        ---@return nil
        function M_Mesh:SelectNone()
        end
        --- | Ver: 11.0
        ---@param b boolean Ver: 11.0
        ---@return nil
        function M_Mesh:SetAnimatedShapeOrder(b)
        end
        ---Sets the curve interpretation mode (legacy or v7+)
        ---See curve type constants . The getter method is M_Mesh : CurveInterpretation .
        --- | Ver: < 9.5
        ---@param interp number MOHO constant of the Curve Type | Ver: < 9.5
        ---@return nil
        function M_Mesh:SetCurveInterpretation(interp)
        end
        ---Access a shape in the mesh. The id is the "depth" in the stack of the shape and, for any particular shape, this could vary over time (e.g. "raise shape" or "lower to back" will change  the shape ids). (Contrast this to M_Shape : ShapeID which gives a unique and fixed identifier for a shape.)
        --- | Ver: < 9.5
        ---@param id number Shape identifier - shape 0 is at the back of the stack; shape (M_Mesh:CountShapes()-1) is at the top | Ver: < 9.5
        ---@return M_Shape --a shape object
        function M_Mesh:Shape(id)
        end
        --- | Ver: 11.0
        ---@param id number Ver: 11.0
        ---@return M_Shape
        function M_Mesh:ShapeByID(id)
        end
        --- | Ver: < 9.5
        ---@param name string Ver: < 9.5
        ---@return M_Shape
        function M_Mesh:ShapeByName(name)
        end
        --- | Ver: < 9.5
        ---@param shape M_Shape Ver: < 9.5
        ---@return number
        function M_Mesh:ShapeID(shape)
        end
        --- | Ver: < 9.5
        ---@param curveID number Ver: < 9.5
        ---@param angleTolerance number Ver: < 9.5
        ---@return nil
        function M_Mesh:SimplifyCurve(curveID, angleTolerance)
        end
        --- | Ver: 11.0
        ---@return nil
        function M_Mesh:StoreShapeOrder()
        end
        --- | Ver: < 9.5
        ---@param offset Vector2 Ver: < 9.5
        ---@param sx number Ver: < 9.5
        ---@param sy number Ver: < 9.5
        ---@param angle number Ver: < 9.5
        ---@param centerVec Vector2 Ver: < 9.5
        ---@return nil
        function M_Mesh:TransformPoints(offset, sx, sy, angle, centerVec)
        end
        ---Translate the currently selected points.
        --- | Ver: < 9.5
        ---@param offset Vector2 The distance to translate them | Ver: < 9.5
        ---@return nil
        function M_Mesh:TranslatePoints(offset)
        end
        --- | Ver: 13.5.2
        ---@return M_Shape
        function M_Mesh:UserEdgeShape()
        end
        --- | Ver: 10.1
        ---@param id number Ver: 10.1
        ---@return M_Point
        function M_Mesh:VisiblePoint(id)
        end
        ---Welds two points together. The point identified by movingID will move if necessary to line up with solidID. Not all welding operations are allowed, so this function will return the status of the weld.
        --- | Ver: < 9.5
        ---@param movingID number Point identifier | Ver: < 9.5
        ---@param solidID number Point identifier | Ver: < 9.5
        ---@param frame number Frame number | Ver: < 9.5
        ---@return boolean --true if successful, otherwise false
        function M_Mesh:WeldPoints(movingID, solidID, frame)
        end
    ---@class M_Mesh3D  
    ---The M_Mesh3D class represents a 3D object in a Mesh3DLayer.
    M_Mesh3D = {}
        ---This method accepts multiple input formats:
        ---M_Mesh3D:AddFace(p1, p2, p3) | M_Mesh3D:AddFace(p1, p2, p3, p4)
        ---Add a new three-sided or four-sided face to the mesh.
        --- | Ver: < 9.5
        ---@param arg1 number The first corner point | Ver: < 9.5
        ---@param arg2 number The second corner point | Ver: < 9.5
        ---@param arg3 number The third corner point | Ver: < 9.5
        ---@param arg4 number|nil The fourth corner point | Ver: < 9.5
        ---@return nil
        function M_Mesh3D:AddFace(arg1, arg2, arg3, arg4)
        end
        ---This method accepts multiple input formats:
        ---M_Mesh3D:AddFaceWithTexture(p1, p2, p3) | M_Mesh3D:AddFaceWithTexture(p1, p2, p3, p4)
        --- | Ver: 9.5
        ---@param arg1 number The first corner point | Ver: 9.5
        ---@param arg2 number The second corner point | Ver: 9.5
        ---@param arg3 number The third corner point | Ver: 9.5
        ---@param arg4 number|nil The fourth corner point | Ver: 9.5
        ---@return nil
        function M_Mesh3D:AddFaceWithTexture(arg1, arg2, arg3, arg4)
        end
        ---Adds a material to the mesh.
        --- | Ver: < 9.5
        ---@param m M_Material3D Material to add | Ver: < 9.5
        ---@return nil
        function M_Mesh3D:AddMaterial(m)
        end
        --- | Ver: < 9.5
        ---@param vec Vector3 Ver: < 9.5
        ---@return nil
        function M_Mesh3D:AddNormal(vec)
        end
        ---Add a new point to the mesh.
        --- | Ver: < 9.5
        ---@param vec Vector3 Point location | Ver: < 9.5
        ---@return number
        function M_Mesh3D:AddPoint(vec)
        end
        ---Add a new texture coordinate.
        --- | Ver: < 9.5
        ---@param vec Vector2 The value of the new texture coordinate | Ver: < 9.5
        ---@return nil
        function M_Mesh3D:AddTexturePoint(vec)
        end
        --- | Ver: < 9.5
        ---@param vec Vector3 Ver: < 9.5
        ---@return number
        function M_Mesh3D:AddUniquePoint(vec)
        end
        ---Completely empties the mesh of all points, faces, materials, etc.
        --- | Ver: < 9.5
        ---@return nil
        function M_Mesh3D:Clear()
        end
        ---Test whether front-facing 3D faces are aligned clockwise or counter-clockwise.
        --- | Ver: < 9.5
        ---@return boolean --true for clockwise, false for counter-clockwise
        function M_Mesh3D:Clockwise()
        end
        ---Return the number of faces in the 3D mesh.
        --- | Ver: < 9.5
        ---@return number --the number of faces
        function M_Mesh3D:CountFaces()
        end
        ---Returns the number of materials in the mesh.
        --- | Ver: < 9.5
        ---@return number --number of materials
        function M_Mesh3D:CountMaterials()
        end
        --- | Ver: < 9.5
        ---@return number
        function M_Mesh3D:CountNormals()
        end
        ---Returns the number of points in the mesh.
        --- | Ver: < 9.5
        ---@return number --number of points
        function M_Mesh3D:CountPoints()
        end
        ---Return the number of texture coordinates.
        --- | Ver: < 9.5
        ---@return number --the number of texture coordinates
        function M_Mesh3D:CountTexturePoints()
        end
        ---Creates a new material in the mesh.
        --- | Ver: < 9.5
        ---@param name string|nil Default: NULL | Ver: < 9.5
        ---@return M_Material3D --the properties of the new material
        function M_Mesh3D:CreateNewMaterial(name)
        end
        ---Get the default color for faces with no assigned material.
        --- | Ver: < 9.5
        ---@return rgb_color --default face color
        function M_Mesh3D:DefaultColor()
        end
        --- | Ver: < 9.5
        ---@return rgb_color
        function M_Mesh3D:DefaultEdgeColor()
        end
        --- | Ver: < 9.5
        ---@return number
        function M_Mesh3D:DefaultEdgeWidth()
        end
        ---Get the properties of a particular face in the mesh.
        --- | Ver: < 9.5
        ---@param id number Face identifier | Ver: < 9.5
        ---@return M_Face3D --a face in the mesh
        function M_Mesh3D:Face(id)
        end
        ---Gets the properties for a given material.
        --- | Ver: < 9.5
        ---@param id number Material identifier | Ver: < 9.5
        ---@return M_Material3D --a material in the mesh
        function M_Mesh3D:Material(id)
        end
        --- | Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@return Vector3
        function M_Mesh3D:Normal(id)
        end
        ---Get the position of a point in the mesh.
        --- | Ver: < 9.5
        ---@param id number Point identifier | Ver: < 9.5
        ---@return Vector3 --point location
        function M_Mesh3D:Point(id)
        end
        --- | Ver: < 9.5
        ---@return nil
        function M_Mesh3D:RebuildEdgeList()
        end
        --- | Ver: < 9.5
        ---@param overrideExisting boolean Ver: < 9.5
        ---@return nil
        function M_Mesh3D:RebuildNormals(overrideExisting)
        end
        ---Delete a face from the mesh.
        --- | Ver: < 9.5
        ---@param id number Face identifier | Ver: < 9.5
        ---@return nil
        function M_Mesh3D:RemoveFace(id)
        end
        ---Deletes a material from the mesh.
        --- | Ver: < 9.5
        ---@param id number Material identifier | Ver: < 9.5
        ---@return nil
        function M_Mesh3D:RemoveMaterial(id)
        end
        --- | Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@return nil
        function M_Mesh3D:RemoveNormal(id)
        end
        ---Remove a point from the mesh.
        --- | Ver: < 9.5
        ---@param id number Point identifier | Ver: < 9.5
        ---@return nil
        function M_Mesh3D:RemovePoint(id)
        end
        ---Remove a texture coordinate from the mesh.
        --- | Ver: < 9.5
        ---@param id number Texture coordinate identifier | Ver: < 9.5
        ---@return nil
        function M_Mesh3D:RemoveTexturePoint(id)
        end
        ---Scale an entire mesh to fit a given radius. The larger dimension in X, Y, or Z will be scaled to fit this radius, and the other dimensions will scale to maintain the correct proportions. Scaling to a radius of 1.0 is a quick way to make sure that very large or very small 3D models fit the Moho workspace in a reasonable way.
        --- | Ver: < 9.5
        ---@param r number Radius to scale to | Ver: < 9.5
        ---@return nil
        function M_Mesh3D:ScaleToRadius(r)
        end
        ---Set whether front-facing 3D faces are aligned clockwise or counter-clockwise.
        --- | Ver: < 9.5
        ---@param b boolean True for clockwise, false for counter-clockwise | Ver: < 9.5
        ---@return nil
        function M_Mesh3D:SetClockwise(b)
        end
        --- | Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@return nil
        function M_Mesh3D:SetCurMaterial(id)
        end
        ---Set the default color for faces with no assigned material.
        --- | Ver: < 9.5
        ---@param col rgb_color|nil Default face color | Ver: < 9.5
        ---@return nil
        function M_Mesh3D:SetDefaultColor(col)
        end
        --- | Ver: < 9.5
        ---@param col rgb_color Ver: < 9.5
        ---@return nil
        function M_Mesh3D:SetDefaultEdgeColor(col)
        end
        --- | Ver: < 9.5
        ---@param width number Ver: < 9.5
        ---@return nil
        function M_Mesh3D:SetDefaultEdgeWidth(width)
        end
        --- | Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@param vec Vector3 Ver: < 9.5
        ---@return nil
        function M_Mesh3D:SetNormal(id, vec)
        end
        ---Change an existing point's position.
        --- | Ver: < 9.5
        ---@param id number Point identifier | Ver: < 9.5
        ---@param vec Vector3 New position | Ver: < 9.5
        ---@return nil
        function M_Mesh3D:SetPoint(id, vec)
        end
        ---Get the value of a texture coordinate.
        --- | Ver: < 9.5
        ---@param id number Texture coordinate identifier | Ver: < 9.5
        ---@return Vector2 --the value of a texture coordinate
        function M_Mesh3D:TexturePoint(id)
        end
    ---@class M_Point  
    ---@field fAnimPos AnimVec2 --the point's position animation channel | Ver: < 9.5
    ---@field fColor AnimColor --Added in version 11 | Ver: 11.0
    ---@field fColorDrift AnimVal --Added in version 14.3 | Ver: < 9.5
    ---@field fColored boolean --Added in version 11 | Ver: 11.0
    ---@field fColorStrength AnimVal --Added in version 11 | Ver: 11.0
    ---@field fHidden boolean --Added in version 10 | Ver: 10.0
    ---@field fOpacity AnimVal --Added in version 14.3 | Ver: < 9.5
    ---@field fParent number --id number of the parent bone this point is bound to. A value of -1 means the point is not bound to any bone. A value of -2 means the point is flexi-bound to all bones in the parent layer. | Ver: < 9.5
    ---@field fPos Vector2 --the point's current position | Ver: < 9.5
    ---@field fPrevSelected boolean --true if the point was previously selected, otherwise false | Ver: < 9.5
    ---@field fSelected boolean --true if the point is selected, otherwise false | Ver: < 9.5
    ---@field fTempColorDrift number --Added in version 14.3 | Ver: < 9.5
    ---@field fTempOpacity number --Added in version 14.3 | Ver: < 9.5
    ---@field fTempPos Vector2 --a temporary variable for storing position, useful when transforming points | Ver: < 9.5
    ---@field fTempWidth number --a temporary variable to store line width | Ver: < 9.5
    ---@field fWidth AnimVal --the width of curves that pass through this point (-1 means that curves aren't affected by variable width) | Ver: < 9.5
    ---The M_Point class represents a single control point in a 2D vector layer.
    M_Point = {}
        ---Returns the number of curves that pass through the point. Sometimes one mesh point can be used in a curve for several times. Each use will be counted as a separate curve index.
        --- | Ver: < 9.5
        ---@return number --the number of curves that pass through the point
        function M_Point:CountCurves()
        end
        ---Returns a curve that passes through the point and the point id in that curve | To call this it is necessary to define and assign any value to "where" - the value will be overwritten by the call where = -1
        --- | Ver: < 9.5
        ---@param id number The curve's id (0 is the first curve passing through the point) | Ver: < 9.5
        ---@param where number The point id in the relevant M_Curve - see M_Curve : Point | Ver: < 9.5
        ---@return M_Curve, number
        function M_Point:Curve(id, where)
        end
        --- | Ver: 12.0
        ---@param frame number Ver: 12.0
        ---@return nil
        function M_Point:FlipControlHandles(frame)
        end
        ---Returns which curve, and which segment of that curve, this point is an endpoint of.
        --- | Ver: < 9.5
        ---@param curveID number The curve identifier | Ver: < 9.5
        ---@param segID number Which segment of that curve | Ver: < 9.5
        ---@return number, number
        function M_Point:GetEndpointEdge(curveID, segID)
        end
        ---Tests whether this point is the endpoint of a curve
        --- | Ver: < 9.5
        ---@return boolean --true if endpoint, false if the point is in the middle of a curve
        function M_Point:IsEndpoint()
        end
        --- | Ver: < 9.5
        ---@param curveID number Ver: < 9.5
        ---@param segID number|nil Default: -1 | Ver: < 9.5
        ---@return boolean
        function M_Point:IsPointOnCurve(curveID, segID)
        end
        --- | Ver: 12.0
        ---@param frame number Ver: 12.0
        ---@return nil
        function M_Point:ResetControlHandles(frame)
        end
        ---Sets the curvature of all curves that pass through this point.
        --- | Ver: < 9.5
        ---@param curvature number Curvature value | Ver: < 9.5
        ---@param frame number Frame number | Ver: < 9.5
        ---@return nil
        function M_Point:SetCurvature(curvature, frame)
        end
        ---Set the position of the point at a given time.
        --- | Ver: < 9.5
        ---@param pos Vector2 Position | Ver: < 9.5
        ---@param frame number Frame number | Ver: < 9.5
        ---@return nil
        function M_Point:SetPos(pos, frame)
        end
    ---@class M_PointGroup 
    ---@field fName String --Ver: < 9.5 
    ---The M_PointGroup class represents a named group of points in a vector mesh.
    M_PointGroup = {}
        --- | Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@return nil
        function M_PointGroup:AddPointID(id)
        end
        --- | Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@return boolean
        function M_PointGroup:ContainsPointID(id)
        end
        ---Returns the number of points in the group.
        --- | Ver: < 9.5
        ---@return number --the number of points in the group
        function M_PointGroup:CountPoints()
        end
        ---Returns the name of the group.
        --- | Ver: < 9.5
        ---@return string --the name of the group
        function M_PointGroup:Name()
        end
        ---Returns a point in the group.
        --- | Ver: < 9.5
        ---@param id number The id number of the point (0 is the first point in the group) | Ver: < 9.5
        ---@return M_Point --a point in the group
        function M_PointGroup:Point(id)
        end
    ---@class M_Shape  
    ---@field f3DThickness AnimVal --Ver: < 9.5
    ---@field fComboBlend AnimVal --Added in version 14.0 | Ver: 14.0
    ---@field fComboMode number --Added in version 14.0. See MOHO combo constants. | Ver: 14.0
    ---@field fEffectOffset AnimVec2 --Ver: < 9.5
    ---@field fEffectRotation AnimVal --Ver: < 9.5
    ---@field fEffectScale AnimVal --Ver: < 9.5
    ---@field fFillAllowed boolean --True if a fill is allowed, whether the shape actually has a fill or not. | Ver: < 9.5
    ---@field fHasFill boolean --True if the shape is filled, otherwise false. | Ver: < 9.5
    ---@field fHasOutline boolean --True if the shape has an outline, otherwise false. | Ver: < 9.5
    ---@field fHidden boolean --Ver: < 9.5
    ---@field fInheritedStyle M_Style --Ver: < 9.5
    ---@field fInheritedStyle2 M_Style --Ver: < 9.5
    ---@field fInheritedStyleName String --Starting in AS11, these actually refer to style UUIDs, not names. The variable name here hasn't changed in order to not break the scripting interface. | Ver: < 9.5
    ---@field fInheritedStyleName2 String --Starting in AS11, these actually refer to style UUIDs, not names. The variable name here hasn't changed in order to not break the scripting interface. | Ver: < 9.5
    ---@field fMyStyle M_Style --The shape's style properties. The style defines a shape's fill color, line width, etc. | Ver: < 9.5
    ---@field fName String --Name of the shape. | Ver: < 9.5
    ---@field fSelected boolean --True if the shape is selected, otherwise false. | Ver: < 9.5
    ---The M_Shape class represents a visible shape in Moho. A shape can either be a fill, an outline, or both.
    M_Shape = {}
        ---Ver: 10.0
        ---@param curveID number Ver: 10.0
        ---@param segID number Ver: 10.0
        ---@return nil
        function M_Shape:AddEdge(curveID, segID)
        end
        ---Ver: < 9.5 | Ver: 14.0
        ---@return boolean
        function M_Shape:AllPointsSelected()
        end
        ---Ver: 11.0
        ---@param shape M_Shape Ver: 11.0
        ---@return boolean
        function M_Shape:ArePropertiesEqual(shape)
        end
        ---Ver: 14.0
        ---@return M_Shape
        function M_Shape:BottomOfCluster()
        end
        ---Ver: 11.0
        ---@param pos Vector2 Ver: 11.0
        ---@return rgb_color
        function M_Shape:ColorAtPoint(pos)
        end
        ---Ver: 14.0
        ---@param curveID number Ver: 14.0
        ---@return boolean
        function M_Shape:ContainsCurve(curveID)
        end
        ---Ver: < 9.5
        ---@param curveID number Ver: < 9.5
        ---@param segID number Ver: < 9.5
        ---@return boolean
        function M_Shape:ContainsEdge(curveID, segID)
        end
        ---Ver: < 9.5
        ---@param pointID number Ver: < 9.5
        ---@return boolean
        function M_Shape:ContainsPoint(pointID)
        end
        ---Ver: 13.5.2
        ---@return boolean
        function M_Shape:ContinuousTriangulation()
        end
        --- Copies the style properties from another shape.
        --- | Ver: < 9.5
        ---@param fromShape M_Shape Ver: < 9.5
        ---@param skipFill boolean|nil Default: false | Ver: < 9.5
        ---@param skipLine boolean|nil Default: false | Ver: < 9.5
        ---@return nil
        function M_Shape:CopyStyleProperties(fromShape, skipFill, skipLine)
        end
        --- Returns the number of edges that make up this shape.
        --- | Ver: < 9.5
        ---@return number --the number of edges that make up this shape
        function M_Shape:CountEdges()
        end
        --- Returns the number of points in the shape
        --- | Ver: 11.0
        ---@return number --Number of points in the shape
        function M_Shape:CountPoints()
        end
        ---Ver: 14.0
        ---@param edgeID number Ver: 14.0
        ---@return BBox
        function M_Shape:EdgeBounds(edgeID)
        end
        ---Ver: < 9.5
        ---@return Vector2
        function M_Shape:EffectHandle1()
        end
        ---Ver: < 9.5
        ---@return Vector2
        function M_Shape:EffectHandle2()
        end
        --- Returns information about a specific edge. Given an edge on the shape, this function returns the curve id and segment id of the actual curve in the underlying mesh.
        --- It requires the input arguments to be defined and initialised before the call:
        --- For example:
        --- local curveID, segID = -1, -1 -- any value will do
        --- curveID, segID = M_Shape:GetEdge(edgeID, curveID, segID)
        --- | Ver: < 9.5
        ---@param edgeID number The id number of the edge (0 for the first edge in a shape) | Ver: < 9.5
        ---@param curveID number The curve id | Ver: < 9.5
        ---@param segID number Which segment of the curve is this edge | Ver: < 9.5
        ---@return number, number --curveID, segID
        function M_Shape:GetEdge(edgeID, curveID, segID)
        end
        --- This will throw error "M_Shape:GetPoint - out of range" and return -1 unless you first call: M_Shape:CountPoints()
        --- | Ver: 11.0
        ---@param pointID number Point ID in shape, starting from 0 | Ver: 11.0
        ---@return number --Point ID in mesh
        function M_Shape:GetPoint(pointID)
        end
        ---Ver: 11.0
        ---@return boolean
        function M_Shape:HasPointColors()
        end
        --- | Ver: < 9.5 | Ver: 14.2
        ---@param fillOrStroke number|nil Default: 2 | Ver: 14.2
        ---@return boolean
        function M_Shape:HasPositionDependentStyles(fillOrStroke)
        end
        ---Ver: 14.0
        ---@return boolean
        function M_Shape:IsInCluster()
        end
        ---Removes all applied effects/styles and not-basic customized properties from a shape.
        ---Ver: < 9.5
        ---@return nil
        function M_Shape:MakePlain()
        end
        ---Ver: < 9.5
        ---@return string
        function M_Shape:Name()
        end
        ---Ver: 14.0
        ---@return M_Shape
        function M_Shape:NextInCluster()
        end
        ---Ver: 14.0
        ---@return M_Shape
        function M_Shape:PreviousInCluster()
        end
        ---Removes all inherited styles from the shape.
        --- | Ver: < 9.5
        ---@return nil
        function M_Shape:RemoveStyles()
        end
        ---Ver: 14.0
        ---@return nil
        function M_Shape:SelectAllPoints()
        end
        ---Ver: < 9.5
        ---@param name string Ver: < 9.5
        ---@return nil
        function M_Shape:SetName(name)
        end
        ---This method accepts multiple input formats:
        ---M_Shape:ShapeBounds(min, max) | M_Shape:ShapeBounds(min, max, frame) | M_Shape:ShapeBounds()
        --- | Ver: < 9.5 | Ver: 14.0
        ---@param arg1 Vector2|nil Ver: < 9.5
        ---@param arg2 Vector2|nil Ver: < 9.5
        ---@param arg3 number|nil Ver: < 9.5
        ---@return nil|BBox --nil in Ver: < 9.5 | BBox in Ver: 14.0
        function M_Shape:ShapeBounds(arg1, arg2, arg3)
        end
        ---Ver: < 9.5
        ---@return Vector2
        function M_Shape:ShapeCenter()
        end
        ---A unique identifier for the shape.
        ------This function returns a unique ID for the shape. This ID could be anything - after a bunch of editing, adding and deleting shapes, you might have three shapes with IDs of 246, 7, and 42. The unique ID is used internally for animated shape order. It is not the same as the shapeID relevant to id = moho:CreateShape mesh:Shape (id) id = mesh:ShapeID(shape) It is the shapeID relevant to id =shapeID() shape = mesh:Shape---This function returns a unique ID for the shape. This ID could be anything - after a bunch of editing, adding and deleting shapes, you might have three shapes with IDs of 246, 7, and 42. The unique ID is used internally for animated shape order. It is not the same as the shapeID relevant to id = moho:CreateShape mesh:Shape (id) id = mesh:ShapeID(shape) It is the shapeID relevant to id =shapeID() shape = mesh:Shape
        --- | Ver: 11.0
        ---@return number --A unique identifier for the shape
        function M_Shape:ShapeID()
        end
        ---Ver: 14.0
        ---@return M_Shape
        function M_Shape:TopOfCluster()
        end
    ---@class M_Skeleton 
    ---@field fBindingMode number --Which automatic binding mode should be used for this skeleton. See the binding mode constants. | Ver: < 9.5 
    ---The M_Skeleton class represents the actual skeleton structure that is encapsulated by a bone layer.
    M_Skeleton = {}
        ---Create a new bone and add it to the skeleton.
        --- | Ver: < 9.5
        ---@param frame number Ver: < 9.5
        ---@return M_Bone --the newly-created bone
        function M_Skeleton:AddBone(frame)
        end
        ---Ver: 13.5.2
        ---@param frame number Ver: 13.5.2
        ---@return M_BoneGroup
        function M_Skeleton:AddGroup(frame)
        end
        ---Access a particular bone in the skeleton.
        --- | Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@return M_Bone --a bone object
        function M_Skeleton:Bone(id)
        end
        ---Ver: < 9.5
        ---@param name string Ver: < 9.5
        ---@return M_Bone
        function M_Skeleton:BoneByName(name)
        end
        ---Given a bone object, this function returns the identifier value for that bone in the skeleton.
        --- | Ver: < 9.5
        ---@param bone M_Bone A bone in the skeleton | Ver: < 9.5
        ---@return number --bone identifier
        function M_Skeleton:BoneID(bone)
        end
        ---Returns the number of child bones attached to the given bone.
        --- | Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@param ignoreControlledBones boolean|nil Default: false | Ver: < 9.5
        ---@return number --number of children bones
        function M_Skeleton:CountBoneChildren(id, ignoreControlledBones)
        end
        ---Returns the number of bones in the skeleton.
        --- | Ver: < 9.5
        ---@return number --number of bones
        function M_Skeleton:CountBones()
        end
        ---Ver: 13.5.2
        ---@return number
        function M_Skeleton:CountGroups()
        end
        ---Ver: 10.0
        ---@return number
        function M_Skeleton:CountVisibleBones()
        end
        ---Delete a bone from the skeleton.
        --- | Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@param recursion number|nil Default: 0 | Ver: < 9.5
        ---@return nil
        function M_Skeleton:DeleteBone(id, recursion)
        end
        ---Ver: 13.5.2
        ---@param group M_BoneGroup Ver: 13.5.2
        ---@return nil
        function M_Skeleton:DeleteGroup(group)
        end
        ---Ver: 13.5.2
        ---@param id number Ver: 13.5.2
        ---@return nil
        function M_Skeleton:DeleteGroupByID(id)
        end
        ---Ver: 13.5.2
        ---@return M_BoneGroup
        function M_Skeleton:FirstSelectedGroup()
        end
        ---Flips the given bone at frame zero (and all the following animation angles and positions of it and its children). Added in version 11.
        --- | Ver: 11.0
        ---@param id number Ver: 11.0
        ---@param horizontal boolean|nil Ver: 11.0
        ---@return nil
        function M_Skeleton:FlipBone(id, horizontal)
        end
        ---Return the first child of the given bone.
        --- | Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@param ignoreControlledBones boolean|nil Default: false | Ver: < 9.5
        ---@return number --bone identifier
        function M_Skeleton:GetFirstChildBone(id, ignoreControlledBones)
        end
        ---Ver: 13.5.2
        ---@param id number Ver: 13.5.2
        ---@return M_BoneGroup
        function M_Skeleton:Group(id)
        end
        ---Ver: 13.5.2
        ---@param boneID number Ver: 13.5.2
        ---@param considerParents boolean|nil Default: true | Ver: 13.5.2
        ---@return M_BoneGroup
        function M_Skeleton:GroupForBone(boneID, considerParents)
        end
        ---Runs the inverse kinematics solver in order to move a chain of bones to reach a specified target point. The specified bone will move as necessary in order for its tip to touch the given target.
        --- | Ver: < 9.5
        ---@param boneID number Ver: < 9.5
        ---@param target Vector2 Ver: < 9.5
        ---@param iterMultiplier number|nil Default: 1 | Ver: < 9.5
        ---@param allowTwoBoneShortcut boolean|nil Default: false | Ver: 10.0
        ---@param allowBoneStretching boolean|nil Default: false | Ver: 10.0
        ---@return nil
        function M_Skeleton:IKAngleSolver(boneID, target, iterMultiplier, allowTwoBoneShortcut, allowBoneStretching)
        end
        ---Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@return boolean
        function M_Skeleton:IsAncestorSelected(id)
        end
        ---Ver: 13.5.2
        ---@param boneID number Ver: 13.5.2
        ---@return boolean
        function M_Skeleton:IsBoneActiveInAGroup(boneID)
        end
        ---Test whether one bone is being pointed by any other(s) in the skeleton at the specified frame.
        --- | Ver: 10.0
        ---@param id number Ver: 10.0
        ---@param frame number Ver: 10.0
        ---@return boolean --true if the provided bone is being the target of any other bone(s), otherwise false
        function M_Skeleton:IsBoneATarget(id, frame)
        end
        ---Test whether one bone is a decendant of another.
        --- | Ver: < 9.5
        ---@param boneID number Ver: < 9.5
        ---@param childID number Ver: < 9.5
        ---@return boolean --true if the childID bone is decended from boneID, otherwise false
        function M_Skeleton:IsBoneChild(boneID, childID)
        end
        ---Ver: 12.0
        ---@param boneID number Ver: 12.0
        ---@param childID number Ver: 12.0
        ---@return boolean
        function M_Skeleton:IsBoneIKChild(boneID, childID)
        end
        ---Ver: 13.5.2
        ---@param boneID number Ver: 13.5.2
        ---@return boolean
        function M_Skeleton:IsBoneInAGroup(boneID)
        end
        ---Test whether one bone is an ancestor of another. Returns true even if both arguments are equal.
        --- | Ver: < 9.5
        ---@param boneID number Ver: < 9.5
        ---@param parentID number Ver: < 9.5
        ---@return boolean --true if parentID is an ancestor of boneID, otherwise false
        function M_Skeleton:IsBoneParent(boneID, parentID)
        end
        ---Locks the given bone at a specified time.
        --- | Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@param frame number Ver: < 9.5
        ---@return nil
        function M_Skeleton:LockBone(id, frame)
        end
        ---Ver: 12.0
        ---@param id number Ver: 12.0
        ---@return nil
        function M_Skeleton:MakeBoneNameUnique(id)
        end
        ---Ver: 13.5.2
        ---@return nil
        function M_Skeleton:MarkGroupVisibilityUnknown()
        end
        ---Returns the closest bone to a 2D vector position.
        --- | Ver: < 9.5
        ---@param vec Vector2 A 2D position in the bone layer | Ver: < 9.5
        ---@return number --bone identifier
        function M_Skeleton:NearestBone(vec)
        end
        ---Ver: < 9.5
        ---@return nil
        function M_Skeleton:SelectAll()
        end
        ---Ver: < 9.5
        ---@return nil
        function M_Skeleton:SelectConnected()
        end
        ---Returns the identifier of the currently selected bone.
        --- | Ver: < 9.5
        ---@return number --bone identifier
        function M_Skeleton:SelectedBoneID()
        end
        ---Ver: < 9.5
        ---@return nil
        function M_Skeleton:SelectInverse()
        end
        ---Ver: < 9.5
        ---@return nil
        function M_Skeleton:SelectNone()
        end
        ---Given a bone identifier, this function returns the identifier of the bone it is pointing at in the skeleton at the specified frame.
        --- | Ver: 10.0
        ---@param id number Ver: 10.0
        ---@param frame number Ver: 10.0
        ---@return number --bone identifier
        function M_Skeleton:TargetOfBone(id, frame)
        end
        ---Ver: 10.0
        ---@param id number Ver: 10.0
        ---@param frame number Ver: 10.0
        ---@return number
        function M_Skeleton:TargetOfBoneChain(id, frame)
        end
        ---Unlocks the given bone at a specified time.
        --- | Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@param frame number Ver: < 9.5
        ---@return nil
        function M_Skeleton:UnlockBone(id, frame)
        end
        ---Updates the transformation matrices for the given bone. Use this after changing a bone's position or rotation.
        --- | Ver: < 9.5
        ---@param id number|nil Bone identifier | Default: -1 | Ver: < 9.5
        ---@return nil
        function M_Skeleton:UpdateBoneMatrix(id)
        end
    ---@class M_Style  
    ---@field fBrushAlign boolean --Ver: < 9.5
    ---@field fBrushAngleDrift number --Added in version 11 | Ver: 11.0
    ---@field fBrushHueDrift number --Added in version 14.3 | Ver: < 9.5
    ---@field fBrushIsColor boolean --Ver: < 9.5
    ---@field fBrushJitter number --Ver: < 9.5
    ---@field fBrushMergedAlpha boolean --Added in version 11 | Ver: 11.0
    ---@field fBrushName String --Ver: < 9.5
    ---@field fBrushRandomInterval number --Added in version 14.0 | Ver: 14.0
    ---@field fBrushRandomize boolean --Ver: < 9.5
    ---@field fBrushRandomOrder boolean --Added in version 11 | Ver: 11.0
    ---@field fBrushSatDrift number --Added in version 14.3 | Ver: < 9.5
    ---@field fBrushSizeVariationAmp number --Added in version 14.0 | Ver: 14.0
    ---@field fBrushSizeVariationScale number --Added in version 14.0 | Ver: 14.0
    ---@field fBrushSpacing number --Ver: < 9.5
    ---@field fBrushTint boolean --Ver: < 9.5
    ---@field fBrushValDrift number --Added in version 14.3 | Ver: < 9.5
    ---@field fDefineFillCol boolean --True if this style defines a fill color, otherwise false | Ver: < 9.5
    ---@field fDefineLineCol boolean --True if this style defines a line color, otherwise false | Ver: < 9.5
    ---@field fDefineLineWidth boolean --True if this style defines a line width, otherwise false | Ver: < 9.5
    ---@field fFillCol AnimColor --The style's fill color | Ver: < 9.5
    ---@field fLineCaps number --0 means none, 1 - round | Ver: < 9.5
    ---@field fLineCol AnimColor --The style's line color | Ver: < 9.5
    ---@field fLineWidth number --The style's line width | Ver: < 9.5
    ---@field fName String --The name of the style. | Ver: < 9.5
    ---@field fUUID String --Added in version 11 | Ver: 11.0
    ---The M_Style class represents the visual style of a shape, either the shape's own style or an inherited style.
    M_Style = {}
        ---Ver: 11.0
        ---@param style M_Style Ver: 11.0
        ---@return boolean
        function M_Style:ArePropertiesEqual(style)
        end
        ---Ver: 14.0
        ---@param gradientType number See gradient constants | Ver: 14.0
        ---@param allowTransparency boolean|nil Default: false | Ver: 14.0
        ---@return nil
        function M_Style:SetGradient(gradientType, allowTransparency)
        end
        ---Ver: 14.0
        ---@param position number Ver: 14.0
        ---@param color rgb_color Ver: 14.0
        ---@return nil
        function M_Style:SetGradientColor(position, color)
        end
        ---	Applies a halo effect
        ---Ver: < 9.5
        ---@param haloRadius number Ver: < 9.5
        ---@param blurRadius number Ver: < 9.5
        ---@param haloColor rgb_color Ver: < 9.5
        ---@param haloOnly boolean Ver: < 9.5
        ---@return nil
        function M_Style:SetHalo(haloRadius, blurRadius, haloColor, haloOnly)
        end
        ---Applies a shading effect.
        ---Ver: < 9.5
        ---@param angle number Shading angle | Ver: < 9.5
        ---@param offset number Shading offset | Ver: < 9.5
        ---@param blur number Blur radius | Ver: < 9.5
        ---@param color rgb_color Shading color | Ver: < 9.5
        ---@return nil
        function M_Style:SetShading(angle, offset, blur, color)
        end
        ---Applies a drop shadow effect
        ---Ver: < 9.5
        ---@param angle number Ver: < 9.5
        ---@param offset number Ver: < 9.5
        ---@param blur number Ver: < 9.5
        ---@param color rgb_color Ver: < 9.5
        ---@return nil
        function M_Style:SetShadow(angle, offset, blur, color)
        end
        ---Applies a soft edge effect.
        ---Ver: < 9.5
        ---@param radius number Radius of the blur | Ver: < 9.5
        ---@return nil
        function M_Style:SetSoftEdge(radius)
        end
        ---Applies a halo effect to the stroke
        ---Ver: < 9.5
        ---@param haloRadius number Inset radius of the halo | Ver: < 9.5
        ---@param blurRadius number Blur radius of the halo | Ver: < 9.5
        ---@param haloColor rgb_color Color of the halo | Ver: < 9.5
        ---@param haloOnly boolean True if only the halo should be visible, otherwise false | Ver: < 9.5
        ---@return nil
        function M_Style:SetStrokeHalo(haloRadius, blurRadius, haloColor, haloOnly)
        end
        ---	Applies a soft edge effect to the stroke
        ---Ver: < 9.5
        ---@param radius number Radius of the soft edge | Ver: < 9.5
        ---@return nil
        function M_Style:SetStrokeSoftEdge(radius)
        end
    ---@class PoserActorGroup 
    ---@field fExternalName String --Ver: < 9.5
    ---@field fFigureName String --Ver: < 9.5
    ---@field fInternalName String --Ver: < 9.5
    ---@field fSelectedParameter number --Ver: < 9.5
    ---NOTE! This class is no longer available in version 13.5.2.  
    ---Version added: before 9.5
    PoserActorGroup = {}
        ---Ver: < 9.5
        ---@return number
        function PoserActorGroupCountParameters()
        end
        ---Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@param createIfMissing boolean Ver: < 9.5
        ---@return AnimVal
        function PoserActorGroupParameter(id, createIfMissing)
        end
        ---Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@return number
        function PoserActorGroupParameterMaxVal(id)
        end
        ---Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@return number
        function PoserActorGroupParameterMinVal(id)
        end
        ---Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@return string
        function PoserActorGroupParameterName(id)
        end
        ---Ver: < 9.5
        ---@return number
        function PoserActorGroupPrimaryParameterID()
        end
    ---@class ScriptInterface  
    ---@field document MohoDoc --The current document object open in Moho | Ver: < 9.5
    ---@field drawingFrame number --Added in version 11 | Ver: 11.0
    ---@field drawingLayer MohoLayer --Added in version 11. This is the vector layer where drawn paths / shapes will be placed and, especially when using frame-by-frame, is not necessarily the layer that is shown as active in the Layers window | Ver: 11.0
    ---@field drawingLayerFrame number --Added in version 11 | Ver: 11.0
    ---@field frame number --The current frame in the timeline | Ver: < 9.5
    ---@field gridOn boolean --True if the grid is turned on in the editing view, otherwise false | Ver: < 9.5
    ---@field gridSize number --The size of one grid unit | Ver: < 9.5
    ---@field layer MohoLayer --The currently selected layer in the Layers window | Ver: < 9.5
    ---@field layerFrame number --the frame relative to the start of the layer (if the layer has not been moved in the sequencer this will be the same as the timeline frame) | Ver: < 9.5
    ---@field view MohoView --The editing view | Ver: < 9.5
    ---The ScriptInterface class is a script's way of interacting with the Moho application itself.  
    ---When a script is invoked (by a mouse click, a key press, or a menu command), it is provided with an object called "moho" - this object is an instance of the ScriptInterface class.
    ScriptInterface = {}
        --- Adds a point motion keyframe to all the currently selected vector points.
        --- | Ver: < 9.5
        ---@param frame number Frame number (moho timeline, not layer frame) | Ver: < 9.5
        ---@param layer MohoLayer|nil Default: nil | Ver: < 9.5
        ---@param allSelectedKeys boolean|nil Default: false | Ver: < 9.5
        ---@return nil
        function ScriptInterface:AddPointKeyframe(frame, layer, allSelectedKeys)
        end
        ---Ver: 11.1
        ---@param alignMessage string See the description for possible values. | Ver: 11.1
        ---@return nil
        function ScriptInterface:AlignLayers(alignMessage)
        end
        ---	Returns the path to Moho's application data
        --- Path to the top-level application data directory.
        --- Moho's application data includes standard brushes, templates, character wizard elements and the like.
        --- There are files that are common to both Debut and Pro -> variantDir = false or omitted.
        --- There are also files that are specific to the variant (Debut or Pro) -> variantDir = true
        --- | Ver: < 9.5 | Ver: 12.0
        ---@param variantDir boolean|nil Added in version 12 | Default: false | Ver: 12.0
        ---@return string
        function ScriptInterface:AppDir(variantDir)
        end
        ---Added in version 9.5 - Gets the AS version number as a text string
        --- Examples of how to get the version as a decimal number (can be useful if the version is something like "10.1.1", which cannot be converted to a number using "tonumber").
        --- ex1 - strip off the second and any later point versions: if moho.AppVersion ~= nil then
        ---  local sVersion = string.gsub(moho:AppVersion(), "^(%d+)(%.%d+)(%..+)", "%1%2")
        ---  version = tonumber(sVersion)
        --- end
        --- ex2 - builds a decimal number assuming point versions will not be greater than 9 (i.e. nn.9.9.9.9)
        --- local order = 1
        --- local numVers = 0
        --- local n
        --- local vers = moho:AppVersion() -- assumes AS9.5 or later
        --- for n in string.gmatch (vers, "%d+%.*") do
        ---     numVers = numVers + (n * order)
        ---     order = order / 10
        --- end
        --- print (numVers)
        --- | Ver: 9.5
        ---@return string
        function ScriptInterface:AppVersion()
        end
        ---Ver: < 9.5
        ---@param dir string Ver: < 9.5
        ---@param listHiddenAndSystemFiles boolean|nil Default: true | Ver: < 9.5
        ---@return nil
        function ScriptInterface:BeginFileListing(dir, listHiddenAndSystemFiles)
        end
        ---Ver: 11.0
        ---@return string --"space" if "Classic point editing keyboard shortcuts" is enabled in the preferences, and "enter" if not (default). Invoke as MOHO.BindingKeyName()
        function ScriptInterface:BindingKeyName()
        end
        ---Ver: 12.0
        ---@param ch AnimChannel Ver: 12.0
        ---@return AnimBool
        function ScriptInterface:ChannelAsAnimBool(ch)
        end
        ---Ver: 12.0
        ---@param ch AnimChannel Ver: 12.0
        ---@return AnimColor
        function ScriptInterface:ChannelAsAnimColor(ch)
        end
        ---Ver: 12.0
        ---@param ch AnimChannel Ver: 12.0
        ---@return AnimString
        function ScriptInterface:ChannelAsAnimString(ch)
        end
        ---Ver: 12.0
        ---@param ch AnimChannel Ver: 12.0
        ---@return AnimVal
        function ScriptInterface:ChannelAsAnimVal(ch)
        end
        ---Ver: 12.0
        ---@param ch AnimChannel Ver: 12.0
        ---@return AnimVec2
        function ScriptInterface:ChannelAsAnimVec2(ch)
        end
        ---Ver: 12.0
        ---@param ch AnimChannel Ver: 12.0
        ---@return AnimVec3
        function ScriptInterface:ChannelAsAnimVec3(ch)
        end
        ---Ver: < 9.5
        ---@param layer MohoLayer Ver: < 9.5
        ---@param showAlert boolean Ver: < 9.5
        ---@return boolean
        function ScriptInterface:CheckIfLocked(layer, showAlert)
        end
        ---Play a little clicking sound. This is currently used as feedback for welding operations.
        --- | Ver: < 9.5
        ---@return nil
        function ScriptInterface:Click()
        end
        ---Ver: 9.5
        ---@return string
        function ScriptInterface:ClipboardText()
        end
        ---	Basically the same as pressing Ctrl+C
        ---If you call this method without any arguments it does the same thing as pressing Ctrl+C - copies the selected keyframes, bones, or points into the clipboard.
        --- | Ver: < 9.5
        ---@param mesh M_Mesh|nil Default: nil | Ver: < 9.5
        ---@return nil
        function ScriptInterface:Copy(mesh)
        end
        ---Ver: < 9.5
        ---@param mesh M_Mesh|nil Default: nil | Ver: < 9.5
        ---@return nil
        function ScriptInterface:CopyAlternate(mesh)
        end
        ---Ver: 9.5
        ---@param color rgb_color Ver: 9.5
        ---@return nil
        function ScriptInterface:CopyColor(color)
        end
        --- Place the text on the clipboard.
        --- | Ver: 9.5
        ---@param text string Ver: 9.5
        ---@return nil
        function ScriptInterface:CopyText(text)
        end
        ---Ver: < 9.5
        ---@return number
        function ScriptInterface:CountAudioLayers()
        end
        --- Returns the number of bones in the current bone layer.
        --- | Ver: < 9.5
        ---@return number
        function ScriptInterface:CountBones()
        end
        --- Returns the number of curves in the current vector layer.
        --- | Ver: < 9.5
        ---@return number
        function ScriptInterface:CountCurves()
        end
        --- Returns the number of edges in the current vector layer. (An edge is one segment of a curve between two control points.)
        --- | Ver: < 9.5
        ---@return number
        function ScriptInterface:CountEdges()
        end
        ---Ver: 12.0
        ---@return number
        function ScriptInterface:CountFilledShapes()
        end
        --- Returns the number of points in the current vector layer.
        --- | Ver: < 9.5
        ---@return number
        function ScriptInterface:CountPoints()
        end
        --- Returns the number of selected bones in the current bone layer.
        --- | Ver: < 9.5
        ---@param updateCount boolean|nil Default: false | Ver: < 9.5
        ---@return number
        function ScriptInterface:CountSelectedBones(updateCount)
        end
        ---Ver: < 9.5
        ---@param updateCount boolean|nil Default: false | Ver: < 9.5
        ---@return number
        function ScriptInterface:CountSelectedCurves(updateCount)
        end
        --- Returns the number of selected edges in the current vector layer. (An edge is considered selected if both points at the ends of the edge are selected.)
        --- | Ver: < 9.5
        ---@param updateCount boolean|nil Default: false | Ver: < 9.5
        ---@return number
        function ScriptInterface:CountSelectedEdges(updateCount)
        end
        ---Ver: 12.0
        ---@return number
        function ScriptInterface:CountSelectedFilledShapes()
        end
        --- Returns the number of selected points in the current vector layer.
        --- | Ver: < 9.5
        ---@param updateCount boolean|nil Default: false | Ver: < 9.5
        ---@return number
        function ScriptInterface:CountSelectedPoints(updateCount)
        end
        --- Returns the number of selected shapes in the current vector layer.
        --- | Ver: < 9.5
        ---@param updateCount boolean|nil Default: false | Ver: < 9.5
        ---@return number
        function ScriptInterface:CountSelectedShapes(updateCount)
        end
        --- Returns the number of shapes in the current vector layer.
        --- | Ver: < 9.5
        ---@return number
        function ScriptInterface:CountShapes()
        end
        --- Creates a new layer, inserts it directly above the currently selected layer in the Layers window, and makes it the selected layer.
        --- | Ver: < 9.5 | Ver: 10.0
        ---@param layerType number A layer type code. See Layer_Type constants | Ver: < 9.5
        ---@param undoable boolean|nil Default: true | Ver: 10.0
        ---@return MohoLayer
        function ScriptInterface:CreateNewLayer(layerType, undoable)
        end
        --- Creates a shape based on the currently selected vector points. Not all shapes are "legal" (for example, non-closed fill shapes), so if this function fails it will return -1.
        --- | Ver: < 9.5
        ---@param filled boolean True to create a filled shape, false to create an outline only | Ver: < 9.5
        ---@param behindNeighborStrokes boolean|nil Default: false | Ver: < 9.5
        ---@param frame number|nil Default: -1000000 | Ver: < 9.5
        ---@param checkForBadShapes boolean|nil Default: true | Ver: < 9.5
        ---@param skipDuplicateStrokes boolean|nil Default: false | Ver: < 9.5
        ---@param skipFill boolean|nil Default: false | Ver: < 9.5
        ---@param skipLine boolean|nil Default: false | Ver: < 9.5
        ---@return number --id number of the new shape
        function ScriptInterface:CreateShape(filled, behindNeighborStrokes, frame, checkForBadShapes, skipDuplicateStrokes, skipFill, skipLine)
        end
        ---Ver: < 9.5
        ---@return nil
        function ScriptInterface:CreateTextObject()
        end
        ---Ver: 9.5
        ---@return M_Style
        function ScriptInterface:CurrentEditStyle()
        end
        ---Ver: 12.0
        ---@return string
        function ScriptInterface:CurrentTool()
        end
        ---Ver: < 9.5
        ---@param layer MohoLayer Ver: < 9.5
        ---@return nil
        function ScriptInterface:DeleteLayer(layer)
        end
        ---Deletes selected layers
        ---Ver: < 9.5
        ---@return nil
        function ScriptInterface:DeleteMultipleLayers()
        end
        ---Ver: 11.0
        ---@return nil
        function ScriptInterface:DeselectShapes()
        end
        ---Ver: < 9.5
        ---@param doc MohoDoc Ver: < 9.5
        ---@return nil
        function ScriptInterface:DestroyDocument(doc)
        end
        ---Ver: < 9.5
        ---@return boolean
        function ScriptInterface:DisableDrawingTools()
        end
        ---When the user specifies things like line width, blur radius, and shadow offset in Moho, they enter a value in pixels.
        ---However, behind the scenes, these values are stored in "document coordinates".
        ---This allows an animation to be rendered at different resolutions while still looking correct.
        ---This function will convert between document coordinates and pixels.
        --- | Ver: < 9.5
        ---@param doc number A distance in document coordinates | Ver: < 9.5
        ---@return number --the equivalent distance in pixels
        function ScriptInterface:DocToPixel(doc)
        end
        ---Ver: 11.0
        ---@return M_Mesh
        function ScriptInterface:DrawingMesh()
        end
        ---Ver: 10.0
        ---@param g LM_Graphics Ver: 10.0
        ---@param mesh M_Mesh Ver: 10.0
        ---@param shapeID number Ver: 10.0
        ---@return nil
        function ScriptInterface:DrawShape(g, mesh, shapeID)
        end
        ---Duplicates the given layer, placing the new copy above the old one and making it the selected layer.
        --- | Ver: < 9.5 | Ver: 11.0
        ---@param layer MohoLayer An existing layer to duplicate | Ver: < 9.5
        ---@param byReference boolean|nil Default: false | Ver: 11.0
        ---@return MohoLayer --the new layer
        function ScriptInterface:DuplicateLayer(layer, byReference)
        end
        ---Doesn't seem to do anything.
        ---Ver: < 9.5
        ---@return nil
        function ScriptInterface:EditLayerSettings()
        end
        ---Ver: 12.0
        ---@return string
        function ScriptInterface:ExporterPath()
        end
        ---Ver: 11.0
        ---@return nil
        function ScriptInterface:FileClose()
        end
        ---Ver: < 9.5
        ---@param path string Ver: < 9.5
        ---@param mode number|nil Only applies to Anime files: 0=import as copy, 1=import as reference, 2=ask the user. | Default: 0 | Ver: 11.1
        ---@return nil
        function ScriptInterface:FileImport(path, mode)
        end
        ---Ver: < 9.5
        ---@return nil
        function ScriptInterface:FileNew()
        end
        ---Ver: < 9.5
        ---@param path string Ver: < 9.5
        ---@return nil
        function ScriptInterface:FileOpen(path)
        end
        ---Render current frame
        ---When called with a valid path that includes a filename and extension that is recognised as a "simple" image type (i.e. .png, .jpg, .bmp) this function will create an image file with the correct attributes (e.g. transparency for .png) in the nominated directory as well as saving an image file in the render cache.
        ---Otherwise, the function will open the render window as though the File / Preview command had been invoked.
        --- | Ver: < 9.5
        ---@param path string Ver: < 9.5
        ---@return nil
        function ScriptInterface:FileRender(path)
        end
        ---Ver: < 9.5
        ---@return nil
        function ScriptInterface:FileSave()
        end
        ---Ver: < 9.5
        ---@param path string Ver: < 9.5
        ---@return nil
        function ScriptInterface:FileSaveAs(path)
        end
        ---Fills in an LM_TextList interface object with the list of available fonts on the system.
        --- | Ver: < 9.5
        ---@param list LM_TextList Ver: < 9.5
        ---@return nil
        function ScriptInterface:FillInFontList(list)
        end
        ---Ver: < 9.5
        ---@param id number Ver: < 9.5
        ---@return AudioLayer
        function ScriptInterface:GetAudioLayer(id)
        end
        ---Iterate next file in directory during iteration started with ScriptInterface:BeginFileListing
        --- | Ver: < 9.5
        ---@return string
        function ScriptInterface:GetNextFile()
        end
        ---Ver: 14.0
        ---@return boolean
        function ScriptInterface:HasMaximumSmartBones()
        end
        ---Ver: 14.0
        ---@return boolean
        function ScriptInterface:HasMaximumWarpLayers()
        end
        ---Import an EPS or Adobe Illustrator file into Moho, creating a new vector layer to hold it.
        --- | Ver: < 9.5
        ---@param path string Pathname to the imported file | Ver: < 9.5
        ---@return nil
        function ScriptInterface:ImportEPS(path)
        end
        ---This method accepts multiple input formats:
        ---ScriptInterface:InsertText(text, font, fill, stroke, groupTogether, centerH, lineOffset) | ScriptInterface:InsertText(textParams, lineOffset)
        ---the first one is Deprecated. Use the new InsertText method instead that receives two arguments.
        ---Creates a new text object in the currently active vector layer. See the ScriptInterface:FillInFontList method for information on how to get a list of the available fonts.
        ---for textParams you can create a struct: "typedef struct { int32 Font; LM_String FontName; LM_String Text; int32 Justification; bool Fill; bool Stroke; bool OneFill; bool CenterH; rgb_color LineCol; rgb_color FillCol; real LineWidth; int32 Leading; int32 Kerning; bool MakeLayer; LM_String BalloonStyle; bool BalloonFill; bool BalloonStroke; int32 BalloonSize; real BalloonLineWidth; rgb_color BalloonLineCol; rgb_color BalloonFillCol; bool BalloonSizeProportional; LM_String BalloonPoses; int32 TextSize; bool BalloonFlipV; bool BalloonFlipH; } InsertTextParams;"
        ---then you use it to Add text to a mesh layer:: "local param = MOHO.MohoGlobals.InsertText; param.Text:Set(\"Hello\"); param.OneFill = true; moho:InsertText(param, 0);"
        --- | Ver: < 9.5
        ---@param arg1 string |InsertTextParams The text to create | See InsertTextParams description | Ver: < 9.5
        ---@param arg2 string|number The name of the font to use. Leave empty if first argument is InsertTextParams | Ver: < 9.5
        ---@param arg3 boolean|nil True to group all characters as a single fill shape, false to fill them separately. Leave empty if first argument is InsertTextParams | Ver: < 9.5
        ---@param arg4 boolean|nil True to center the text horizontally, false to left-justify. Leave empty if first argument is InsertTextParams | Ver: < 9.5
        ---@param arg5 boolean|nil How many lines down to offset the text (used to create multi-line text objects). Leave empty if first argument is InsertTextParams | Ver: < 9.5
        ---@param arg6 boolean|nil Leave empty if first argument is InsertTextParams | Ver: < 9.5
        ---@param arg7 number|nil Leave empty if first argument is InsertTextParams | Ver: < 9.5
        ---@return nil
        function ScriptInterface:InsertText(arg1, arg2, arg3, arg4, arg5, arg6, arg7)
        end
        ---Ver: < 9.5
        ---@return boolean
        function ScriptInterface:IsCopyable()
        end
        ---Ver: < 9.5
        ---@return boolean
        function ScriptInterface:IsPasteable()
        end
        ---Ver: < 9.5
        ---@return boolean
        function ScriptInterface:IsPlaying()
        end
        ---Ver: 10.0
        ---@return boolean
        function ScriptInterface:IsPro()
        end
        --- Converts a generic layer object into a 3D layer.
        --- | Ver: < 9.5
        ---@param layer MohoLayer The layer to cast | Ver: < 9.5
        ---@return Mesh3DLayer --a Mesh3DLayer object
        function ScriptInterface:LayerAs3D(layer)
        end
        ---Ver: < 9.5
        ---@param layer MohoLayer Ver: < 9.5
        ---@return AudioLayer
        function ScriptInterface:LayerAsAudio(layer)
        end
        ---Ver: < 9.5
        ---@param layer MohoLayer Ver: < 9.5
        ---@return BoneLayer
        function ScriptInterface:LayerAsBone(layer)
        end
        --- Converts a generic layer object into a group layer.
        --- | Ver: < 9.5
        ---@param layer MohoLayer The layer to cast | Ver: < 9.5
        ---@return GroupLayer --a GroupLayer object
        function ScriptInterface:LayerAsGroup(layer)
        end
        --- Converts a generic layer object into an image layer.
        --- The layer is not modified - this is equivalent to "casting" from one type of pointer to another in C.
        --- Some functions return a generic layer object, even if the layer is really an image layer - use this function if you need to work with ImageLayer-specific functions.
        --- | Ver: < 9.5
        ---@param layer MohoLayer The layer to cast | Ver: < 9.5
        ---@return ImageLayer --an ImageLayer object
        function ScriptInterface:LayerAsImage(layer)
        end
        ---Ver: < 9.5
        ---@param layer MohoLayer Ver: < 9.5
        ---@return NoteLayer
        function ScriptInterface:LayerAsNote(layer)
        end
        --- Converts a generic layer object into a particle layer.
        --- | Ver: < 9.5
        ---@param layer MohoLayer The layer to cast | Ver: < 9.5
        ---@return ParticleLayer --a ParticleLayer object
        function ScriptInterface:LayerAsParticle(layer)
        end
        --- Converts a generic layer object into a switch layer.
        --- | Ver: < 9.5
        ---@param layer MohoLayer The layer to cast | Ver: < 9.5
        ---@return SwitchLayer --a SwitchLayer object
        function ScriptInterface:LayerAsSwitch(layer)
        end
        ---Ver: < 9.5
        ---@param layer MohoLayer Ver: < 9.5
        ---@return MeshLayer
        function ScriptInterface:LayerAsVector(layer)
        end
        ---Ver: 12.2
        ---@return string
        function ScriptInterface:LayersWindowGetSearchContext()
        end
        ---Ver: 12.2
        ---@return string
        function ScriptInterface:LayersWindowGetSearchContextValue()
        end
        ---Ver: 12.2
        ---@param context string See the search context constants | Ver: 12.2
        ---@return nil
        function ScriptInterface:LayersWindowSetSearchContext(context)
        end
        ---Ver: 12.2
        ---@param val string Ver: 12.2
        ---@return nil
        function ScriptInterface:LayersWindowSetSearchContextValue(val)
        end
        ---Ver: < 9.5
        ---@param path string Ver: < 9.5
        ---@return MohoDoc
        function ScriptInterface:LoadDocument(path)
        end
        ---Returns the 2D mesh associated with the currently active layer. If the active layer is not a vector layer, then this function returns nil.
        --- | Ver: < 9.5
        ---@return M_Mesh --a 2D mesh object
        function ScriptInterface:Mesh()
        end
        ---Returns the 3D mesh object associated with the current layer. If the current layer is not a 3D layer, this function returns nil.
        --- | Ver: < 9.5
        ---@return M_Mesh3D --a 3D mesh object
        function ScriptInterface:Mesh3D()
        end
        ---Tells the timeline window to display a new keyframe in the specified animation channel. This does not actually add the keyframe - you need to do that separately - it just displays the keyframe in the timeline. See the animation channel constants.
        --- | Ver: < 9.5
        ---@param channel number Which animation channel | Ver: < 9.5
        ---@return nil
        function ScriptInterface:NewKeyframe(channel)
        end
        ---Ver: 9.5
        ---@return rgb_color
        function ScriptInterface:NewShapeFillColor()
        end
        ---Ver: 9.5
        ---@return rgb_color
        function ScriptInterface:NewShapeLineColor()
        end
        ---Returns what the line width would be for a new shape. This is based on the user settings in the Style window.
        --- | Ver: < 9.5
        ---@return number --line width for new shapes
        function ScriptInterface:NewShapeLineWidth()
        end
        ---Ver: < 9.5
        ---@return M_Shape
        function ScriptInterface:NewShapeProperties()
        end
        ---Generates a semi-random vector.
        --- | Ver: < 9.5
        ---@param pos Vector3 Location to generate the random vector at (nearby locations will have similar "random" vectors) | Ver: < 9.5
        ---@param amp number The average amplitude of the generated vector | Ver: < 9.5
        ---@param freq number The frequency of the noise | Ver: < 9.5
        ---@return Vector3 --the random vector
        function ScriptInterface:NoiseVector(pos, amp, freq)
        end
        ---Ver: < 9.5
        ---@return nil
        function ScriptInterface:OpenPoserPalette()
        end
        ---Returns the skeleton object associated with the parent layer of the current layer. If there is no parent layer, or the parent layer is not a bone layer, this function returns nil.
        --- | Ver: < 9.5
        ---@return M_Skeleton --a skeleton object
        function ScriptInterface:ParentSkeleton()
        end
        ---	Basically the same as pressing Ctrl+V
        ---This method does the same thing as pressing Ctrl+V - pastes the keyframes, bones, or points from the clipboard.
        --- | Ver: < 9.5
        ---@return nil
        function ScriptInterface:Paste()
        end
        ---Ver: < 9.5
        ---@return nil
        function ScriptInterface:PasteAlternate()
        end
        ---Ver: < 9.5
        ---@param fromShape M_Shape Ver: < 9.5
        ---@return nil
        function ScriptInterface:PickStyleProperties(fromShape)
        end
        ---When the user specifies things like line width, blur radius, and shadow offset in Moho, they enter a value in pixels. However, behind the scenes, these values are stored in "document coordinates". This allows an animation to be rendered at different resolutions while still looking correct. This function will convert between pixels and document coordinates.
        --- | Ver: < 9.5
        ---@param pixel number A distance in pixels | Ver: < 9.5
        ---@return number --the equivalent distance in document coordinates
        function ScriptInterface:PixelToDoc(pixel)
        end
        ---Moves one layer behind (or below) another in the layer ordering
        --- This method can also be used for moving a layer out of its group: moho:PlaceLayerBehindAnother(moho.layer, moho.layer:Parent())
        --- | Ver: < 9.5
        ---@param moveLayer MohoLayer The layer to move | Ver: < 9.5
        ---@param behindThis MohoLayer The layer to place it behind | Ver: < 9.5
        ---@return nil
        function ScriptInterface:PlaceLayerBehindAnother(moveLayer, behindThis)
        end
        ---Moves a layer into a group
        --- To move a layer out of its group, you can use the ScriptInterface:PlaceLayerBehindAnother method.
        --- | Ver: < 9.5 | Ver: 10.0
        ---@param child MohoLayer The layer to move | Ver: < 9.5
        ---@param group MohoLayer The group layer to put it in | Ver: < 9.5
        ---@param top boolean|nil Default: true | Ver: < 9.5
        ---@param isUndoable boolean|nil Default: true | Ver: 10.0
        ---@return nil
        function ScriptInterface:PlaceLayerInGroup(child, group, top, isUndoable)
        end
        ---Ver: 11.0
        ---@return nil
        function ScriptInterface:PurgeTracingImage()
        end
        ---Ver: < 9.5
        ---@param toShape M_Shape Ver: < 9.5
        ---@return nil
        function ScriptInterface:PushStyleProperties(toShape)
        end
        ---Ver: < 9.5
        ---@return nil
        function ScriptInterface:Quit()
        end
        ---Ver: 11.0
        ---@return boolean
        function ScriptInterface:RestoreTracingImage()
        end
        ---Ver: 11.0
        ---@return nil
        function ScriptInterface:SaveTracingImage()
        end
        ---Returns the first selected shape, but starting in version 10 there may be multiple selected shapes. (This method added in version 10)
        ---Ver: 10.0
        ---@return M_Shape
        function ScriptInterface:SelectedShape()
        end
        --- Set the current time.
        --- | Ver: < 9.5 | Ver: 9.5
        ---@param frame number Frame number | Ver: < 9.5
        ---@param outAccess LM_SecureAccess|nil Default: nil | Ver: 9.5
        ---@return nil
        function ScriptInterface:SetCurFrame(frame, outAccess)
        end
        ---Ver: 11.0
        ---@param width number Ver: 11.0
        ---@return nil
        function ScriptInterface:SetNewShapeLineWidth(width)
        end
        ---	Changes which layer is selected in the Layers window
        ---You also have to call ScriptInterface:UpdateUI to update display of Layers window.
        ---If the layer is in the closed group You can call ScriptInterface:ShowLayerInLayersPalette to open the group and show the layer.
        ---This action includes updating UI too.
        --- | Ver: < 9.5
        ---@param layer MohoLayer New selected layer | Ver: < 9.5
        ---@param multiSelect boolean|nil If false, all previously selected layers will be deselected. | Default: false | Ver: < 9.5
        ---@param allowDeselect boolean|nil Default: true | Ver: < 9.5
        ---@return nil
        function ScriptInterface:SetSelLayer(layer, multiSelect, allowDeselect)
        end
        ---Ver: < 9.5
        ---@param layer MohoLayer Ver: < 9.5
        ---@return nil
        function ScriptInterface:ShowLayerInLayersPalette(layer)
        end
        ---Returns the skeleton object associated with the current layer. If the current layer is not a bone layer, this function returns nil.
        --- | Ver: < 9.5
        ---@return M_Skeleton --a skeleton object
        function ScriptInterface:Skeleton()
        end
        ---Takes a vector position and "snaps" it to the nearest grid intersection.
        --- | Ver: < 9.5
        ---@param v Vector2 The vector to "snap" | Ver: < 9.5
        ---@return nil
        function ScriptInterface:SnapToGrid(v)
        end
        ---Ver: 10.0
        ---@param frame number Ver: 10.0
        ---@param gapFilling number|nil Default: 0 | Ver: 12.0
        ---@return number
        function ScriptInterface:TraceWhitePixels(frame, gapFilling)
        end
        ---Selects the points that are bound to the currently selected bone.
        --- | Ver: < 9.5
        ---@return nil
        function ScriptInterface:UpdateBonePointSelection()
        end
        ---Updates the display of "selected" channels in the timeline. (Like selected points, selected bone, selected shape, etc.) Use this function if your script changes which object(s) are selected.
        --- | Ver: < 9.5
        ---@return nil
        function ScriptInterface:UpdateSelectedChannels()
        end
        ---Force the user interface to redraw itself. This is in case certain elements have become disabled or enabled due to the changing state of the Moho document.
        --- | Ver: < 9.5
        ---@return nil
        function ScriptInterface:UpdateUI()
        end
        ---Ver: < 9.5
        ---@return string
        function ScriptInterface:UserAppDir()
        end
        ---	The path to the directory structure that holds User Content.
        ---The User Content directory contains several folders with (moho) system-specified names.
        ---Moho looks in these folders for additional and replacement functionality that the user chooses.
        ---Examples include Scripts/Tools where custom tool scripts can be stored Brushes Swatches see also the Moho user guide
        --- | Ver: 13.5.2
        ---@return string
        function ScriptInterface:UserContentDir()
        end
        ---Ver: 13.5.2
        ---@return string
        function ScriptInterface:UserPrefsDir()
        end
        ---Ver: 13.5.2
        ---@return string
        function ScriptInterface:UserPrefsFile()
        end
        MOHO.ScriptInterfaceHelper  = {}
            ------instances of ScriptInterfaceHelper class can be created by this method locally. Unlike most objects, it must be explicitly deleted when you're done using it.
            ---example: MOHO.ScriptInterfaceHelper:new_local()
            ---local moho = helper:MohoObject()
            ---print(moho:CountPoints())
            ---helper:delete()
            ---Ver: 14.0
            ---@return ScriptInterfaceHelper  
            function MOHO.ScriptInterfaceHelper:new_local()
            end
            ---Ver: 14.0
            ---@return nil
            function MOHO.ScriptInterfaceHelper:delete()
            end
    ---@class ScriptInterfaceHelper  
    ---The ScriptInterfaceHelper class is a bit special. It allows you to access sensitive resources that must be released when you're done with them. Unlike most objects, it must be explicitly deleted using delete method when you're done using it.  
    ---Version added: 14.0
    ScriptInterfaceHelper = {}
        ---e.g: moho = helper:MohoObject()
        ---Ver: 14.0
        ---@return ScriptInterface
        function ScriptInterfaceHelper:MohoObject()
        end
    ---@class ScriptPrefs  
    ---The ScriptPrefs class allows user scripts to store and retrieve settings. These stored settings are re-loaded each time Moho is run.  
    ---Stored values are stored under a key name (a text string). Typically, so that different scripts don't interfere with one another, a script should use its own name as part of the key.  
    ---For example, if the "MyFirstScript" script wanted to save the "pointCount" setting, it would save it using the key "MyFirstScript.pointCount".
    ScriptPrefs = {}
        ---Retrieve a boolean value that was previously stored.
        --- | Ver: < 9.5
        ---@param key string The key to locate the value | Ver: < 9.5
        ---@param defaultValue boolean A default value to return if the key cannot be found | Ver: < 9.5
        ---@return boolean --the stored value
        function ScriptPrefs:GetBool(key, defaultValue)
        end
        ---Ver: < 9.5
        ---@param key string Ver: < 9.5
        ---@param defaultValue number Ver: < 9.5
        ---@return number
        function ScriptPrefs:GetFloat(key, defaultValue)
        end
        ---Ver: < 9.5
        ---@param key string Ver: < 9.5
        ---@param defaultValue number Ver: < 9.5
        ---@return number
        function ScriptPrefs:GetInt(key, defaultValue)
        end
        ---Ver: < 9.5
        ---@param key string Ver: < 9.5
        ---@param defaultValue string Ver: < 9.5
        ---@return string
        function ScriptPrefs:GetString(key, defaultValue)
        end
        ---Store a boolean value.
        --- | Ver: < 9.5
        ---@param key string The key to locate the value | Ver: < 9.5
        ---@param value boolean The value to store | Ver: < 9.5
        ---@return nil
        function ScriptPrefs:SetBool(key, value)
        end
        ---Ver: < 9.5
        ---@param key string Ver: < 9.5
        ---@param value number Ver: < 9.5
        ---@return nil
        function ScriptPrefs:SetFloat(key, value)
        end
        ---Ver: < 9.5
        ---@param key string Ver: < 9.5
        ---@param value number Ver: < 9.5
        ---@return nil
        function ScriptPrefs:SetInt(key, value)
        end
        ---Ver: < 9.5
        ---@param key string Ver: < 9.5
        ---@param value string Ver: < 9.5
        ---@return nil
        function ScriptPrefs:SetString(key, value)
        end
    ---@class TrackingPoint
    ---@field fAnimPos AnimVec2 --Ver: < 9.5
    ---@field fCompareRadius number --Ver: < 9.5
    ---@field fLastFrame number --Ver: < 9.5
    ---@field fSearchRadius number --Ver: < 9.5
    ---@field fSelected boolean --Ver: < 9.5
    TrackingPoint = {}
        ---Ver: < 9.5
        ---@return string
        function TrackingPoint:Name()
        end
        ---Ver: < 9.5
        ---@param str string Ver: < 9.5
        ---@return nil
        function TrackingPoint:SetName(str)
        end
        ---Ver: < 9.5
        ---@return number
        function TrackingPoint:StartFrame()
        end

    ---@class XGraphics  
    ---The XGraphics class provides no additional functions for scripts to use at this time.  
    ---LM_Graphics > LM_Graphics_OpenGL_Fixed > XGraphics  
    ---Version added: before 9.5
    LM_Graphics.LM_Graphics_OpenGL_Fixed.XGraphics = {}

    















    


    




    
    
    
    
    
    


    





    
    
